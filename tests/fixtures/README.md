# Planted-defect fixtures

`S-0.7` builds a repository here containing deliberately planted defects, each
with a known measurement signature:

- an N+1 query
- a quadratic loop
- an over-fetch
- a slow import

This is the single most useful test asset in the project. Grow it whenever a
real repository surprises you — a surprise that is not captured here will
surprise us again.
