# flight_controller — a planted real-time subject

Built for S-2.8. Every marker here is deliberate and the code is not real: it
exists so the screening has a subject with genuine RTOS imports, deadline
annotations, certification markers and framework signatures to refuse.

Its control is `../task_tracker`, which uses every innocent word this detector
must not fire on. Read that one before widening any pattern here.
