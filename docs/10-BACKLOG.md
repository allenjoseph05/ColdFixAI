# 10 — IMPLEMENTATION BACKLOG

**Epics, stories, and acceptance criteria — build this ticket by ticket**

Read `00-BRIEF.md` first for scope and the authority map. This file is the execution plan; where it conflicts with the build order in `00-BRIEF.md` §5, this file wins because it is more granular.

---

## How to use this

**Story format:**

```
S-x.y — Title
Depends: prerequisite stories
Why: one line
AC: testable acceptance criteria
Notes: gotcha carried from the design docs
```

**Rules for whoever implements:**

- A story is done when every AC is demonstrably true, not when the code looks right.
- Every `Notes` line exists because a design pass found a failure mode. Do not skip them — they are the expensive knowledge in this document.
- Stories marked **SPIKE** produce a decision or a finding, not shippable code. Timebox them.
- Stories marked **SAFETY** must ship before any story that depends on them, without exception.

**Milestones:**

| Milestone | Epics | What exists at the end |
|---|---|---|
| M1 — Viability | E0 | Three cheap experiments that could invalidate the design |
| M2 — Useful without AI | E1–E5 | A tool that finds performance problems with zero model calls |
| M3 — The thesis | E6–E8 | An agent that switches instruments when a hypothesis fails |
| M4 — The contribution | E9–E12 | Adversarial verification and durable orchestration |
| M5 — Generalization | E13–E17 | Memory, adapters, evaluation, reporting |

**Stop at the end of M2 and reassess.** If the deterministic core cannot find real problems in real repos, no amount of agent work fixes that.

---

# EPIC 0 — Foundations and viability

**Goal:** resolve open decisions and run the three experiments that could invalidate the architecture.

**Why first:** each spike costs a day and any of them can change what gets built. Running them after two months of work is how projects die.

### S-0.1 — Repository scaffold
Depends: none
Why: everything else needs a home.
AC:
- Python 3.12+ project with `pyproject.toml`, dependency locking, and a package layout matching the epic structure (`bench/`, `primitives/`, `agents/`, `orchestrator/`, `adapters/`, `eval/`)
- `ruff` and `mypy` configured and passing on an empty project
- `pytest` runs and reports zero tests without error
- `README.md` states the project's one-line claim from `00-BRIEF.md` §1

Notes: Python is the right choice — the first adapter targets Django, and the instrumentation hooks are Python-native. Cross-language support arrives via MCP in E14, not by writing the core twice.

### S-0.2 — Architecture decision records
Depends: S-0.1
Why: seven decisions are currently implicit and will be re-litigated otherwise.
AC:
- ADR directory with one file per decision
- ADR-001: implementation language and why
- ADR-002: LLM SDK and provider strategy (including the different-vendor requirement for the Adversary)
- ADR-003: persistence — SQLite for checkpoints in dev, Postgres for concurrent campaigns; separate store for persistent data
- ADR-004: sandboxing approach (Docker vs alternatives)
- ADR-005: first target framework (Django + Postgres) and why
- ADR-006: how the tool tests itself (see S-0.7)
- ADR-007: the refusal list and its rationale, copied from `00-BRIEF.md` §3

Notes: ADR-002 must record that Surgeon and Adversary should run on different model vendors where possible. If that is deferred, record it as a known limitation rather than dropping it.

### S-0.3 — SPIKE: can we ground real repositories?
Depends: S-0.1
Why: **this is the highest-risk assumption in the entire design.**
AC:
- Pick three arbitrary Django projects from GitHub that are not tutorials
- For each, by hand: stand up a database, run migrations, seed data, hit one endpoint that returns real data
- Record for each: time taken, number of distinct obstacles, whether fixtures existed
- Write a findings note: which obstacles recurred, which were unique

Notes: if this is hard by hand it is much harder for an agent. If two of three fail, reconsider the target framework or the workload-discovery approach before building E7.

### S-0.4 — SPIKE: does ablation produce clean deltas?
Depends: S-0.3
Why: ablation is the most-used primitive; if its measurements are noisy the design's core is unsound.
AC:
- Monkeypatch a serializer method in a running Django app to return a recorded value
- Measure the endpoint 20 times patched and 20 times unpatched, interleaved
- Report coefficient of variation for each condition and whether the delta is statistically separable
- Repeat with an empty-collection stub and compare — do the two stub strategies give materially different numbers?

Notes: the second half of this AC is what motivates S-3.4's record-and-replay requirement. Confirm it empirically before building on it.

Result (2026-08-02, `spikes/S-0.4-ablation/FINDINGS.md`): confirmed, with a twist. The two stub strategies were indistinguishable on **timing** (p=0.64) while differing **6x in payload size** — so the premise holds but wall time alone cannot show it, because the ablated component was database-bound and the downstream work it fed was cheap. Timing-only measurement would have concluded the strategy does not matter and deleted S-3.4's recording requirement. Ablation delta itself was clean: 1454.73ms to 434.64ms, Cliff's delta -1.000, against a detection floor of ~20ms.

### S-0.5 — SPIKE: is state reset reliable?
Depends: S-0.3
Why: every experiment assumes a clean starting state.
AC:
- Seed, run a workload, roll back — ten cycles
- Assert row counts across all tables are identical every cycle
- Test with transaction rollback, and separately with a database snapshot restore
- Record which strategy is reliable and how long each takes

Notes: sequence counters, cached querysets, and connection-level state commonly survive a rollback. Check for those specifically.

Result (2026-08-02, `spikes/S-0.5-reset/FINDINGS.md`): **the first AC is too weak and would have shipped a broken reset.** Plain rollback kept row counts, content hashes and max ids identical across all ten cycles while leaving sequences permanently advanced (`helpdesk_ticket_id_seq` 509→759). Assert sequence values too, not just row counts. Rollback + explicit `setval` is correct at 19ms, against 163ms for a template copy and 2022ms for a dump restore — and needs no exclusive access, so unlike `template` it composes with concurrent experiments. The note is right about sequences and cached querysets; **Postgres session state does not survive a rollback** (`SET` is reverted on abort). The cached-queryset leak is untouched by every database-side strategy, so the reset contract has to cover process state as well.

### S-0.6 — Target repository selection
Depends: S-0.3
Why: development needs a fixed subject with known problems.
AC:
- One repository chosen and vendored or pinned by commit
- At least one known performance defect documented with its expected measurement signature
- A second repository chosen as a holdout, never used during development

Notes: the holdout matters — developing and evaluating against the same repo produces a tool that works on exactly one repo.

Result (2026-08-02, ADR 011, pins in `targets.toml`): target `django-helpdesk` @ `3a22901` — nested N+1 on `/api/tickets/`, 1193 queries for 100 tickets, scaling as `1 + T + F + T`, ablation ratio 0.29x against a ~20ms floor, **plus a second N+1 underneath it** (504 customfield queries, invisible until the first is ablated). Reserve `netbox` @ `4877d11` for S-17.3.

**The holdout is named in ADR 011 and `targets.toml`, and deliberately not here.** It was chosen because its endpoint is *already correct* — a holdout containing a defect only tests generalization, while one where the right answer is "nothing found" tests whether the system can resist manufacturing a finding. `tests/test_holdout_discipline.py` enforces that it stays out of everything except the files listed there, and it caught two real references while S-0.6 was being written — including this line's first draft. Reference the ADR rather than repeating the name.

### S-0.7 — Test strategy for the tool itself
Depends: S-0.2
Why: a system whose job is verification must be verifiable.
AC:
- Unit tests for the lab bench against synthetic programs with known complexity
- A fixture repository under `tests/fixtures/` containing deliberately planted defects: an N+1, a quadratic loop, an over-fetch, a slow import
- Golden-file tests for evidence chain serialization
- A mock LLM client that replays recorded responses, so agent logic is testable without API calls

Notes: the planted-defect fixture repo is the single most useful test asset in the project. Build it early and grow it whenever a real repo surprises you.

**Status (2026-08-02): partially delivered — one AC of four. The other three are blocked on code that does not exist, and the story should be split.**

Delivered: the planted-defect fixture repository, in `tests/fixtures/`. Six query defects and controls, five complexity functions, a slow/fast import pair, and 25 tests asserting every documented signature. It goes beyond the four defects listed above with two additions the spikes demanded — a **decoy** with a high constant query cost that must *not* be flagged (the netbox shape from S-0.3), and a component whose **downstream** work dominates, which is the case S-0.4 could not test and S-3.4 most needs. Every defect is paired with a control, because a detector that always answers "N+1" passes a fixture that only contains defects.

Blocked, with the blocker in each case:

| AC | Blocked on |
|---|---|
| Unit tests for the lab bench | **E1** — the lab bench does not exist. There is nothing to unit-test. |
| Golden-file tests for evidence chain serialization | **S-4.1** — the evidence chain schema is not defined, so there is no serialization to freeze. |
| A mock LLM client replaying recorded responses | **S-0.2 / ADR-002** — the SDK and provider strategy are undecided, and writing a mock against a guessed interface is the speculative abstraction `CLAUDE.md` forbids. |

The dependency line above (`Depends: S-0.2`) is also partly circular: S-0.2's ADR-006 is *"how the tool tests itself"*, which is this story's subject. ADR-006 should be written from S-0.7's outcome rather than before it.

**Recommendation:** split into `S-0.7a` (the fixture repository — done) and `S-0.7b` (lab-bench unit tests, golden files, mock LLM client), with S-0.7b resequenced to depend on S-0.2, E1 and S-4.1. Nothing is lost by waiting; the fixture repo is the piece E1 needs on day one, and it is available now.

**S-0.7b — MOCK LLM CLIENT DONE (2026-08-11)** — `src/coldfix/llm/client.py`, ADR 069, 21 tests in `tests/llm/test_model_client.py`. Taken up because **all three of its dependencies (S-0.2, E1, S-4.1) are done**, so the deferral's own reason — *writing a mock against a guessed interface is the speculative abstraction `CLAUDE.md` forbids* — has expired rather than been overridden, and because Epic 7 is the first epic with agent logic and `CLAUDE.md` requires it be tested against exactly this. **A recording is a real API response, validated by the vendor's own model**: the store holds JSON that `anthropic.types.Message` parses and building one runs it through that model, so a payload the API could not have returned **fails to load** rather than being replayed — the project's own lesson (*a test double more forgiving than the real thing turns a structural assertion into a decoration*) applied to what will be its most-used double, and free, since the SDK's response types validate offline with no network and no key. **Both clients share one `translate`**, so a test passing against the double exercised the same parsing the real client uses. **An unrecorded request is refused, never answered** — a default would make every agent test pass while testing the default — and the refusal names the four things that look identical from the call site. **The model is part of the request identity**, because S-5.5 routes the same step to different tiers, which is exactly when a recording for one model would otherwise serve another. **`text` is never `content[0].text`**: a refusal carries an *empty* content list (HTTP 200, `stop_reason: "refusal"`) and adaptive thinking puts a `thinking` block first; `stop_reason` is carried so `refused` and `truncated` are answerable, since a double that always reports `end_turn` hides a decline until production. **The replaying client holds no vendor client at all**, so *no test hits a real API* is structural. **Epic 5 has its first real caller**: a test drives `Session.run` with a replayed completion and gets a priced ledger and a report ending in euros per confirmed finding. **Only the mock-client half was built** — lab-bench unit tests and golden files are E1-era and E1 has its own suite, so that half is left for Allen to call rather than silently absorbed. `anthropic` is now a project dependency, and recordings are coupled to the SDK's response model — an upgrade that changes it invalidates the store, which is the right trade against a store that keeps parsing after the API has moved. **Sabotage-verified on eleven properties, all caught — after two survived, and both were weak *fixtures* rather than weak assertions**: the refusal fixture had an empty content list so `content[0]` behaved identically, and the usage fixture had a zero cache write so collapsing the two cache figures changed nothing. Both now have discriminating cases. **Seventh time a passing sabotage has meant a weak test, and the first time it was the fixture.**

### S-0.8 — SPIKE: can a model select the right instrument?
Depends: S-0.3, S-0.4, S-0.5
Why: **this is the project's central claim, and nothing in E0 tests it.** `00-BRIEF.md` §1 says the agent exists because *"choosing which one applies to a given program, sequencing them, and interpreting the results"* is the bottleneck. That claim is tested in exactly one place — S-8.7, *the thesis behaviour* — which sits behind E1 through E7.
AC:
- Present recorded measurement results from the E0 spikes as scenarios; the model chooses the next experiment
- Responses are constrained to a schema (conclusion, next instrument, whether a finding is warranted) so scoring is programmatic, not a reading of prose
- Every scenario carries a trap: an answer that is plausible and wrong
- Score separately: instrument selection, trap avoidance, and **refusal to manufacture a finding**
- Run each scenario N≥5 times to measure consistency, not a single lucky answer

Notes: no primitives are needed — the measurements already exist and are recorded, so this tests the *selection* step alone, which is precisely the named bottleneck. The decoy scenario is the sharp one: 37 queries, constant with dataset size, where a detector keying on "many queries" reports an N+1 that is not there. The noise scenario is sharper still — a 12.76ms shift with guard counters unchanged, where the correct answer is *no finding*, and a model that produces one has violated the invariant that null results are valid output.

Result (2026-08-02, **executed 2026-08-04**): scenarios and scoring harness in `spikes/S-0.8-instrument-selection/`; full write-up in its `FINDINGS.md`, raw per-run record committed at `results/selection.json`. **PASS on the pre-registered decision rule** — `claude-opus-5`, 6 scenarios × 10 repeats, 60 requests, ≈$2. **Trap avoidance 100% (30/30), finding discipline 97%.** The model never manufactured a finding: the decoy (37 queries constant across a 20× row change) produced `constant_per_request_overhead` and never an N+1, and the noise scenario (7.4ms shift inside a measured 12.76ms spurious-shift floor, guard counters byte-identical) produced no finding 10/10. The thesis scenario passed on both axes it exists to test — correct diagnosis 10/10 and the instrument switch away from query counting 10/10. **The result inverts E9's premise.** The finding audit was scoped against fabrication; across 60 runs that never occurred once. What occurred instead is persistent *under*-commitment — correct reasoning, withheld verdicts, and always one more experiment proposed. `none_report_no_finding` was chosen **0 times in 60**, including on the scenario where stopping is the only correct answer. So the risk E9 must address is **non-termination**, not invention, and the stopping decision likely cannot be the agent's own — S-5.4's budget halt bounds the damage without deciding sufficiency. **One scenario's criteria did not survive the run**: `post_ablation_residual` scored diagnosis 0%, but its evidence gives 504 customfield queries as a single-point measurement with no row count, from which an N+1 and a fixed per-request count are indistinguishable; the model answered `insufficient_evidence` 10/10 and chose the one instrument that separates them 10/10. Recorded as a criterion defect, and **the scenario was repaired rather than the criterion loosened**: the evidence now carries the scale sweep the model asked for (126/252/504 customfield queries against 25/50/100 rows, flat at ~5.04 per row), while `acceptable_diagnoses` stays at `('n_plus_one',)` — widening it to admit `insufficient_evidence` would have made the scenario unable to fail, and commitment is what it exists to test. Self-check re-confirmed 6/6, and the **re-run scored 100% on all four axes for $0.29**. Nothing about the model changed between the two runs, only the sufficiency of what it was shown. This is the second scorer-calibration defect in this spike's life (cf. `502d774`), which is itself a finding about curated scenario sets: five scenarios agreeing with the model reveal nothing about whether their criteria are right, and this one was caught only because the model disagreed. The harness also gained cost reporting, stdout flushing, and `--scenario`/`--out` — the first execution could not report its own cost, and a 16-minute run emitted nothing until it exited.

---

# EPIC 1 — The lab bench

**Goal:** five deterministic operations that measure faithfully and decide nothing.

**Why:** the agents reason about measurements. If the measurements are wrong, everything downstream is confidently wrong.

### S-1.1 — execute()
Depends: S-0.1
AC:
- `execute(cmd, cwd, timeout, env)` returns stdout, stderr, exit code, wall time
- Timeout kills the process group, not just the parent
- Output is captured without deadlocking on large writes
- Raises a typed error on timeout, distinguishable from a non-zero exit

**DONE (2026-08-02)** — `src/coldfix/bench/execute.py`, 10 tests in `tests/bench/test_execute.py`. All four AC met. A non-zero exit is returned as a *result*, not raised — only the absence of a usable result raises (`ExecutionTimeoutError`). `timeout` is required and keyword-only: a subprocess with no deadline can hang an investigation with no diagnostic. `env` replaces rather than extends, matching subprocess semantics, because a variable leaking in from the parent shell is how two runs of the same measurement come to differ. Process-group kill needed different code per platform — `start_new_session` + `killpg` on POSIX, `taskkill /T` on Windows. **The safety test was verified to discriminate**: sabotaging the kill to reach only the direct child makes the orphan survive and the test fail.

### S-1.2 — time()
Depends: S-1.1
AC:
- `time(fn, repetitions)` returns a list of durations using `perf_counter`
- Discards nothing automatically — warmup handling is the caller's decision
- Records whether the process was fresh or reused for each sample

Notes: do not build in a fixed warmup discard. Barrett et al. showed at most 43.5% of VM/benchmark pairs reach steady state, so "discard the first N" is an assumption that is wrong more often than not.

**DONE (2026-08-03)** — `src/coldfix/bench/timing.py`, 10 tests in `tests/bench/test_timing.py`. All three AC met. The note generalised into **ADR 012**: warmup is not the only decision `timeit` makes on the caller's behalf, and the other three are wrong here in the same direction — no batching (per-sample variance is what S-1.5's rank test consumes), no garbage-collection control (disabling it makes a patch that increases allocation pressure look free), and no `min`-taking (a workload that is pathological one run in five is a finding, not noise). `ProcessState.FRESH` is **scoped to the run** — it means no earlier sample of this run used this process, and claims nothing about the interpreter being newly started, because `time()` cannot observe that. `fresh_process_per_sample` is the caller declaring what it built; the test for it composes with `execute()` and checks the child pids are genuinely distinct rather than trusting the label. **The discard test was verified to discriminate**: sabotaging `time()` to drop the first sample fails it on `samples went missing`, along with three others.

### S-1.3 — count()
Depends: S-1.1
AC:
- `count(hook_name)` context manager returns an integer and a list of captured stacks
- Stack capture is optional and off by default (it is expensive)
- Counting overhead is verified to be under 5% of the counted operation's cost
- One test proves counts match an uninstrumented run's observable behaviour

**DONE (2026-08-03)** — `src/coldfix/bench/counting.py`, 17 tests in `tests/bench/test_counting.py`. All four AC met. **ADR 013**: this story is the mechanism, not the counters — S-3.6 defines those and S-14.1 makes hook points part of the adapter interface, so what ships here is the registry, the install/remove context manager, and stack capture. The decision worth not re-litigating is that `count()` **raises on an unknown hook name rather than returning zero**: zero is a publishable result in this system, so a misspelled instrument would otherwise produce a null finding indistinguishable from a real one. `calls_to()` refuses a `classmethod`/`staticmethod`/`property` rather than wrapping it, because a plain wrapper changes how the attribute binds — a correct count of a different program, which is the ADR 008 failure again. Overhead came in at **0.26µs per event against a 366µs operation (0.07%)**, but only after fixing a `Path.resolve()` — a filesystem call — that ran once per counted event on the capture path: 590µs per event, 132 seconds for the test file, and precisely the defect class this tool exists to find. Both AC-bearing tests were verified to discriminate by sabotage: dropping the `finally` fails only the raising-body removal test, and a wrapper that alters return values fails the uninstrumented-equivalence test.

### S-1.4 — diff()
Depends: S-0.1
AC:
- `diff(a, b)` compares JSON payloads and returns identical/differs plus a structural diff
- Order-insensitive comparison available as an explicit option
- Handles floats with a configurable tolerance
- Handles nested structures and returns the path to the first difference

Notes: order-insensitivity must be opt-in per comparison, decided by whether the original query had an ORDER BY. Defaulting to order-insensitive hides real regressions.

**DONE (2026-08-03)** — `src/coldfix/bench/diffing.py`, 28 tests in `tests/bench/test_diffing.py`. All four AC met. **ADR 014**: this is the one instrument that produces a verdict a patch is gated on rather than a fact someone reasons about, which inverts which failure matters — a false "differs" is visible and wastes a cycle, a false "identical" approves a patch and looks exactly like a correct approval. So everything is strict by default and each loosening is a named argument on a single call. The note's case generalised: `ignore_order` compares **multisets, not sets** (`[1,1,2]` vs `[1,2,2]` differ — the shape of a patch that duplicated one row and dropped another), and the other traps are all places where the obvious Python answer is unsafe — `True == 1` in Python but a boolean is not a number in JSON, `null` is not an absent key, a `str` satisfies `Sequence` but is not an array of characters, `bytes` is refused rather than read as integers, and two NaNs must agree or a payload containing one differs from itself. Paths are structural tuples rendered separately, so a key containing a dot cannot read as a separator. Both AC-bearing tests were verified by sabotage: defaulting `ignore_order` to true, and replacing the multiset with a set, each fail exactly the tests written for them. **Open debt**: unordered comparison *with* a tolerance is greedy and quadratic, because approximate equality is not transitive — conservative, but it wants a real bipartite matching if a workload ever needs it.

### S-1.5 — stats()
Depends: S-0.1
AC:
- Returns mean, median, stdev, coefficient of variation
- Fits a curve against a scale variable and returns slope, exponent, and r²
- Classifies growth as constant / linear / superlinear with a stated threshold
- Provides a rank-based significance test (Mann-Whitney U or Wilcoxon)

Notes: use a rank test, not a t-test. Timing distributions are not normal.

**DONE (2026-08-03)** — `src/coldfix/bench/stats.py`, 24 tests in `tests/bench/test_stats.py`. All four AC met, and **Epic 1's five instruments now exist**. **ADR 015**: three of the four AC are already in `statistics` (`fmean`, `median`, `stdev`, `linear_regression`, `correlation`), so the only gap was the hypothesis test — written out in ~40 lines rather than pulling in scipy for one function. That is "not yet", not "never": the USL fit in S-3.x needs non-linear least squares and that story can take the dependency. Correctness is checked against definitions rather than against itself — U against direct pairwise counting, and the p-value against an **exact permutation test** enumerating all 12,870 relabellings of two groups of eight. That cross-check produced measured limits: the approximation agrees within a few percent in the body, is conservative by ~10× in the far tail, and runs ~30% low on heavily tied data (the unsafe direction — tolerable only because tied metrics are counts, and counts are deterministic and read directly). Below 8 observations per group it **refuses** rather than returning an untrustworthy p-value; S-1.7 requires 20–30 anyway. A flat metric is handled before either fit is attempted, because "queries constant at 7,7,7" is the canonical exclusion this system exists to publish *and* the input a log-log fit degenerates on. Two fits are returned with separate r², since their disagreement is the superlinear signal. **Two of my own claims were wrong and were corrected by measurement**: that a rank test's p-value barely moves under an outlier (it moves an order of magnitude at n=10 — what does not move is the verdict), and that the tie correction stops ties inflating significance (it lowers the variance, so it raises significance).

**AUDIT (2026-08-03)** — the five instruments were run against inputs their tests did not cover, before S-1.6 was started. **ADR 016**. One serious defect: `rank_test([nan]*8, [1.0]*8)` returned **p = 0.0004** — a decisive, well-formed, fictional finding, because every comparison against NaN is false so ranking silently produces nonsense and every line downstream completes. Non-finite values are now refused by all three `stats` entry points. Three smaller ones: a missing binary, a bad `cwd` and an empty command raised three different untyped `OSError` subclasses and are now `ExecutionStartError`; a self-referential payload exhausted the stack and now raises `TooDeepError` at depth 200; and the drain after a timeout kill was unbounded, so a grandchild surviving the kill would hang `execute` forever inside its own timeout handler. One limitation is documented and pinned rather than fixed: `calls_to` cannot see a caller that did `from module import work`, and the undercount is silent — it matters less than it looks because nearly every real counter wraps a method, where the attribute is looked up on the class at every call.

**AUDIT 2 (2026-08-03)** — a second pass, asking what an *unseen repository* supplies that the fixtures do not: input that is well-formed and merely large. **ADR 017**. Three findings. `execute` captured output unbounded — a print loop handed back 80,400,000 characters, and memory exhaustion arrives before the timeout can fire, so the mechanism that bounds a runaway command could not help; each stream is now capped at 8,388,608 characters, head and tail kept, middle elided, count on the result. Reading is now bounded by the caller's deadline too, closing the case where the child exits but a grandchild holds a pipe and EOF never comes. `fit_growth` refused any metric that was zero at a scale — an ordinary count shape — and now returns the linear fit with `exponent`/`power_r_squared`/`growth` as `None`, never guessing growth from the line, because the thresholds are defined on the exponent. `stdin` is now `DEVNULL`, so a command that reads it gets EOF instead of spending its timeout on a prompt. Decoding moved in-process with an incremental decoder, since a 64 KiB read boundary can split a multi-byte character. The memory bound is asserted against peak allocation and **the test was verified to discriminate**: removing the drop fails it at 16 MB against a 100,000-character budget. Also fixed: `TooDeepError` was in `bench.__all__` but never imported, so `from coldfix.bench import *` raised.

### S-1.6 — Interleaved measurement sessions
Depends: S-1.2, S-1.5
Why: comparing against a stored baseline produces false positives.
AC:
- `compare(variant_a, variant_b, n)` alternates between conditions in randomized order within one session
- Never compares against a previously stored measurement
- Returns both distributions plus the rank test result
- A test proves that shuffling the order does not change the verdict on a known-equal pair

Notes: Laaber et al. found naive mean comparison produces high false-positive rates, and that same-instance randomized interleaving is what makes 10% differences reliably detectable.

**DONE (2026-08-04)** — `src/coldfix/bench/interleaving.py`, 15 tests in `tests/bench/test_interleaving.py`. All four AC met. **ADR 018**. The second AC is the one that needed code rather than convention, and the answer is the signature: `compare()` takes two **callables** and runs both itself, so a stored baseline — a list of numbers — has no parameter it fits. Not discouraged, unrepresentable, the same construction as `execute()` making `timeout` required. A runtime `TypeError` backs the annotation for callers that are not type-checked, which is the case that matters: an agent assembling a comparison from an artifact it read. The AC's "alternates ... in randomized order" has two readings and they protect against different things — a single shuffle of `n` A's and `n` B's can deal one condition into the first half by chance, which is the block design interleaving exists to replace, so this **randomizes within each round** and stays balanced across every prefix. Seed and the schedule as it ran are both on the result, because an experiment that cannot be re-run in its original order is not reproducible. `Sample.index` is the position in the *session*, not the variant's own run, since plotting against it is how drift becomes visible. No verdict field — instruments decide nothing, and the caller owns the threshold. `n` is floored at `MINIMUM_GROUP_SIZE` before anything runs, so a session cannot be spent discovering the rank test will refuse it. **The AC-bearing test was verified to discriminate, and its adversarial half is the interesting one**: `test_interleaving_cancels_a_drifting_machine` compares a function against *itself* on a machine whose cost grows every call and requires all twelve seeds to find nothing, while `test_a_block_design_manufactures_a_difference_on_the_same_work` runs the same workload the way this module refuses to and asserts p < 0.001. Sabotaging the schedule to a block design fails the first at **p = 0.0000 for a function against itself** — the Laaber false positive, reproduced. Removing only the shuffle correctly fails the randomization test and leaves the drift test passing, since strict alternation does still cancel monotonic drift. One wart found and fixed while testing: chaining through `time(fn, 1)` put a `TimingError` reading "sample 0 of 1" between `ComparisonError` and the real exception, so the intermediate is unwrapped.

### S-1.7 — Noise floor certification
Depends: S-1.6
Why: an auto-generated harness must prove it can see the effect it is hunting.
AC:
- Runs the baseline 20–30 times before any experiment
- Computes coefficient of variation and minimum detectable effect
- Refuses to proceed if the noise floor exceeds the target effect size, with a clear message
- Certification result is recorded in the experiment log

Notes: this is a novel contribution — no evolve-style framework certifies its evaluator before optimizing against it. Do not skip it as a refinement.

**DONE (2026-08-04)** — `src/coldfix/bench/certification.py`, 16 tests in `tests/bench/test_certification.py`. **Epic 1 is complete.** **ADR 019**. The decision worth not re-litigating: **the minimum detectable effect is simulated, not taken from a formula.** The textbook expression assumes normality, which is the assumption the rank test was chosen to avoid — using it here would reintroduce it one layer down. Instead the baseline is resampled into control and treatment, the treatment scaled by a candidate effect, and the pair put through `rank_test()` — *the same function the real comparison will call* — with the effect tightened by bisection until detection reaches 80% power. The returned value always had its power measured at or above target, never interpolated, so error falls on the conservative side. A refusal raises `NoiseFloorTooHighError` **carrying the certification**, following `ExecutionTimeoutError` carrying partial output: refusing by return value lets a caller ignore it, refusing without the evidence makes the refusal unloggable. **AC 4's dependency is missing and was handled deliberately, not improvised**: the experiment log is S-8.4 (depends on S-6.1 and S-5.7, does not exist), and building a second one now would guess at a schema S-8.4 already specifies — entries carry hypothesis, primitive, design, measurement and verdict, none of which a certification has. So `Certification` is a Pydantic model with stable field order and S-8.4 owns the appending; **confirmed with Allen before proceeding**. Two readings recorded: `n` has a floor of 20 and **no ceiling**, since "20–30" is guidance about sufficiency and refusing 60 samples would be refusing better evidence; and `alpha`/`power`/`trials` are constants recorded on every result rather than parameters. **Sabotage-verified**: making `certify()` never refuse fails two tests, and making the floor optimistically small — the dangerous direction, since it certifies a harness that can see nothing — fails five including the end-to-end check. The estimate is validated from a different seed with twice the trials (so it cannot pass by memorising its own resampling) and then end to end against `compare()`: an effect 4× the certified floor is found in 5 sessions of 5, one at ⅙ of it in at most 1 — both probabilistic, so asserting either on a single run would have been a flaky test dressed as a strict one. **One measurement worth keeping**: a bare busy-wait loop certifies at a floor of ~0.02%, which made the first end-to-end test meaningless because no effect was below the floor — real noise had to be injected to test the mechanism. A spin loop is a far better instrument than any workload this project will meet.

---

# EPIC 2 — Execution environment and safety

**Goal:** everything runs somewhere it cannot cause harm, and dangerous categories are refused before an agent ever sees them.

### S-2.1 — Sandboxed runner — **SAFETY**
Depends: S-1.1
AC:
- Every workload and experiment executes inside a container
- CPU and memory limits enforced
- No external network egress; localhost only
- Filesystem writes confined to the workspace
- Container is destroyed after each diagnostic run

**DONE (2026-08-06)** — `src/coldfix/sandbox/runner.py`, ADR 020, 57 tests in `tests/sandbox/` split into 39 that need no daemon and 18 marked `docker` that attempt each forbidden thing against a live one. All five AC met. **Two AC are not satisfied by the obvious construction, and both were found by writing the test rather than the code.** `--rm` does *not* destroy the container after each run: it fires when the `docker run` client exits cleanly, so the timeout path — the one case that matters — leaves the container running under the daemon, holding the workspace and consuming the CPU every later measurement is taken against. This is S-1.1's orphan problem moved one level up, where its process-group kill cannot see it, because the workload was never a process on this host. Removal is therefore forced by name in a `finally` on every path. Second, an exit code does not say whether a limit was *enforced*: an OOM kill arrives as SIGKILL and is indistinguishable from any other, and `docker run` exits 125 for its own failures, which is also a legal workload exit code. Both are resolved by `docker inspect` before removal — `.State.OOMKilled` separates a memory kill from any other kill, and the container's *absence* separates "docker never started one" from "the workload exited 125". An OOM kill raises rather than returns, on the S-1.1 rule that a truncated run is not a measurement. **The policy is not parameterised**: `Sandbox` has three fields and no argument that enables networking, adds a second bind mount, or lifts the read-only root, and a test asserts the field set itself so that widening it fails a test rather than merging quietly. **Sabotage-verified, four properties**: replacing `--network none` with `bridge` fails both egress tests; dropping the forced destroy fails six tests including three that check the daemon afterwards, and leaks exactly three containers; dropping `--read-only` fails both filesystem tests. The fourth is the honest one — **removing the `--memory-swap` pinning is caught only by the policy test, not by the live memory test**, because this WSL2 VM has no swap to hand out. That flag's necessity rests on documented docker behaviour rather than on anything this machine demonstrates, and a host configured with swap is where it would matter. Left open in ADR 020: `--cpus` is a quota rather than a `--cpuset-cpus` pinning, and the container runs as the image's default user, which on a Linux host leaves root-owned files in the bind-mounted workspace for S-2.2 or S-7.2 to own.

### S-2.2 — Git worktree management
Depends: S-0.1
AC:
- Create, list, and destroy worktrees programmatically
- A worktree can be created at an arbitrary revision
- Destroying a worktree removes all uncommitted changes
- Refuses to operate on a dirty main working tree

**DONE (2026-08-06)** — `src/coldfix/sandbox/worktrees.py`, ADR 021, 27 tests in `tests/sandbox/test_worktrees.py`, all against real repositories and real git. All four AC met. **The fourth AC needed a decision it does not state**: "refuses to operate on a dirty main working tree" does not say which operations count, and the two readings differ in what they protect. The guard applies to `create` — uncommitted edits live in no commit, so a worktree at HEAD does not contain them and every finding would cite code differing from the user's copy — and **deliberately does not apply to `destroy` or `list`**. Applied to removal the argument inverts: a main tree that went dirty mid-investigation would strand a worktree full of ablated source, which is the exact outcome ADR 004 exists to prevent, in the name of safety. Refusing to *list* would block a caller trying to discover what needs cleaning up because something needs cleaning up. The non-obvious direction is asserted by its own test so that tightening it into a safety regression fails. Untracked files count as dirty and ignored files do not, since `git status --porcelain` omits the latter and a repository with build output stays measurable; the two are carried separately on the error because `stash` fixes one and not the other. A worktree *inside* the main tree is refused though git permits it — it would appear there as untracked content, making the tree dirty and making every later `create` refuse, the module disabling itself by having run once. Removal is verified against the filesystem rather than git's exit code, since `remove --force` can report success and leave files something still holds — routine on Windows, and possible everywhere once S-2.3 bind-mounts a worktree into a container. **Sabotage-verified, and the sabotage found a real gap in the tests.** Dropping `--force`, skipping the survival check, and applying the guard to `destroy` each fail their test. Dropping `--detach` failed nothing — because the revision is resolved to a SHA before git is invoked, so git never sees a branch name. The first test set could not have detected this at all: every case used `main`, which git refuses to check out in a second worktree, so its refusal masked the flag. A branch checked out nowhere was added to the fixture, and even then detachment survives removing either mechanism alone — **it has two independent sufficient guarantees, and only removing both attaches the worktree**. Recorded in ADR 021, because it also means neither test is evidence that either mechanism individually works. Left open: nothing prunes a worktree whose run crashed; `Worktree.prunable` reports the state and acting on it belongs to whoever owns run lifecycle.

### S-2.3 — Execution mode separation — **SAFETY**
Depends: S-2.1, S-2.2
Why: ablation deliberately produces broken code; it must be structurally incapable of shipping.
AC:
- Two modes exist: `diagnostic` and `candidate`
- Each mode uses a distinct container and a distinct worktree
- A diff produced in diagnostic mode has no code path by which it becomes a patch — verified by a test that attempts it and fails
- Diagnostic worktrees are destroyed on container exit
- Mode is a required argument on every execution call, with no default

Notes: this is enforcement, not convention. A test must actively attempt the violation and assert it is impossible.

**DONE (2026-08-06)** — `src/coldfix/sandbox/modes.py`, ADR 022, 20 tests in `tests/sandbox/test_modes.py` written as attacks. All five AC met. **The enforcement is an absent method, not a rejected call.** `02-architecture.md` §6 gives the spec as a table whose decisive row is *Output — measurements only*, which is a claim about what diagnostic mode **can do**, not about what it is permitted to do. `DiagnosticSession` therefore exposes exactly `run`, `close`, `mode` and `worktree`; there is no `diff` to call, no argument to pass and no flag to set. A test asserts that public surface **by name as an exact set**, so adding any accessor fails it. **Three independent things must fail before a diagnostic change can ship**: there is no method; there is no repository inside the container, because a linked worktree's `.git` is a file naming a path in the main repo's `.git/worktrees/` and S-2.1 mounts exactly one directory, so git there has nothing to read; and there is no worktree afterwards, since closing destroys it and S-2.2 verifies that against the filesystem. **The second was not designed — it falls out of S-2.1's single mount, was found by checking, and is now asserted so that mounting a second directory later fails a test in this file.** That test asserts the metadata's absence directly rather than running `git diff` in the container, because `python:3.12-slim` ships no git at all and the naive test would pass for the wrong reason and keep passing if the metadata were later mounted. **AC 5 was ambiguous and the user chose the stronger reading**: mode is required with no default at `Workbench.open` and selects which of two *types* is returned, rather than being a per-call argument two calls on one session could disagree about. **Sabotage-verified one route at a time**: adding a `diff` to `DiagnosticSession` fails 2 tests, making `close()` stop destroying fails 3, giving `mode` a default fails 1, pointing both modes at one worktree fails 2 — and is additionally impossible, since S-2.2's `create_worktree` refuses an existing path, unplanned defence in depth now recorded so it is not removed by accident. Left open: container persistence "within the attempt" from the architecture table is **not** implemented — S-2.1 destroys every container after every run, so a candidate attempt re-enters a fresh container per command and reinstalls anything it needs. Reusing one is a caching change to a hot path, which `CLAUDE.md` requires be driven by a measured cost rather than a table row.

### S-2.4 — Protected path enforcement — **SAFETY**
Depends: S-2.2
AC:
- `apply_patch(diff)` rejects any diff touching test files, fixtures, conftest, the harness, or instrumentation
- Rejection happens server-side in the patch applier, never by instructing a model
- Protected paths are configurable per project but have safe defaults
- A test proves a patch modifying a test file is rejected

Notes: environmental hardening of this kind reduced exploit rates by 87.7% relative in the Reward Hacking Benchmark. This single gate does more work than any detector.

**DONE (2026-08-06)** — `src/coldfix/sandbox/patching.py`, ADR 023, 35 tests in `tests/sandbox/test_patching.py` written as attempts, reached through `CandidateSession.apply_patch`. All four AC met. **The obvious implementation is a complete bypass, and it was found before writing code by constructing the attack.** Asking git what a patch touches — `git apply --numstat` — **reports a rename by its destination only**: a diff renaming `tests/test_slow.py` to `src/harmless.py` reads as touching one unprotected path, so the test suite is deleted and the filter reports success. `git apply --summary` knows about the rename but compacts the paths (`rename tests/deep/{test_a.py => test_renamed.py} (100%)`), which cannot be split reliably. **So the diff is parsed here, taking both sides of every rename and copy, and git is consulted only as a cross-check** — if git reports a destination the parser did not find, the patch is refused as unparsable, because the filter must be a superset of git's view or it is not a filter. **The parser tracks hunk line counts** rather than scanning prefixes: inside a hunk every line starts with a space, `+`, `-` or `\`, so a removed line beginning `-- a/x` renders as `--- a/x` and is indistinguishable from a header to anything simpler; this removes both the missed header and the documentation file that quotes a diff being read as touching it. Matching is **case-insensitive** (Windows and macOS resolve `Tests/` and `tests/` to one file, so a case-sensitive rule is bypassable by one letter) and `**` spans segments while `*` does not cross a `/` — written out because `PurePath.full_match` is 3.13+ and because `fnmatch` over a whole path would silently protect `src/latest/thing.py`. **Every ambiguity resolves toward refusing**: a quoted path is rejected rather than decoded, since a filter that decodes git's C-quoting almost correctly would match a protected path as a different string than it names on disk. The audit runs before anything is written and rejection is of the whole patch, because a partial application leaves the source edited and the test untouched, which is indistinguishable from a fix that works. **Sabotage-verified on five properties, and one exposed a bad test**: dropping rename-source parsing fails 2, applying before auditing fails 2, skipping the escape check fails 4, letting `*` cross a `/` fails the `latest/` case — and making matching case-sensitive **failed nothing**, because the test used `Tests/test_speed.py` where `**/test_*.py` matches the filename whatever case the directory is in, so the directory rule was never exercised. Rewritten to use `Tests/helpers.py`, it now fails under the sabotage. **Second time in Epic 2 a test passed for a reason other than the one it claimed** (cf. ADR 021), both found by sabotage rather than review. Left open: symlinks — a patch editing a file that is a symlink to a protected path is not detected here, and the current protection is git's own refusal to follow symlinks when applying (since 2.32, after CVE-2021-21300), which is worth testing rather than inheriting once S-7.x stands up repositories this system did not create.

### S-2.5 — Production guard — **SAFETY**
Depends: S-0.1
AC:
- The system refuses to start unless the database URL matches a configured test pattern
- The check runs before any other initialization
- The error message states exactly what was expected and what was found
- No override flag exists

**DONE (2026-08-06)** — `src/coldfix/sandbox/production.py`, ADR 024, 44 tests in `tests/sandbox/test_production.py`. All four AC met, with one honestly qualified — see the last paragraph. **The check is the constructor**: `VerifiedDatabase(url)` either returns a verified handle or raises, so there is no unverified handle to hold, no `verify()` a caller could forget, and no ordering to get wrong. Downstream code takes a `VerifiedDatabase` rather than a string, so an unverified database cannot be *described*, let alone connected to — the same construction as ADR 022, where the enforcement was an absent method and here is an absent unverified state. **Default-deny on scheme, host and name.** A denylist of production-looking things fails the first time somebody names a database something it did not anticipate, silently, in the direction of destroying data. **The name check is load-bearing and the host list is the weak one** — `localhost` is one SSH tunnel from anything and a production compose file may call its service `db` too; what actually separates them is that people name production databases after the product. **No override exists, including the ones spelled as configuration**: no `force`, no `allow_production`, no environment variable, and a policy that would admit everything (`*`, or an empty pattern list) is refused at construction, because that is an override flag with a different name. The refusal **redacts the password** and `VerifiedDatabase` defines its own `__repr__`, since a frozen dataclass renders every field and this object is designed to be logged — a guard that refused the production database while printing its credential would be its own incident. Named `VerifiedDatabase` rather than `TestDatabase` because pytest tried to collect the latter as a test class. **Sabotage-verified on five properties, and two of the sabotages taught a lesson about sabotage**: dropping the name check fails 6 tests, a host denylist fails 2, a vacuous policy fails 1, removing the custom `__repr__` fails the leak test, un-redacting the error fails its own — but **the last two first reported no failures and both were wrong**, because `@dataclass` does not overwrite a `__repr__` defined in the class body, and a `sed` pattern containing `\n` does not match a literal backslash-n in source. A sabotage reporting no failures must be checked for having applied, or it reads as evidence for a property it never tested. **AC 1 is qualified: there is no runnable entry point yet** — nothing in this repository starts, E6 owns the orchestrator. What exists is the stronger half, that no database handle can exist without the check having passed; an entry point built later gets the refusal for free because it has nothing else to construct. Also open: an SSH tunnel from `localhost:5432` to production is undetectable by any URL-pattern check, and only an honestly-named database would be caught.

### S-2.6 — State reset strategies
Depends: S-0.5, S-2.1
AC:
- Three strategies implemented: transaction rollback, database snapshot restore, container restart
- Strategy is selectable per project and recorded in the workload artifact
- Each strategy is verified by the harness in S-2.7 before use

**DONE (2026-08-06)** — `src/coldfix/sandbox/reset.py`, ADR 025, 17 tests in `tests/sandbox/test_reset.py` against a real Postgres 16 container, marked `postgres`. Adds `psycopg[binary]` as the first runtime dependency after pydantic. All three AC met. **The first strategy as named does not work, and S-0.5 already proved it**: plain rollback failed 10/10 cycles while passing the check that story specified — row counts, content hashes and every `max(id)` identical, and `helpdesk_ticket_id_seq` 509→759, the workload's insert count accumulated and never returned. Postgres sequences are non-transactional by design (`nextval` must not roll back, or concurrent transactions could share an id), which is correct behaviour and precisely why it defeats a naive reset. So the strategy is **`ROLLBACK_AND_RESTORE_SEQUENCES`, and the name is part of the fix** — a test asserts it, so simplifying back to "rollback" has to argue with a failing assertion. Cost is 19.2 ms against 0.4 ms broken, and it is the only strategy needing no exclusive access. A never-used sequence restores to `start_value` with `is_called` false, since `pg_sequences.last_value` is NULL until first use and defaulting it to 1 makes the first real `nextval` return 2 — the same defect one row smaller. `SNAPSHOT_RESTORE` is a template copy at 163 ms that undoes schema changes and work another connection committed; `CONTAINER_RESTART` destroys server and storage and reseeds from SQL text rather than a dump, because `pg_restore` needs a version-matched client binary that S-0.5 found reports errors it then ignores. **The central test is the control**: `test_rollback_alone_leaves_sequences_advanced` reproduces the defect on a live server and asserts the *broken* behaviour, so its partner proves something. **Rollback's precondition cannot be checked here and is left to S-2.7**: it undoes work on its own connection, and a containerised workload commits on its own — a connection cannot see what another committed and attribute it. That makes `SNAPSHOT_RESTORE` the realistic default for container-driven workloads, and is the strongest argument for the "verified before use" criterion. **Sabotage-verified on four properties, and one corrected a false claim in this codebase**: removing the sequence restore fails 3 tests, mishandling a never-used sequence fails 1, skipping the reset after a raise fails 1 — and **dropping `--volumes` failed nothing, because the comment explaining it was wrong**. The Postgres image uses an *anonymous* volume, so a rebuilt container gets fresh storage either way and the reset is correct regardless; what `--volumes` prevents is every cycle stranding a full data directory nothing reclaims, which fills the disk over a few hundred resets. It now has its own test asserting the dangling-volume count does not grow, and that test does fail under the sabotage. Third time in this epic sabotage found what review did not, and the first time the finding was a comment giving a false reason for correct code. Left open, and already handled elsewhere: **no strategy resets process state** — S-0.5's cached `QuerySet` survives every database-side reset — but S-2.1 destroys the container after every run, so the process holding it does not survive to the next experiment. The reset contract is the database half of a guarantee whose other half is the container lifecycle; a change making containers persistent between runs would silently reopen it.

### S-2.7 — Reset verification harness
Depends: S-2.6
AC:
- Runs seed → workload → reset ten times
- Asserts row counts identical across all cycles
- Checks sequence counters and cache state, not just row counts
- Fails the workload with a clear diagnostic if reset is unreliable, falling back to the next strategy

**DONE (2026-08-06)** — `src/coldfix/sandbox/verification.py`, ADR 026, 21 tests in `tests/sandbox/test_verification.py` against a real Postgres. All four AC met. The fingerprint has **four database parts** — row counts, content hashes, max ids, sequence positions — because S-0.5 proved one is not enough: plain rollback returned identical row counts, content hashes *and* `max(id)` across ten cycles while sequences climbed 250. The central test hands the harness that exact defect and requires rejection with the sequence named, asserting alongside it that the other three checks stayed silent — the three S-2.6's criterion asked for are satisfied by the broken strategy, which is the entire point. **AC 3's "cache state" needed a design the obvious one cannot deliver, and this was found by writing the test and watching it pass for a leaking workload.** A workload with a stale cache returns the same value every cycle; so does a correct one, because a correct reset makes every cycle identical — the two are **indistinguishable by output**. So the observation comparison stays (it catches state the fingerprint cannot reach, like a table it cannot hash or a file) but is *not* the cache check. Cache state is checked by requiring **`process_identity` to differ every cycle**: a process that survives can carry a cached row no reset will clear, which is the *condition* for the defect rather than the defect, and is checkable without knowing the framework. That turns ADR 025's claim — S-2.1 destroys the container after every run — from an assumption into something checked, and would notice if containers were ever made persistent as an optimisation. Supplying no `process_identity` skips it, documented as a real hole at every level. The content hash **orders rows by their own text**, without which a restore's changed physical order makes every correct strategy look broken. An unreliable reset is a **report, never an exception** — `SNAPSHOT_RESTORE` exists because rollback cannot undo another connection's commit, so failure is expected traffic; only running out of candidates raises, carrying every report. `VerifiedReset` cannot be built from a failing report, which is the third use of that construction after `VerifiedDatabase` and the session types, and is what makes S-2.6's "verified before use" unskippable. **Sabotage-verified on five properties, each asserting the edit applied** — a precaution added after ADR 024, where two sabotages silently no-opped: row-counts-only fails 3, dropping the observation comparison fails 1, dropping the process check fails 1, `choose_reset` returning its first candidate regardless fails 2, removing the content hash's `ORDER BY` fails 1. Left open: verification is evidence about **the workload it ran**, and a reset that returns the state after one workload may not after another — S-0.5's file-writing workload is reset by no strategy here, and S-7.x's workload artifacts are where that pairing gets recorded.

### S-2.8 — Real-time system detection and refusal — **SAFETY**
Depends: S-0.1
Why: measurement-based analysis is provably insufficient for WCET, and a caching optimization would improve every metric we measure while degrading worst-case timing.
AC:
- Detects RTOS imports, deadline annotations, safety-certification markers, and known real-time framework signatures
- On detection, refuses to proceed and explains why in one paragraph
- Detection runs before grounding, not after
- A test fixture with real-time markers is refused

Notes: this is the only category where running the system could make things worse while reporting success. Ship it before the tool is ever pointed at an unfamiliar repository.

**DONE (2026-08-06)** — `src/coldfix/sandbox/realtime.py`, ADR 027, 48 tests in `tests/sandbox/test_realtime.py`, fixture pair in `tests/fixtures/realtime/`. All four AC met. **Detecting real-time systems is easy; not refusing Django applications is the whole problem**, and that inversion is what the story turned out to be about. This tool's pinned development target is a helpdesk; `deadline` is an ordinary field name in half the task trackers ever written, and `scheduler`, `priority`, `critical`, `real-time` and `safety` are ordinary English appearing in exactly the software this system exists to speed up. A detector keying on those refuses its own target on day one and is worse than no detector, failing in the direction that makes the tool useless while looking diligent. So **every pattern is anchored to a token that does not occur in ordinary application code** — `SCHED_DEADLINE` not `deadline`, `IEC 61508` not `safety`, `\bSIL[- ]?[1-4]\b` not `SIL`, the last because a bare `SIL` matches *silicon*, *silent* and `SILENCE_DEPRECATION`. **The fixture is a pair and the control is the load-bearing half** (ADR 006): `flight_controller` plants markers in all four categories and is refused; `task_tracker` is an ordinary web app packed with every tempting word at once and must be **cleared**; a third test asserts the control still *contains* those words, because the way that claim stops meaning anything is somebody tidying the vocabulary out of the fixture rather than the detector changing. **AC 3 is structural**: grounding will require a `ScreenedRepository` and screening is the only thing that makes one, so there is no unscreened repository object for grounding to accept — fourth use of that construction after `VerifiedDatabase`, the session types and `VerifiedReset`. **An incomplete scan is not a clear one**: a repository too large to finish is refused certification rather than reported clean, because for a check whose failure mode is degrading a safety-critical system while reporting success, "nothing found" and "we stopped looking" must never be the same answer. `screen()` reports and `ScreenedRepository` decides, and the refusal names the marker, category, file and line, since a refusal nobody can audit is one that gets worked around. Generated trees (`node_modules`, `.venv`) are skipped; `vendor` and `third_party` deliberately are not, because a vendored RTOS is exactly what is being looked for and third-party code being unpatchable does not make it undetectable. **Sabotage-verified on five properties, each asserting the edit applied**: loosening the deadline pattern to a bare `\bdeadline\b` fails 4 tests including the control — the most valuable result here, because it is the mistake that would have shipped — matching `SIL` without its number fails 3, treating a truncated scan as clear fails 1, screening without refusing fails 3, scanning binary files fails 1. Left open: a system whose timing requirements live only in a specification document is not detected, since the screening is evidence-based. And **this repository would refuse itself** — `realtime.py` holds every pattern as a literal and the fixtures plant markers on purpose — which is correct behaviour rather than a bug, and is why the self-check test is pointed at `src/coldfix/bench`; recorded so nobody "fixes" it with a self-exemption any repository could use by naming a directory `coldfix`.

### S-2.9 — Scope refusals
Depends: S-2.8
AC:
- Concurrency and locking findings are marked `diagnose-only` and can never enter the repair path
- Causes localized inside third-party dependencies are reported, never patched
- Unsupported project types (frontend, mobile, embedded, mainframe) are detected where possible and reported honestly

**DONE (2026-08-06)** — `src/coldfix/sandbox/scope.py`, ADR 028, 52 tests in `tests/sandbox/test_scope.py`. All three AC met. **The three bullets are not one thing, and reading them as one invites one implementation.** `00-BRIEF.md` §3 already draws the line: a *refused on principle* table of four categories where no verifier makes a change safe, and a separate *not covered* list which is a capability boundary. Concurrency and third-party are the first kind and are enforced **structurally, per finding** — the repair path takes a `RepairableFinding` and constructing one runs the classification, so a diagnose-only finding has no route to repair rather than a rejected one (fifth use of that construction, after `VerifiedDatabase`, the session types, `VerifiedReset` and `ScreenedRepository`). Unsupported project types are the second kind and are **reported, not refused**: `report_scope` returns rather than raising, because a Django application with a React frontend is a perfectly good subject *for its backend* and refusing the repository would decline work this system can do. A test asserts that direction specifically, since collapsing it into a refusal is the obvious tidy-up and would quietly cost the tool most of its real subjects. **`classify()` takes a mechanism string and a site path rather than a finding object**, because the evidence-chain schema belongs to E8 and inventing one here would have the Diagnostician inherit a shape it did not choose; when that schema arrives it supplies these two fields and nothing changes. **Erring toward `DIAGNOSE_ONLY` is deliberate and is the opposite of S-2.8's bias** — a wrongly-refused fix costs one fix, a wrongly-permitted concurrency patch risks a race no check here can detect — so *deadlock-free* is treated as a concurrency mention rather than a control, an algorithm chosen for deadlock-freedom being a concurrency change. **Sites are POSIX regardless of host**: a stack frame comes from a Linux container and `Path("/usr/lib/...").is_absolute()` is `False` on Windows, so relying on it would report every standard-library site as the user's own code in development and as third-party in CI — found by a test failing on Windows, and invisible on the platform the system actually runs on. Third-party trees are excluded from project-type detection and included in S-2.8's marker scan, which is not a contradiction: React in `node_modules` is something the project *uses*, a vendored RTOS in `third_party` is something the project *is*. **Sabotage-verified on five properties, each asserting the edit applied**: a substring search for `lock` fails 5 tests on *clock*, *unblocking* and *block*; classifying without blocking repair fails 3; treating a bare `package.json` as a frontend fails the control (the Django-with-Tailwind case, which is most of the target population); searching inside `node_modules` for project type fails 1; raising instead of reporting for unsupported areas fails 1. Left open: the repair path does not exist yet — E10 owns the Surgeon — so what exists is the guarantee that it will have nothing but `RepairableFinding` to accept, the same qualification as ADR 024's "refuses to start"; and area detection is by manifest and extension, so an undeclared out-of-scope part is not detected, which is why the AC says *where possible*.


**EPIC 2 COMPOSITION CHECK (2026-08-06)** — after S-2.9, the epic was run as a whole for the first time, and **it could not perform its own purpose**. Nine stories of module tests, five constructor-enforced safety properties, four sabotage findings and 487 passing tests, and no test anywhere had a containerised workload reach a database: the reset tests connected to Postgres from the host, the sandbox tests ran workloads that talked to nothing, and the two sets of files had zero overlap. Writing the end-to-end test was **impossible as the code stood** — `Sandbox` hardcoded `--network none`, giving a container loopback and nothing else, so the architecture as built refused to run a Django application. `docker_run_argv` had already named the answer in a comment and filed it as standup's problem; a story that never ran a real workload never noticed it was its own. Fixed in ADR 029: `Sandbox` gains a `network` field typed `InternalNetwork | None`, where `InternalNetwork`'s constructor runs `docker network inspect` and refuses anything not reporting `Internal: true` — so AC 3 is kept rather than traded, since a container on such a network cannot reach `1.1.1.1` and can query a sibling database, measured before the code was written. A second problem appeared only once the first was fixed: `SnapshotRestoreReset` connects from the *host*, which cannot reach an internal network, so the database sits on **two** networks (default bridge with a published port for the harness, internal network under the alias `db` for the workload) while the workload container sits on one. The subject's code has no route off the host; the database, which runs no subject code, is reachable by the thing that must reset it. S-2.1's brittle field-set test **failed when the field was added**, which is what it was written for, and now also asserts the annotation so a future change to `str` fails the same way. `tests/sandbox/test_end_to_end.py` runs ten cycles of sandboxed workload and reset through the S-2.7 harness and asserts every cycle ran in its own container. **The lesson: per-module verification says nothing about composition, and a suite where every file tests one import will not tell you.**

---

# EPIC 3 — Primitives

**Goal:** fourteen experiment types the Diagnostician can compose.

**Why the ordering:** ablation and scaling first because they carry the most weight; the rest as they become needed.

### S-3.1 — Primitive registry
Depends: S-0.1
AC:
- Every primitive declares: name, required capabilities, cost class, applicability predicate
- The Diagnostician receives only primitives applicable to the current project
- Adding a primitive requires no change to agent code
- Registry is introspectable for the prompt's instrument list

Notes: this is what makes primitive 15 an afternoon rather than a refactor. Build it before the second primitive, not after the fifth.

**DONE (2026-08-06)** — `src/coldfix/primitives/registry.py`, ADR 030, 34 tests in `tests/primitives/test_registry.py`. All four AC met. **Three of the four declarations are bookkeeping and the fourth is the story.** An applicability predicate typed `Callable[[ProjectProfile], bool]` cannot express the answer this system will have most of the time — *not known yet* — and both flattenings are wrong in ways already paid for: ignorance as `True` runs longitudinal on a CLI tool, which does not fail but fits a flat line, which reads as *no ramp*, which `00-BRIEF.md` §9 ships as an exclusion (ADR 013's failure with a different instrument, and `08-audit.md` F7 is the same shape for proportional perturbation); ignorance as `False` deletes the instrument from the list and the agent concludes it has exhausted the applicable experiments, which is the line `08-audit.md` closes on. So applicability has **four states** — applicable, unsupported here, undetermined, not applicable — chosen because the reader's next action differs for each, and `ProjectProfile.check()` is the only way to read a fact so that `facts.get(fact, False)` is not reachable. **Withholding is recorded, never silent**, and visibility is not callability: `Selection.get()` raises `PrimitiveUnavailableError` for a withheld name carrying its reason, `UnknownPrimitiveError` for one nobody registered. **A selection is a snapshot, and that is ADR 002's requirement rather than a convenience** — tools render at position 0 and prompt caching is a prefix match, so an instrument learned about mid-investigation is available to the *next* investigation, not this one. Capabilities (what this environment provides) and facts (what the subject is) gate independently and are checked separately, because either absence withholds a primitive and the two call for different actions. Cost class is named for its unit — `seconds`/`minutes`/`tens of minutes`/`hours` — because `01-primitives.md` uses "cheapest" for measurement validity in §2 and "most expensive" for wall clock in §5 and §14, and a registry inheriting that ambiguity would hand it to the agent. Signatures are read from the callable with annotations resolved, since a module with `from __future__ import annotations` renders `workload: 'str'` and one without renders `workload: str` — a cached prompt prefix must not depend on an import in a file nobody would connect to prompt cost. **Sabotage-verified on six properties**, each asserting the edit was detected: collapsing undetermined into applicable fails 6 tests, registration-order rendering fails 3, a dispatchable withheld primitive fails 1, a silently overwritten duplicate fails 1, an uncopied facts mapping fails 1, unresolved annotations fail 2. **A seventh sabotage found a hole in the tests instead** — `all_of()` returning the first failing condition rather than the most decisive one passed everything, because the test listed the decisive condition first; it now runs both orders. Like S-1.3, this module ships the mechanism and no primitives. Left open: a fact nobody establishes withholds its instrument, so E7's grounding stories now have a consumer with a stated appetite, and `ProjectFact` grows one member per gate rather than being complete today.

### S-3.2 — Scaling: volume
Depends: S-1.5, S-2.6
AC:
- Runs a workload at three or more scale points with reset between each
- Fits every recorded metric against the scale variable
- Subtracts the framework baseline measured at N=0
- Forces lazy results to materialize before measurement stops
- Clears caches between scale points

Notes: the three failure modes above (baseline offset, lazy evaluation, warm cache) each silently produce a wrong answer. Test each.

**DONE (2026-08-06)** — `src/coldfix/primitives/scaling.py`, ADR 031, 28 tests in `tests/primitives/test_scaling.py`. All five AC met. **The three failure modes are one failure wearing three hats: each flattens a metric that really grows**, and a flat metric is not an error — it is *queries flat across 100x scale*, which §9 of the brief ships as a finding and a human acts on by looking elsewhere. So each is tested in a pair: one test reproduces the wrong answer from the sweep's own numbers, one shows the mechanism producing the right one, because a test that only shows the correct result passes equally against an implementation where the failure was never possible. **The baseline is subtracted always, and the reason it is easy to skip is that it does not change the slope** — it changes the *exponent*, which is what growth classification rests on: S-0.3's netbox floor of ~35 queries against one query per row reads 36,37,38,39 at volumes 1,2,3, exponent 0.05, `CONSTANT`; the same numbers less the baseline are 1,2,3, exponent 1.0, `LINEAR`. A workload that cannot run at N=0 raises rather than skipping the subtraction. **The warm cache is prevented, not detected**, because ADR 026 already proved it cannot be detected — a stale cache and a correct reset both produce identical cycles — so the sweep requires either a process identity that differs at every point or an explicit clear, and **refuses to start given neither**; ADR 026 left that hole open deliberately for verification, and it is closed here because an unqualifiable measurement is worth less than none. Which guarantee was held is recorded on the result, since exclusions carry their preconditions. The measured window closes only after the result is drained, one level deep, and the item count is recorded because a lazy result yielding nothing and a workload returning nothing are the same query count and different findings. Seeding happens **inside** the reset cycle and the reset is a `VerifiedReset` rather than a callable — sixth use of the constructor-enforced construction. **Sabotage-verified on six properties** (dropped subtraction fails 1, dropped materialization 3, no cache-control requirement 1, no identity check 1, fitting only named counters 2, draining strings 1). **A seventh sabotage passed and exposed the test double, not the code**: moving `seed()` outside the reset cycle changed nothing because `RecordingReset` emptied the subject unconditionally, cleaning up after the leak the test existed to catch. A real rollback restores the state as of `begin()`; the double now snapshots and restores, and the sabotage fails with `[0, 0, 1, 2]`. **A test double more forgiving than the real thing turns a structural assertion into a decoration**, and only sabotage finds it. Left open: `seconds` is one sample per point against S-0.4's ~20 ms floor and is labelled `DURATION` for that reason — interleaved timing is S-1.6 and instruction counting S-3.19; the eight parameters stay flat rather than becoming the workload artifact S-4.1 owns.

### S-3.3 — Scaling: shape
Depends: S-3.2
Why: uniform fixtures hide skew-dependent defects at every volume.
AC:
- Generates fixtures with configurable distribution: uniform, power-law, long-tail
- Can hold volume constant while varying distribution
- Distribution used is recorded in every measurement
- A test proves a skew-dependent defect is invisible under uniform data and visible under power-law

Notes: an N+1 that costs milliseconds at three related rows and minutes at three thousand is invisible if every generated parent has exactly three children.

**DONE (2026-08-06)** — `compare_shapes` and `allocate` in `src/coldfix/primitives/scaling.py`, the skew fixture in `tests/fixtures/planted/skew.py`, ADR 032, 38 tests in `tests/primitives/test_scaling_shape.py`. All four AC met. **The blindness is provable, not anecdotal**: a per-parent cost totals `Σ k²` up to constants, and `Σ k²` is minimized exactly when every parent holds the same count (Cauchy-Schwarz), so the uniform fixture is the *provably weakest* shape for that whole class — and it is what `build_store(authors, books_per_author)`, this project's own fixture generator, produces. **The allocation is generated here so the volume cannot move with the shape**: every distribution returns exactly `groups` counts summing to exactly `total`, by largest-remainder apportionment, because rounding each share independently silently gives 199 rows under one shape and 201 under another and a comparison where both moved attributes nothing. Every parent gets at least one child — a shape that empties parents varies the parent count too. No RNG anywhere, so the same arguments give the same fixture on every machine, which is what S-5.1's replay cache will key on. **The risk this story is most likely to be failed on is shipping three names for one distribution**, and it nearly was: the first long-tail definition gave a head mass of 0.37 against the power law's 0.39. It is now the shape data engineers mean — *most customers have one order, one has fifty thousand* — bimodal rather than smooth, and the deliberate worst case the note describes. A test separates all three on mass concentration *and* spectrum. The baseline costs something different on this axis: a volume sweep loses its exponent, a shape comparison loses its **ratio** — a fixed floor of 2,000 comparisons drags a 9.1x sensitivity down to 3.5x, always toward looking survivable. `sensitivity` returns infinity when the reference shape charged nothing (the literal case of *invisible*, e.g. a chunked fetch below its threshold) and 1.0 for zero against zero, because a metric nothing spends is not a finding. **`scale_volume` gained a required `distribution` argument in this story** — AC 3 says *every* measurement, and a growth curve measured under uniform data is exactly the blind result; declared by the caller, like `time()`'s `fresh_process_per_sample`, since the function cannot see what a seeding callable generated. **Sabotage-verified on three properties** (independent rounding fails 12, long tail defined as a power law fails 3, no one-child floor fails 23). **Two assertions in this story were wrong before they were run, both assuming a shape was more extreme than it was**; the second is what exposed the duplicate distribution, and slightly lower thresholds would have shipped it. Left open: a subject whose seeding cannot take a per-parent allocation gets the volume axis only.

### S-3.4 — Ablation with record-and-replay stubs
Depends: S-2.3, S-0.4
AC:
- Records a real return value for the target during a baseline run
- Replays that value during ablation
- Falls back to a minimal valid value where replay is impossible (streams, stateful objects)
- **Records which strategy was used** in the experiment result
- Runs only in diagnostic mode — enforced, not conventional

Notes: stub choice changes what is measured. An empty-collection stub measures the component *plus* all downstream work that consumed its output; a replayed real value measures the component alone. The interpretation differs, so the strategy must be recorded.

**DONE (2026-08-06)** — `src/coldfix/primitives/ablation.py`, ADR 033, 26 tests in `tests/primitives/test_ablation.py`. All five AC met. **S-0.4 handed this story three findings and each became a mechanism.** The replayed value is the one whose size is **closest to the median** of everything observed, and the distribution it came from is recorded beside it: the obvious implementation — keep the first value seen — is exactly what the spike did on its first run, recording a one-followup value out of a population whose median was six, which silently converts the replay strategy into the empty one. *A replay stub that is not size-representative measures the component plus most of the downstream work, under a label saying it measures the component alone.* The **cardinality gap** is computed rather than assumed small (the spike measured +0.8% and said it would not stay harmless if the component fed something expensive — the ablated run is then charged more downstream work than the baseline ever did and the delta **understates** the component). A **single-use iterator is passed through untouched** and selects the fallback, because capturing a generator means consuming it and the workload that asked for it then receives nothing — a baseline measuring a workload that did no work. Recorded values are **deep-copied once at record time**, charged to the baseline alone, so the delta can only be conservative; the stub returns the same object to every call because copying per call would charge that cost to the ablated condition only, which is the distortion S-0.4 avoided with a module-level flag. Where neither strategy is available the ablation is **refused** — a `None` stub measures how long the workload takes to raise `AttributeError`, which looks exactly like a very fast component. AC 5 is enforced by **requiring a `DiagnosticSession` object rather than a mode flag**: it can only come from `Workbench.open(mode=DIAGNOSTIC)`, has no method returning a diff (ADR 022), and a `CandidateSession` is a sibling type so passing one fails type-checking and is refused at runtime. Shared cycle machinery moved to `primitives/measurement.py` — the second caller, which is the point `CLAUDE.md` permits an abstraction. **Sabotage-verified on four properties** (first-value replay fails 3, no mode check fails 2, consuming the iterator fails 3, aliasing the recorded value fails 3). **The last sabotage initially found only 2, and the missing one was a weak test**: it asserted the recorded *size*, which is computed eagerly and survives aliasing, rather than the recorded *value*, which is what gets replayed — an aliased recording of a list the consumer clears replays as empty, this ADR's subject arriving through a third door. Third story running where a passing sabotage found a defective test rather than defective code. Left open: the mode check cannot stop a caller monkeypatching without going through this module, the same qualification ADRs 024 and 028 carry.

### S-3.5 — Delta debugging search over ablation targets
Depends: S-3.4
Why: sequential guessing is O(n); binary search over subsets is O(log n).
AC:
- Implements `ddmin` with a threshold oracle (cost exceeds X) rather than a boolean crash oracle
- Implements the `dd` variant that isolates the difference between a fast and a slow case
- Localizing among 40 candidates completes in materially fewer than 40 ablations on a synthetic test
- Handles the case where removing a subset breaks the workload entirely

Notes: prefer `dd` over `ddmin` — we usually have both a fast case and a slow case, and isolating the difference is exactly what `dd` does.

**DONE (2026-08-06)** — `src/coldfix/primitives/search.py`, ADR 034, 23 tests in `tests/primitives/test_search.py`. All four AC met. **Measured: 40 candidates localize in 11 ablations** for both algorithms (`dd` asks 15 questions and the cache turns 4 into no run), against 40 for one-at-a-time. **`UNRESOLVED` carries the whole adaptation and does two jobs.** The first is AC 4 — a subset that breaks the workload has its exception recorded against the configuration that caused it and the search carries on, a measured failure to measure. The second is not in the story and matters as much: **a measurement near the threshold is a coin flip**, and S-0.4's ~20 ms noise floor is wide enough to decide a branch and so change which component gets named. The oracle takes a `resolution` and answers `UNRESOLVED` inside that band rather than guessing — the state the algorithm already has, used for the thing it is for; zero for counts, which are exact. **The 1-minimality guarantee is weakened and says so**: delta debugging assumes monotonicity, and a component that populates a cache another reads makes the second cheaper by being present, so what is returned is 1-minimal *as measured*. **Both ends are checked before the search runs**, two measurements, each failure a finding: everything active not expensive means the cost is not here (an exclusion), everything ablated still expensive means no candidate owns it — which is exactly S-0.4's shape, where ablating the dominant component left 504 queries of a second N+1 underneath. Without the checks a search completes and names an arbitrary innocent subset with full confidence. Stubs are recorded **once, before the search**, since re-recording per configuration would vary more than the thing being varied. A target never called is refused rather than skipped. **Sabotage-verified on five properties** (no cache fails 1, no resolution band fails 1, narrowed exception handling fails 3, each precondition fails 1). **The cache sabotage found a defect in the instrumentation, not the code**: `measurements` was derived from distinct configurations seen, so disabling the cache doubled the real ablations while the reported number stayed identical — and that number is what AC 3 is judged on. It now counts calls into the workload. Fourth story running where sabotage found what review had not, second where it was in the measuring rather than the measured.

### S-3.6 — Observation: on-CPU counters
Depends: S-1.3
AC:
- Counters for: database queries, rows returned, bytes returned, HTTP requests, file opens, allocations
- Each counter attaches via a framework-specific hook declared by the adapter
- Stack capture per event, optional
- Counter overhead verified under 5%

**DONE (2026-08-07)** — `src/coldfix/primitives/counters.py`, ADR 035, 26 tests in `tests/primitives/test_counters.py`. All four AC met, one of them only after restating it. **Four of the six counters need a magnitude, so `Record` grew one** — a protocol whose amount defaults to 1, and `Count` carries `events` and `total`. One mechanism rather than two, because `db.query` and `db.rows` are the project's canonical guard pair and both numbers must come from the same attachment: a second wrapper would double the cost on the hottest path and let the two drift apart. `measure_once` now records both for every hook, since `db.rows` read as events is the query count — a plausible number and the wrong one. **Most of the module is a vocabulary and that is the deliverable**: ADR 013 made an unknown hook name raise, which only helps if there is one spelling to be wrong about, so the catalogue is the spelling and a name outside it is refused *at registration* rather than when some primitive asks for the counter nobody registered. **Allocations do not fit the hook shape and are not forced into it** — nothing in Python fires per allocation without a C-level profiler, so it is declared a `BLOCK_METER` with tracemalloc's own tracebacks for attribution; the alternative was inventing events, and an invented event is a fabricated measurement. **AC 4 turned out to be unstatable as written, and that is the finding.** The counter measured **77% overhead** against this suite's stand-in cursor — and the counter was fine: its cost is a fixed 0.49µs per event, and the cursor takes 0.4µs. *Under five percent* is a property of a **pair**, not of an instrument, so the denominator is now stated: 366µs, ADR 013's measured instrumented database call. Against that, counting costs **0.13%**, plus an absolute bound of 5µs/event because the defect ADR 013 records cost 590µs and would pass any ratio stated against a slow enough operation. **Per-event stack capture has no fixed percentage at all**: it walks the whole stack, so cost is linear in depth — measured 12.4µs at depth 0, 25.9 at 10, 86.5 at 50, 295.7 at 200, about 1.4µs a frame, while plain counting stays flat at ~0.4µs. A Django request is tens of frames deep before the view, so at a realistic depth **a stack per event costs as much as the database call it observes** (86µs against 366µs is 24%, five times the budget). **S-3.9 inherits this** — sample events or bound the walk, but a screening sweep must not turn it on. tracemalloc measured **327%** of the run it observes and is declared `HEAVY` for it. **Sabotage-verified on four properties** (bypassing the catalogue check fails 1, discarding the recorded amount fails 2, resolving the measuring hook's target at construction rather than install fails 1, leaving tracemalloc running fails 2). The resolve-at-install rule is written against a failure this codebase produces: S-3.4 replaces attributes with ablation stubs, so a hook capturing its target at construction would measure a callable nobody is calling.

### S-3.7 — Observation: off-CPU
Depends: S-3.6
Why: without it the entire saturation column of the USE Method is unmeasurable.
AC:
- Measures time blocked on: disk I/O, network, lock acquisition, scheduler queueing
- Distinguishes "computed a lot" from "waited a lot" in the experiment result
- Works inside the container sandbox
- A test with a deliberate sleep shows blocked time, not CPU time

Notes: an ablation without this tells you a component is expensive but never whether it computed or waited — and those have nothing in common as fixes.

**DONE (2026-08-07)** — `src/coldfix/primitives/off_cpu.py`, ADR 036, 20 tests in `tests/primitives/test_off_cpu.py`. All four AC met. **The total is one subtraction and it is exact**: `perf_counter` is elapsed, `process_time` is CPU charged to the process, and the difference is time the process existed and was not running — no sampling, no tracer, two clock reads. That cheapness is why it is recorded on **every** measurement (AC 2) rather than only when off-CPU time is already the hypothesis. **Attribution by category is a different problem and the honest answer is partial**: the real blocking calls are not reachable from Python — `io.BufferedReader.read`, `socket.socket.recv` and `_thread.LockType.acquire` are C types whose attributes cannot be replaced — so attribution comes from what an adapter *declares* (`blocking()` wraps a known waiting point and records elapsed seconds, reusing S-3.6's magnitude record so blocked time is read by every primitive that reads any counter) and from what the OS already counted (`getrusage` voluntary/involuntary switches, block I/O). **Scheduler queueing has no hook and never will** — being preempted is not a call anything can wrap — so `blocking()` *raises* for that category rather than accepting a wrapper an adapter would then believe had instrumented queueing; it comes from involuntary context switches. **Unavailable is `None`, never `0`** (ADR 013's rule in its original form): zero involuntary switches is a publishable finding — *nothing was preempted, so the cost is not queueing* — so a platform that cannot measure must not be able to produce it, and `resource` does not exist on Windows. **Negative blocked time is reported, not clamped**: CPU above wall clock means the work ran on more than one core, which is `Boundedness.PARALLEL` and needs S-3.12, not a subtraction — clamping would print *never waited* from a case where the decomposition does not apply. **AC 3 was run rather than argued**: the host is Windows, where `resource` does not exist at all, so the signals this story leans on are exactly the ones unverifiable locally; a docker test writes a probe into the sandbox and asserts real blocked time, CPU time and switch counts from inside it. **Sabotage-verified on four properties** (unavailable-as-zero fails 1, clamping fails 1, recording outside a `finally` fails 1 — a call that times out has waited exactly as long as the timeout and that is the finding — dropping the metrics from `measure_once` fails 1). **One test was flaky rather than sabotage-sensitive and was replaced**: four Python threads busy-looping cannot reliably produce CPU above wall clock because the GIL keeps them on one core, and the skip guard did not match the classification threshold, so between the two it tested the scheduler rather than the classification. The parallel case is now constructed directly and the threaded test uses `sleep`, which does release the GIL.

### S-3.8 — Guard counters and global resource envelope
Depends: S-3.6
Why: guard pairs are a denylist and fail by omission.
AC:
- Every primary counter has a declared guard counter
- **Additionally**, every candidate is measured against a global envelope: peak RSS, total CPU, wall time, bytes written, file descriptors, process count
- Any envelope metric outside tolerance triggers a flag regardless of whether that trade was predicted
- A test proves a patch trading queries for a memory explosion is flagged

**DONE (2026-08-07)** — `src/coldfix/primitives/envelope.py`, guard resolution in `counters.py`, ADR 037, 23 tests in `tests/primitives/test_envelope.py`. All four AC met. **AC 1 exposed two guards S-3.6 had left as prose** (`"http.request, by response size"`) **and three as `None`** — guards that guard nothing while looking like guards. `guard` is now a required reference that must name another catalogue counter or an envelope metric, checked **at import**, so a dangling guard fails the import rather than the investigation; six catalogue entries were added to give the references somewhere to point (`http.bytes`, `memory.bytes`, three `blocked.*.calls`), each the other reading of a hook that already existed. A guard may point at an **envelope metric** — `file.open` has no counter to be traded against, and inventing one to satisfy a rule would be worse than admitting it. **The envelope checks every metric always and has no argument for expectations**: `compare` takes two samples and a tolerance table, and there is no parameter through which a caller could name the trade to expect, which is the entire difference from a guard pair. Increases flag, decreases never do. **The finding of the story is that a rise must clear a ratio *and* an absolute floor**, and it arrived as the check failing its own control — two identical runs, 2.4ms and 2.7ms, an 11% rise past a 10% tolerance, nothing having happened. The timing floor is not a guess: it is **S-0.4's measured ~20ms noise floor**. Counts get floors at the other end (two file descriptors becoming three is a 50% rise and is nothing). **Retained memory is a difference, peak RSS is a level**: `sys.getallocatedblocks()` is interpreter-wide, so under pytest a run retaining 24,000 blocks against a 200,000-block interpreter reads as a 12% rise and *passes* — differenced, it is what that block retained, which is what a cache is. Unavailable metrics are **named, not passed**; the report says outright that it covers less than a sandbox run would. **Sabotage-verified on five properties** (unmeasured-as-checked fails 2, checking a chosen few fails 10, no absolute floor fails 2, flagging decreases fails 1, disabled import check fails 1). Two of this story's own tests failed before the code did and both were right to: the first candidate "patch" did not actually reduce the query count, so AC 4 was asserting an improvement that was not there.

### S-3.9 — Stack localization
Depends: S-3.6
AC:
- Normalizes stacks by stripping framework-internal frames from an adapter-supplied deny list
- Groups events by normalized signature
- Walks to the divergence point — the deepest frame common to all occurrences
- Emits the causal site plus the dependency closure (models, relationship declarations, consumers, callers)
- Handles async boundaries where context is lost, or reports that it cannot

Notes: this is how findings span multiple files without the agent reading the repository. The runtime names the files.

**DONE (2026-08-07)** — `src/coldfix/primitives/localization.py`, ADR 038, 23 tests in `tests/primitives/test_localization.py`. All five AC met. **Stripping happens everywhere in the stack, not at the ends** — a real stack is framework, subject, framework, server, and stripping only the innermost run leaves forty frames of framework in every signature and groups nothing. **The divergence point is the longest common suffix and its first element is the site**, which is one computation for both shapes: an N+1's stacks are identical so the site is the innermost frame (the line in the loop), and events from two sites share only their caller so the site is the function calling both. The tempting alternative — the innermost frame of the largest group — gets the N+1 right and the second case wrong, which is why the second case has a test. **A sample localizes as well as a census**, because grouping is by distinct route and the walk is over groups: this is the mitigation S-3.6 handed over (stack capture costs ~1.4µs per frame of depth, 86µs an event at realistic framework depth against a 366µs query), and it is safe *by construction* rather than by a caller remembering. **Two things are reported rather than guessed**: occurrences whose every frame is framework have no site in the subject's code and are counted apart (grouping them under an empty signature would invent a shared site — and the fact is a finding, since S-2.9 already routes dependency costs to diagnose-only); and a stack captured inside a coroutine shows the loop rather than whatever awaited, so the group flags that the trail goes cold instead of naming `base_events.py` as the culprit. The **closure is honest about halves**: callers are exact from the runtime, the source excerpt is read by the *harness* (the agent is what must not read the repository), and models/relationships come from an adapter resolver — with none, it says *not resolved*, which is not *there are none*. A closure over nothing is refused, because an empty closure reads as *a site with no dependencies*. **Sabotage-verified on five properties** (innermost-only stripping fails 2, empty-signature grouping fails 2, busiest-group site fails 3, no async detection fails 3, empty closure fails 1). **Two defects were found by the tests before any sabotage**: `Frame` was hashable but not orderable, so the group sort crashed whenever two groups had equal counts — exactly the unrelated-occurrences case — and a Windows-path test used doubled backslashes inside a raw string, asserting about a path it did not mean. Also fixed here: S-3.7's raising-path test was flaky because `process_time` ticks at ~15.6ms on Windows, so `wall - cpu` understates blocked time by up to a tick; the assertions now allow one tick with the number named.

### S-3.10 — Substitution
Depends: S-1.6
AC:
- Swaps an implementation or configuration value and re-measures
- Configuration substitution supports sweeping a range of values
- Every substitution is reversible and reverted after measurement
- Query-plan comparison supported for index hypotheses

**DONE (2026-08-07)** — `src/coldfix/primitives/substitution.py`, ADR 039, 25 tests in `tests/primitives/test_substitution.py`. All four AC met. **A sweep returns a candidate, not a conclusion**: eight values measured once each cannot separate differences below S-0.4's ~20ms floor, so `confirm` is what turns the candidate into a claim by putting it against the incumbent through S-1.6's interleaved comparison — the same *search first, then validate the single winner* pattern `01-primitives.md` §12 states for instruction counting. The sweep's own explanation says it is a search result and repeats §9's warning that a value tuned on one workload is a claim about that workload only. A sweep that recovers the incumbent proposes nothing and `confirm` refuses it, which is a real result. **Reverting is verified, not performed** — restore in a `finally`, then read the value back and check it. A restore that appears to work and does not is not hypothetical: an object with its own `__setattr__`, a cached property or a validating settings class accepts the call and keeps its own value, raising nothing and silently changing every later measurement. An unreadable original is refused **before anything changes**, and settability is checked by attempting the real assignment rather than probing with a test write, since a probe is a mutation taken before the caller asked for one. **Query plans are the planner's opinion and are labelled as such**: `EXPLAIN` without `ANALYZE` measures nothing, so a comparison reports the *shape change* (sequential scan became index scan) as a fact and the costs as an estimate, saying outright that the workload still has to be timed or counted; `analyze=True` exists and is not the default because an `EXPLAIN ANALYZE` of an `INSERT` inserts. An index the planner ignores is a reported outcome, and nested plan nodes are all read (an index scan under a sort is still an index scan). **Sabotage-verified on five properties** (no restoration check fails 1, restore outside the `finally` fails 1, outermost-node-only fails 1, sweep not restoring between readings fails 2, no-op confirmation allowed fails 1). **The first sabotage silently did not apply and reported five clean passes** — the formatter had reflowed the target text, so the replacement matched nothing and the suite ran against unmodified code. That is ADR 024's failure, and every run above now asserts the edit landed. Left open: pool sizes and cache TTL are in §9's target list *and* in `00-BRIEF.md` §4's slack-reducing list, so a sweep can recommend exactly what the metastability gate exists to catch — S-10.6 owns that classification, and the sweep carries the attribute and value it changed so it can.

### S-3.11 — Temporal
Depends: S-2.2, S-1.6
AC:
- Checks out an earlier revision in a worktree and runs the same workload
- Bisects across a commit range to find a regression point
- Handles revisions that fail to build by reporting and skipping
- Verifies the workload exists at both endpoints before starting

**DONE (2026-08-07)** — `src/coldfix/primitives/temporal.py`, ADR 040, 17 tests in `tests/primitives/test_temporal.py` (real git, real worktrees, marked `slow` at ~80s). All four AC met. **`Oracle` was generalized rather than duplicated**: S-3.5 already built a threshold oracle with a noise band, an outcome cache and an append-only probe log, and a bisect needs exactly that keyed on a revision — two oracles with separately-invented noise semantics would produce findings that disagree for reasons nobody could see. **A revision that cannot be measured is skipped, never counted as cheap**: counting it cheap moves the boundary past it and yields a confident wrong commit, counting it expensive does the same in the other direction. Candidates are tried nearest the midpoint first, since the closer a usable revision is to the middle the less a skip costs. A range where everything between the ends is unmeasurable **reports the pair** — a smaller answer than a commit and a far better one than the wrong commit — naming every skipped revision. The noise band is a skip too: a revision inside the resolution decides a step on noise and every later step inherits it. **Both endpoints are measured first**, with three distinct messages: the older end unmeasurable is usually *the workload did not exist yet*, and a bisect over that returns the commit that **added the workload** — true, and not the regression (AC 4); the older end already expensive means the regression predates the range (an exclusion); the newer end not expensive means there is no regression here (also a result). Worktrees are destroyed in a `finally`, because the diff from an old revision to the current one is a revert of everything since. **Sabotage-verified on four properties** (unmeasurable-as-cheap fails 3, no endpoint check fails 3, destroy outside `finally` fails 1, bisect narrowing the wrong way fails 3). Two test defects were found before any sabotage: the history fixture wrote identical costs in consecutive commits and git refuses a commit that changes nothing, so every test errored in setup with an empty stderr; and the noise-band test poisoned its own endpoint. **A scripted `str.replace` silently matched nothing for the second story running** — this time applying one edit of two and leaving the other, which read as a fixture change that had not happened. Edits to existing files now go through a tool that fails when its target is absent.

### S-3.12 — Load with USL fitting
Depends: S-1.5
AC:
- Drives concurrent load at increasing levels at fixed data size
- Fits throughput against concurrency, returning contention (α), coherency (β), and Nmax
- Cross-checks measurements against Little's Law for self-consistency
- Findings are marked `diagnose-only`

Notes: the fitted coefficients are diagnostic, not just descriptive — high α points at a shared resource, high β at coordination cost. Surface them to the agent, not just the curve.

**DONE (2026-08-07)** — `src/coldfix/primitives/load.py`, ADR 041, 25 tests in `tests/primitives/test_load.py`. All four AC met. **The fit is ordinary least squares because the model linearizes**: `(γN/X(N) - 1)/(N-1) = α + βN`, intercept is contention and slope is coherency, so ADR 015's stdlib-only rule holds. **Two guards were added because the tests demanded them, and both are the same lesson — a fit always returns numbers.** (1) *A sign test on a fitted coefficient needs a tolerance*: a curve generated with β=0 fits β=-8.6e-08 and one with α=0 fits α=-1.2e-06, neither of which is negative contention, and a strict `>= 0` declares an ordinary Amdahl-shaped system unfittable. The floor is **1e-3, chosen against what a load measurement can resolve** — α=0.001 is one part in a thousand serialized and no timing-based test separates that from zero. Same rule S-3.8 needed for its envelope, at the other end of the scale. (2) *A peak beyond the measured range is an extrapolation*: β is never exactly zero and `sqrt((1-α)/β)` turns a tiny β into an enormous peak — measured, a β=0 curve rounded to whole completions gave β=6.5e-5 and a confident peak at N=118 from data stopping at 16. `Nmax` is withheld past twice the largest concurrency driven. A materially negative coefficient is **reported as measured** with the fit marked unfitted; replacing it with zero hides the finding behind a curve that looks fitted. **Little's Law is a validity check, not a result** — `N = X × R` costs one multiplication and catches the failure nothing else sees: a generator that never sustained its concurrency still produces a smooth, plausible, meaningless fit, so the finding says to fix that *before* reading the coefficients. **Diagnose-only is enforced twice** and the second is the real one: the mechanism sentence is written so S-2.9's `RepairableFinding` refuses it in its constructor, independently of what this module remembers about itself. The GIL is stated rather than hidden — for CPU-bound Python the pool is not really concurrent, and Little's Law is exactly what notices. **Sabotage-verified on four properties** (negative coefficients accepted fails 2, no extrapolation limit fails 1, Little's Law always agreeing fails 2, finding marked repairable fails 2). **The extrapolation sabotage initially passed**, showing the branch was unreachable from any test — every curve had either a real peak in range or a β below the floor. A test for the case the rule exists for was added, with its control. A guard no test reaches is a guard nobody has checked.

### S-3.13 — Isolation
Depends: S-3.12
AC:
- Runs a component standalone and in full context, reporting the gap
- Findings marked `diagnose-only`

**DONE (2026-08-07)** — `src/coldfix/primitives/isolation.py`, ADR 042, 21 tests in `tests/primitives/test_isolation.py`. Both AC met. **A gap smaller than the spread of the isolated runs is not interference** — two runs of the same thing differ, so a primitive reporting any positive gap reports interference for everything it is pointed at, and what makes that worse than an ordinary false positive is that **the finding names a real neighbour**: a specific wrong answer sends someone to change a queue their component never touched. The isolated condition is run repeatedly, its *range* is the floor (a standard deviation assumes a shape timing distributions do not have, per S-1.5), and a gap inside it is reported as *no interference detectable* — an exclusion, not a failed search. **Attribution is a separate step and is the one §17's `Load → Isolation → Substitution` needs**: a gap against the whole context says a component is interfered with, a gap against each neighbour alone says by what, and where the whole context interferes but no neighbour does, that is reported as the combination rather than one of them being named. The magnitude is a **search result** — same discipline as S-3.10's sweep, confirm with S-1.6 before quoting. Diagnose-only is enforced twice, the second being S-2.9 refusing the mechanism in its own constructor. The context stops in a `finally`. **Sabotage-verified on four properties** (any-positive-gap fails 3, repairable fails 2, a mechanism S-2.9 misses fails 2, context stopped outside `finally` fails 1 — **but only after a test was added**, since nothing raised inside a live context and the branch was unreachable). **The contended test was flaky and the cause is a platform fact**: at a 20ms lock hold it measured 0.0203 alone against 0.0205 contended for a workload holding the lock its whole life, because Windows' timer granularity is ~15.6ms — the same number S-3.9 hit from the other direction. The hold is now 50ms and the file passed four consecutive runs.

### S-3.14 — Proportional perturbation
Depends: S-3.4
AC:
- Injects a known fractional slowdown into a target and measures the effect
- **Applicability predicate returns false for single-threaded synchronous code**
- Returns a sensitivity curve, not a single point

Notes: Coz's virtual speedup works by pausing concurrently running threads. In single-threaded code there is nothing to pause and the primitive degenerates into ablation. Gate it.

**DONE (2026-08-07)** — `src/coldfix/primitives/perturbation.py`, ADR 043, 19 tests in `tests/primitives/test_perturbation.py`. All three AC met. **The gate is not a caveat on this primitive, it is the reason it exists separately from ablation**: in serial code the sensitivity *is* the share of runtime, so the curve reproduces ablation's answer more slowly; in concurrent code the slope is smaller than the share because other threads absorb part of the delay, and that gap is the whole of what Coz's method adds. The primitive declares `RUNS_CONCURRENT_CODE` and S-3.1's registry withholds it where the fact is false **and where nobody has established it** — ADR 030's three-answer applicability doing the job it was built for, on the first primitive that needed it, and tested through `REGISTRY.select` rather than by asserting a docstring. Writing the insensitive-component test made the same point from the other side: **the case cannot be constructed without concurrency**, since the target has to run alongside something longer for its delay to be absorbed rather than added. The slowdown is proportional to what the call took (a constant delay's slope is a fact about the constant); the speedup is an **extrapolation** with r² beside it, the same rule S-3.12 applies to a peak. **The workload must reach the target through the attribute** — a reference captured before the substitution calls the original, the curve comes back flat, and a flat curve reads as *optimizing this would gain nothing*: the wrong answer in the direction of doing nothing. Found by a test passing `pipeline.bulk` directly and measuring a slope of -0.0002 for a component that was the entire workload. Injection reuses S-3.10's verified substitution rather than patching attributes a sixth time. **Sabotage-verified on three properties** (wrong project fact fails 2, constant delay fails 2, any-positive-slope fails 1). **Also fixed here: S-3.13's contention tests flaked in the full suite**, and the cause is a property of locks — measured, the contended samples were `[0.856, 0.050, 0.050, 0.050, 0.050]`, because Python's locks are not fair and a foreground thread that releases and immediately re-acquires barges ahead of the queued neighbours, so five back-to-back measurements contend once. The fixture now does work either side of the critical section (which is also what a real component looks like) and every sample contends; and `context_cost` now states that a median understates contention living in the tail, pointing at S-1.5's rank test for that question.

### S-3.15 — Longitudinal
Depends: S-3.2
AC:
- Runs a workload repeatedly at fixed size over an extended period
- Fits metrics against elapsed time rather than input size
- Applicability predicate requires a long-running deployment model
- Configurable duration with a hard cap

Notes: the most expensive primitive. Never run it on a CLI tool.

**DONE (2026-08-08)** — `src/coldfix/primitives/longitudinal.py`, ADR 044, 22 tests in `tests/primitives/test_longitudinal.py`. All four AC met. **This primitive inverts the invariant every other one enforces**: ADR 026 and S-3.2 require a fresh process or explicit clear between measurements, and here the carried state *is* the subject — a soak that reset would find nothing, always, and report it as no ramp. **A ramp is read from the straight line, not the growth class**, which was a real defect rather than a preference: `fit_growth`'s power-law half is right against input size and wrong against elapsed time, because the first sample sits at t≈0 where a logarithm is undefined — so `growth` came back `None` and `ramping` could never be true. Real runs passed only because `perf_counter` differences are never exactly zero, which is *worse*: the exponent would have been computed across fourteen orders of magnitude of x. Three conditions on the slope, each removing a way of being confidently wrong: positive, r² ≥ 0.5 (a noisy flat series produces a slope by accident), and a modelled climb of at least a tenth of the starting level (or a leak is reported from a millisecond an hour). Nothing is discarded including the first sample (S-1.2's Barrett et al. rule). **A rising line is not a finding without a control** — four hours of soaking is four hours in which the machine also changed, and another process arriving produces the same line as a leak — so a reference is measured in the same loop, *alongside* rather than afterwards, and a shared rise is not reported; without one the result says its trends are about the subject and the machine together. **Exceeding the cap is refused, not clamped, and the sabotage made the case better than the argument**: with a clamp in place the refusal test **started an actual six-hour run** and had to be killed. A silent clamp turns a rejected argument into a commitment of the cap's whole duration, on the most expensive primitive in the set. **Sabotage-verified on three properties** (clamping fails 1, no control subtraction fails 1, any-positive-slope fails 2).

### S-3.16 — Fault injection
Depends: S-2.1
AC:
- Degrades a declared dependency: added latency, error responses, dropped connections
- Measures the system's behaviour under degradation
- Blast radius limited to one dependency at a time
- **Detects retry amplification** — outbound call count rising superlinearly under injected latency

Notes: the retry-amplification check is what partially rescues the metastability gate. It does not prove safety, but retries are the most commonly cited metastable trigger.

**DONE (2026-08-08)** — `src/coldfix/primitives/faults.py`, ADR 045, 23 tests in `tests/primitives/test_faults.py`. All four AC met, with **AC 4 met differently from how it is written, and the measurement is the argument**: "rising superlinearly" is the wrong test, because every retrying client has a limit, so its curve is a step. A four-attempt client measured 1, 4, 4, 4 against 0/80/120/160ms of injected latency — `fit_growth` returns no class at all, so a superlinearity test would report **nothing** for the textbook amplifying case while the well-behaved control returned a clean `constant`. Amplification is therefore a **multiple of the undegraded call count** (≥ 2.0 — one extra call on a slow dependency is a retry doing its job, and a check that fires on every subject with a retry policy gets switched off, after which the amplifying ones go through too). `growth` is still fitted and reported; when it is `None` the report says what that means rather than printing `None`. The undegraded level is measured in the same run with the interposition in place, and levels are **sorted before measuring** so `responses[0]` is the baseline however the caller listed them — an unsorted list divides every count by a degraded denominator, which is the direction that hides amplification (a sabotage the first test suite did not catch, because it only ever passed ascending latencies). **Three faults, not two**: Netflix's slow-and-erroring pair, plus a connection dropped *after* the request was sent, kept separate because the work on the other side already happened — a client safe to retry against a refusal is not automatically safe against a drop, and the tests pin the difference by asserting the dependency was reached 0 times under `ERROR` and 1 time under `DROPPED_CONNECTION`. **Blast radius is a process-wide lock, not advice**: a second `degrade` while one is open raises, because two simultaneous injections produce a measurement attributable to neither; released in `finally` *and* on the pre-wrap refusal path, both sabotage-verified, since a leak there would make every later injection in the process raise. A workload that fails under fault is **the measurement, not an error** — failing when a dependency fails may be correct, and which it is belongs to whoever reads the report. Interposition is S-3.10's verified substitution, which also decided the test fixture: a `classmethod` is refused (replacing a descriptor changes how the attribute binds), so the dependency is a client object holding a callable attribute, reached through the attribute at call time per S-3.14. **Every path out of the non-amplifying case carries "that is not proof of safety" and a test asserts it is there** — a check that quietly reads as a clearance is worse than no check, which is what `08-audit.md` F1 downgraded the gate to avoid. **Sabotage-verified on nine properties**, all caught.

### S-3.17 — Input space search
Depends: S-3.6
AC:
- Wraps an existing fuzzer (AFL-based) rather than implementing mutation from scratch
- Fitness function rewards resource consumption, not coverage
- Applicability predicate requires user-controlled input parsing
- Findings involving denial-of-service potential are flagged for different disclosure handling
- Hard time cap

Notes: do not write a fuzzer. The agent's contribution is choosing what to fuzz and interpreting results.

**DONE (2026-08-08)** — `src/coldfix/primitives/input_search.py`, ADR 046 and ADR 047, 26 tests in `tests/primitives/test_input_search.py`. Four AC met; **AC 1's "(AFL-based)" parenthetical is deliberately not met, and the measurement is the argument** (ADR 046, agreed with the user before implementation). AFL is coverage-guided — which is precisely why SlowFuzz and PerfFuzz are AFL *forks* rather than AFL configurations — so a literal reading collides with AC 2. And the AFL-lineage Python engines do not run here: `atheris` has no wheel for this platform and dies building from sdist (`error: [WinError 193] %1 is not a valid Win32 application`), `python-afl` needs the Unix-only `afl-fuzz`. The engine is **Hypothesis's `target()`** — targeted property-based testing, Löscher & Sagonas ISSTA 2017 — an existing search engine whose fitness function the caller supplies, so AC 2 is met as *guidance* rather than as ranking a coverage-built corpus afterwards. No mutation code is written, and `search` takes a `SearchStrategy` with no parameter that accepts inputs, so a caller structurally cannot turn this module into the generator. **Hypothesis hill-climbs numeric draws only**, which is load-bearing rather than trivia: `optimiser.py` skips any node that is not `integer`, `float`, `bytes` or `boolean`, so a campaign over `st.text()` is *silently* an unguided random sample — six seeds returned byte-identical worst cases guided and unguided, against 39 → 113 for `st.lists(st.integers())`. A test asserts the text equality, so a Hypothesis release that fixes it fails the test rather than quietly changing what the primitive claims. **The control is the test that matters**: any fuzzer finds a bad input eventually, so the guided campaign has to beat the same budget spent at random — insertion sort, deterministic comparison counter, 284 vs 146, reproducible under `derandomize`. **Guidance comes from the observation, not the phase**: sabotaging `Phase.target` out changed nothing (the generation phase also refuses mutations that lower an observed target), while removing the `target()` call did — so the phase is now unconditional and `guided=False` means only that nothing is observed. **AC 4 is performed, not described** (ADR 047): the denominator for denial-of-service potential is the median cost of the inputs *the same size* as the champion, excluding the champion — §14's whole distinction is which input rather than how much, and leaving the champion in its own denominator understates every asymmetry in the direction of not reporting a vulnerability. At ≥ 10× the state is `RESTRICTED` and `report()` is **built without the payload**, which at that ratio is a working exploit; `witness()` is the one named way to get it and `Candidate.payload` carries `repr=False`. Three states, failing closed: too few same-size peers gives `UNDETERMINED`, which withholds too. Insertion sort scores 1.06× and is correctly *not* flagged. **The cap is refused rather than clamped** (ADR 044's rule), and is checked between examples — a single hour-long input overruns by an hour, and no version of this primitive can do better, since the mechanism that would cut one short is the one that would discard the answer. Hypothesis's own 200ms per-example deadline is off for the same reason. **A campaign produces a candidate, not a finding**: one sample per input sits under S-0.4's ~20ms floor and the search selects the maximum of a noisy population, so `confirm` hands the champion and an equally large input to S-1.6's interleaved comparison. **Sabotage-verified on fourteen properties**, all caught — two only after fixing the tests: a fixture built with `range(MINIMUM_COMPARABLE - 1)` moved with the constant it was testing, so lowering the minimum to 1 built zero peers and the test still passed.

### S-3.18 — Bound comparison
Depends: S-3.6
AC:
- Implements only computable bounds: bytes that must be read, rows required by the response schema, instruction lower bounds
- Returns "not computable" for semantic bounds rather than guessing
- Used during screening as an optional headroom check

Notes: "how many queries must this endpoint issue" is a question about intent and is circular. Restrict to the computable cases and say so.

**DONE (2026-08-08)** — `src/coldfix/primitives/bounds.py`, ADR 048, 31 tests in `tests/primitives/test_bounds.py`. All three AC met. **The refusal is in a constructor, not in the helpers** — restricting `bytes_that_must_be_read`, `rows_required_by` and `instructions_of_reference` to the computable cases still leaves the circular answer one line of arithmetic away, so `Bound.__post_init__` raises `NotComputableError` for any metric whose minimum is semantic (`db.query`, `http.request`, `file.open`, the three blocked-time counters), and the `db.query` message points at `db.rows`, which *is* computable from the response the workload returned. A refusal that leaves somebody stuck gets routed around. The three kept bounds are all facts about data rather than opinions about the program: the sum of the input sizes a transform must read; the count of **distinct** entities the measured response names (100 orders naming 3 customers requires 103 rows, and a response fetching the same customer 34 times is the finding); a reference implementation's retired-instruction count, which the bound records by name because the number is only a bound because something achieved it. **`WORTH_INVESTIGATING = 1.5` is a factor of the measurement, not a percentage of the bound** — a factor is what an investigation is deciding about, and §13's own example (76% of bound, so 1.32× available) is the case it calls "nothing left". **No computable bound is a `Screening` with no comparisons, not an exception**: F8's consequence is that this is the ordinary case, and an optional check that raised on the common path would be wrapped in a `try` and switched off, after which it would not run where a bound *does* exist either. **A measurement below its floor raises rather than reporting efficiency above 100%** — a bound claims the work cannot be done for less, so a measurement under it falsifies one of the two inputs. A floor of zero gives `None` rather than infinite headroom, and every path that cannot compute a factor defaults to *worth investigating*, because a headroom check that could not compute must never be the reason nobody looked. Every explanation carries §13's caveat that a bound is a ceiling and not a target, and a `Screening` where every bound is tight still refuses to read as a clearance. Registered with no capability and no fact gate — the only primitive in the set offered unconditionally, correctly, since whether a floor exists is a property of the workload rather than of the project. **Sabotage-verified on fourteen properties**, all caught first time.

### S-3.19 — Observation: deterministic instruction counting
Depends: S-3.6
Why: **this is the only known way to measure below the timing noise floor**, and `01-primitives.md` §12 already names it while no story implements it.
AC:
- Counts retired instructions for a workload, independent of machine and load
- Counts are reproducible across runs on the same input to within a stated tolerance
- Available to the Diagnostician as a metric alongside wall time and query count
- A test proves two implementations differing by less than the measured timing floor are still separable by instruction count
- The experiment result records which metric the conclusion rests on

Notes: S-0.4 measured the timing noise floor at **~20 ms, about 6% of a 350 ms endpoint**, at 20 repetitions — so a real 2% improvement is invisible to timing no matter how many times it is run. Instruction counts are deterministic, which collapses that floor to roughly zero for CPU-bound work. `01-primitives.md` §12 states the intended workflow: *"search against instruction count, then validate the single winner with proper interleaved statistical timing."* That makes this the enabling primitive for any optimization search (E10 onward), not merely another instrument. It does not help for I/O- or lock-bound cost — that is S-3.7's job, and the two are complementary rather than alternatives.

**DONE (2026-08-08)** — `src/coldfix/primitives/instructions.py`, ADR 049, 25 tests in `tests/primitives/test_instructions.py`. All five AC met. **The unit is a CPython bytecode instruction rather than an x86 one**: callgrind needs valgrind and does not exist on this platform, and a machine-instruction count of a CPython process is dominated by the interpreter loop rather than by the program under test. What §12 actually asks for is independence from machine and load with run-to-run reproducibility, and PEP 669's `INSTRUCTION` event has it exactly — `for i in range(n): total += i` costs `24 + 7n`, the same number every run, in a fresh process, and under three values of `PYTHONHASHSEED` (asserted by subprocess, since that is where it could actually fail). **The stated tolerance is zero**; anything else gives away the only reason to have the instrument. AC 4 is the test that matters: two loops 2% apart, timed with S-1.6's interleaved comparison, land far below S-0.4's 20ms floor and separate exactly by count. **Two corrections, both found by measurement, and each was a silent wrong answer before it.** (1) *The harness's own drain is subtracted and recorded* — forcing a lazy result means iterating it, and counted naively `sorted(range(50_000, 0, -1))` retires **300,096** instructions for a sort that happens entirely in C; the same drain over a filler list of the same length costs 300,087, leaving the subject **9**. Without it, result size masquerades as work and the visibility guard is defeated by any workload returning a large list. `drain_instructions` is recorded rather than folded away, because a subtraction nobody can see is a number nobody can check. (2) *The interpreter is warmed first* — the same workload counted **1311, then 787, 787, 787, 787**, because `isinstance(result, Iterable)` runs `__subclasshook__` once per result type and caches it; left in, that one-time work lands on whichever variant of a comparison ran first, biasing every comparison towards the second. The consequence is stated rather than hidden: **the count is of a warm subject**, so cache control remains the caller's business as in S-3.2 — the one place where reproducibility and a cold cache point in opposite directions, and reproducibility wins because it is the whole point. **An instrumented run reports no duration at all** — a missing key, not a caveat, since counting costs ~33× and a `seconds` measured under it would eventually be compared against a clean one. **`hidden_work` at 10⁶ instructions/second** (against the ~10⁸ measured for an interpreter running flat out) catches what the instrument cannot see: two sorts of different sizes both come back at 14 instructions, `trustworthy` is false, and the report refuses to name a winner rather than calling the smaller count faster. Every `Separation` records `decided_by` (AC 5) and says it is **a search result, not a verified improvement** — §12's workflow has two halves and this is the first. Monitoring tool ids 3 and 4 only, since PEP 669 assigns the rest. **Sabotage-verified on fifteen properties**, all caught; one sabotage was rewritten after it revealed redundancy rather than a gap — the three teardown steps are each sufficient alone, so removing any one changes nothing.

**EPIC 3 COMPOSITION CHECK (2026-08-08)** — after S-3.19, the epic was run as a whole for the first time: `tests/primitives/test_epic_composed.py` takes a toolkit from the registry for a Django-shaped profile and drives the planted-defect fixture through screen → guard → floor → count, with the controls and the decoy alongside. **Three defects, all in shipped, individually tested, sabotage-verified code, none reachable from a test of one module.** ADR 050. (1) **The registry's contents depended on import order.** Registration is an import side effect and nothing imported them all, so a caller with two primitives imported got a two-instrument toolkit and a `Selection` that listed **nothing missing** — a primitive nobody imported is not withheld, it does not exist, and *absent* versus *inapplicable* is exactly the distinction ADR 030 was built to keep. It also broke what `Selection` is for: a snapshot so the list cannot change mid-investigation, with nothing making the list *complete* when the snapshot was taken. `coldfix/primitives/__init__.py` now imports all twelve modules, and a test reads the package directory for `REGISTRY.register(` and asserts each module is reachable — from the filesystem rather than from a list, because a list in a test is forgotten at the same moment as the import. Measured cost: ~1.05s to import the package (~690ms sandbox chain, ~290ms Hypothesis), noted in the module and accepted. (2) **A missing capability outranked a settled fact.** A subject known *not* to parse untrusted input, in an environment with no mutation engine, came back `UNSUPPORTED` — read literally, *go and install a fuzzer* for a subject that will never need one. `all_of` already had the rule and the reason; `verdict` now takes the most decisive of the two verdicts rather than the first one checked. (3) **S-3.18 had no floor for an over-fetch.** The row floor puts the defect and its control at exactly **1.0x**, because `list_titles_over_fetching` returns the same number of *rows* as the projected control — over-fetching is a width defect and the row floor measures height. `01-primitives.md` §13's table had named the case all along (*serialization: fields consumed vs fields serialized*) and S-3.18 implemented the other three rows; `fields_required_by` is it, computable on the same terms as the row floor. **Two of the three are about how the registry gets populated rather than about the registry** — the same shape as ADR 029, and worth expecting again. Also recorded: the decoy out-costs the real N+1 at three of four scale points and crosses over above 36 authors, so a screen ranking by cost is wrong on any small dataset while growth is right at every point; and `linearithmic_sort` is `sorted()`, so its work is in C and S-3.19 correctly refuses to rank it against a Python quadratic — noted in the fixture README, because reaching for it as an instruction-counting baseline is the obvious move and does not work. **Six sabotages, all caught**, one after rewriting a test that passed because the over-fetch response is a list of plain strings, where a field count and an item count are the same number.

---

# EPIC 4 — Screening

**Goal:** find what is worth investigating using zero model calls.

### S-4.1 — Workload enumeration interface
Depends: S-0.1
AC:
- A workload exposes: `invoke()`, `scale(n)`, `reset()`, baseline metrics, fixture recipe, reset method
- Defined as a Pydantic model with full validation
- A hand-written workload for the target repo validates against it

**DONE (2026-08-08)** — `src/coldfix/screening/workload.py`, ADR 051, 26 tests in `tests/screening/test_workload.py`. All three AC met. **The six members are split in two, because three of them cannot cross the boundary this artifact exists to cross.** `02-architecture.md` §1.3 sketches one object with `invoke()`, `scale(n)`, `reset()`, a baseline, a fixture recipe and a reset method, and AC 2 requires a Pydantic model with full validation — but a callable does not serialize into an experiment log, does not go in S-5.1's replay key and does not survive a checkpoint. `Workload` is the artifact; `BoundWorkload` is the artifact plus the callables an adapter supplies locally, and **its constructor refuses a binding whose reset mechanism is not the one the artifact claims**, because measurements taken through such a binding carry a strategy that was never used and ADR 026's finding is that the results cannot reveal it. **`08-audit.md` F6's first condition is corrected, and the correction is the story's main decision** (ADR 051): `queries_at_n100 > queries_at_n10` is false for **every correctly batched endpoint** — two queries at ten rows and two at a hundred is what a prefetched list view does, it is this project's own `list_books_batched` control, and it is the shape the whole tool exists to produce. Written verbatim, a workload is verified only when it has an N+1, and the Explorer would silently discard the well-written half of every repository. The condition becomes *queries did not fall*; nothing is lost, because a stub route fails the payload and time conditions regardless and those were always what caught one, while queries *falling* stays disqualifying since it means something served the second measurement from a cache. Two conditions the audit did not specify: **two scale points minimum** (one measurement of a stub and one of a real endpoint are the same measurement) and **a scale ratio of at least 4×** (F6's thresholds were written against 10× and at 2× they demand a doubled payload for twice the data, rejecting correct workloads — below the ratio the harness says it cannot tell and names widening the spread as the action). **`work_verified` is a property with no field behind it**, and `extra="forbid"` is part of that rather than housekeeping: Pydantic's default silently discards an unrecognised key, so `Workload(..., work_verified=True)` constructed fine and dropped the claim without a word — F6's self-judged criterion reintroduced by a library default, found by a test that attempted the override and did not get an error. The fixture recipe records the **distribution** as well as the size (S-3.3: the uniform fixture is provably the blindest for a per-parent cost). **AC 3's hand-written target workload carries one observation, because ADR 011 measured one volume** — the `queries ≈ 1 + T + F + T` line beside it is a model, and turning a model into an observation would put a computed number where the artifact promises a measured one — so `django-helpdesk`'s own descriptor validates and honestly reports its work as unverified. One false negative survives deliberately: an aggregate endpoint is indistinguishable from a stub from outside, and the evidence string names the two things it cannot tell apart rather than calling the workload broken. **Sabotage-verified on fourteen properties**, all caught; two after fixing the tests — one sabotage passed because three redundant conditions each carried the case, and one because a digest test asserted process-independence by constructing the model twice in the same process, which proves only that equal inputs hash equally.

### S-4.2 — Growth screening
Depends: S-3.2, S-4.1
AC:
- Measures every workload at two or more scale points
- Computes growth ratio per metric
- Zero model calls — asserted by a test that runs screening with no LLM client configured

**DONE (2026-08-08)** — `src/coldfix/screening/growth.py`, 18 tests in `tests/screening/test_growth.py`. All three AC met. **AC 3 is met structurally, because the test as written passes for the wrong reason**: there is no LLM client anywhere in this codebase until E7, so "none configured" is the only state that exists — the literal test asserts nothing today and would go on passing after a client arrived. Instead the transitive import graph of `coldfix.screening` is walked **in a clean subprocess** and asserted to contain no LLM SDK (a screen that *imported* one could call it whether or not it did, and "did not this time" is not a property a test can hold onto), plus a full screen run with `socket.socket` removed — which no model call survives by any route. The socket half is honestly bounded: it establishes that the *screening layer* opens nothing, since a real subject's database connection is the workload's socket rather than the screen's. **Three scale points, not the two the AC asks for**: `fit_growth` needs three, and S-3.2 refuses a two-point sweep outright because two points define a line through themselves. The default spread is 16×, chosen so it also clears S-4.1's four-fold minimum — a screened workload therefore arrives at S-4.3 with its observations recorded and `work_verified` already answerable, which is the composition S-4.1 was shaped for. **Both the ratio and the fit are reported**: a ratio is what a reader checks by hand and what S-4.3 ranks on, an exponent is what the classification rests on, and they disagree in exactly the case that matters. A metric that is **zero at the smallest scale has no ratio** — `None`, not infinity — because a metric absent at the small end is a fact about the small end (a page that never overflowed, a queryset that never fired) and dividing by it would put that at the top of S-4.3's ranking. Observations are recorded **by scale** however the sweep ran, since S-3.2 permits a randomized order as §10's defence against ordering bias while ADR 002 needs byte-identical rendering. A workload that cannot be screened **stops the screen** rather than being skipped: a workload absent from a screen is indistinguishable from one screened and found healthy. **Two corrections to S-4.1, both found by being the first caller.** (1) `BoundWorkload` carried a reset method and no cache control, so `measure_once` refused every sweep — a reset returns the *database* to baseline and says nothing about what the process cached, and the artifact described something that could not be measured; `clear_caches` and `process_identity` are now on the binding, neither defaulted. (2) `Observation` rejected every negative metric, and **`blocked_seconds` is legitimately negative** — it is elapsed minus CPU, so it goes below zero whenever CPU time exceeds the wall clock, which S-3.7 reports as `PARALLEL` and deliberately does not clamp because a zero there would say *never waited*. The rule is now per-metric with the signed set named. **Sabotage-verified on twelve properties**, all caught; two only after adding the tests that reach the branch — no planted metric starts at zero, and the default scales are already ascending, so both guards were unexercised.

### S-4.3 — Flagging and ranking
Depends: S-4.2
AC:
- Flags superlinear growth and unexplained high flat cost
- Ranks by measured magnitude
- **States explicitly that call frequency is unknown** where the project provides no logs or metrics
- Skips healthy workloads

Notes: a 10× win on a monthly batch job outranks a 2× win on the hottest endpoint under magnitude ranking alone. Do not imply a priority the measurements cannot justify.

**DONE (2026-08-08)** — `src/coldfix/screening/flagging.py`, ADR 052, 20 tests in `tests/screening/test_flagging.py`. All four AC met, with AC 1's first half **corrected**: *flag superlinear growth* would walk straight past the N+1. `queries == 1 + A` is **linear**, in this project's planted fixture and in the unplanted defect ADR 011 pinned the development target for, so a screen flagging only a fitted exponent above one clears the single defect this system was built around, on the repository it was pinned against. The comparison is wrong, not the threshold: what gets flagged is growth **above what a metric can be expected to do** — a round-trip count is expected to stay *constant* (one batched round trip serves a hundred rows as easily as ten, and producing exactly that is what a fix *is*), an amount and a duration are expected to grow *linearly*, and anything unrecognised gets the linear expectation so it is flagged only when superlinear, which is AC 1 read literally kept as the safe default. The expectation is read off `counters.py`'s existing `Reading.EVENTS`/`TOTAL` vocabulary, so there is no second catalogue to maintain. **A duration must also clear S-0.4's ~20ms floor, and the screen found that by flagging its own control**: the batched workload — the clean counterpart, the shape a fix produces — came back `SUPERLINEAR` in `seconds` at 8.7× across a sixteenfold sweep, on a workload running in under a millisecond, because screening takes one sample per scale point and a fitted exponent over four single samples is a fit to noise. Duration flags now need the shape *and* an absolute rise, which is the two-test rule S-3.8's envelope already applies; counts are exempt because they reproduce to the integer. **Flat cost is a separate and visibly weaker kind**: the word doing the work in AC 1 is *unexplained* and screening cannot establish that, so `FLAT_COST` ranks below every growth flag, sits at 120 queries (more than three times the ~35-query floor S-0.3 measured on a real mature system, so the 37-query decoy is well clear), applies only to counts, and says in its own explanation that the workload did not grow, that it may be correct, and that removing slack rather than waste is the metastability trap. **AC 3 is on every report**: `08-audit.md` §6's exact scenario is a test — a tenfold win on a monthly batch job sorts above a twofold win on the busiest endpoint — and the requirement is not that the ranking solve it but that it say it cannot. Growth flags outrank flat-cost flags as a class because the two magnitudes are a ratio and a count with no honest exchange rate between them. **Three answers, not two**: a metric whose growth could not be fitted (zero at some scale point, so no exponent) is neither flagged nor cleared, because S-4.5 needs *could not tell* kept apart from *nothing there*. **Sabotage-verified on twelve properties**, all caught; three only after fixing the tests — two were noise-dependent (the control's `seconds` fit landed superlinear on one run and linear on the next, so the duration guard's removal passed on a lucky afternoon) and one test helper silently erased a metric's fit while setting its smallest observation, so the rule it was meant to exercise was never reached.

### S-4.4 — Findings cap
Depends: S-4.3
Why: without it, cost is unbounded.
AC:
- Caps findings investigated per run at a configurable default of 5
- Remaining flagged workloads are listed for the human, not silently dropped
- A test with 30 flagged workloads produces 5 investigations and a list of 25

**DONE (2026-08-08)** — `src/coldfix/screening/budget.py`, 15 tests in `tests/screening/test_budget.py`. All three AC met; no ADR, because nothing here needed a decision `04-cost.md` §12.4 had not already made. **The cap counts workloads, not flags**, and that is the story's one real design question: a workload flagged on three metrics — a query count that climbed, a payload that climbed with it, a duration that cleared the floor — is *one* investigation, because grounding, fixture seeding and the baseline are per workload and shared across everything flagged on it. Counting flags would let a single noisy workload with five flagged metrics consume an entire run's budget while nine others were never looked at, and a test asserts exactly that case. A workload takes its **best** position in the ranking rather than its worst, since taking the last place it occupies would push it down the list for having *more* evidence against it. **The deferral list repeats S-4.3's frequency caveat, and that is not duplication**: in a report, *magnitude ordering cannot express which finding matters more* costs a reader a moment's care, while at the cap it decides what gets investigated at all — a list of twenty-five deferred workloads that did not say so would read as *these matter less*. Deferrals carry their position and their flags, so the list can be resumed from and acted on without re-screening. **A ceiling in code as well as a configurable default**: a cap a caller can set to a thousand is not a cap, and §12.1's worst-case ~$58 per finding makes twenty already a four-figure run, so above that it refuses and says to split the repository. A cap of zero is refused rather than treated as a dry run — it investigates nothing having already paid for the screen, and if that is the intention it belongs at the call site. Four lists survive the cap rather than two, because *investigate these*, *look at these yourself*, *these are fine* and *these could not be classified* ask four different things of whoever reads them — dropping the last would turn S-4.3's *could not tell* back into *nothing there*. **Sabotage-verified on twelve properties**, all caught — one after rewriting a sabotage that **applied and was semantically a no-op**: iterating the flags in reverse produced the same workload ordering as iterating forwards, so it tested nothing. A fourth way for a sabotage to pass, alongside the three already recorded, and the check that the edit applied does not catch it.

### S-4.5 — Honest null result
Depends: S-4.3
AC:
- When nothing is flagged, emits a structured null result naming the workloads screened and the thresholds applied
- The null result is a successful terminal state, not an error
- When workloads run but touch almost no data, the message says so specifically rather than reporting "no issues found"

**DONE (2026-08-08)** — `src/coldfix/screening/null.py`, 16 tests in `tests/screening/test_null.py`. All three AC met. **Three ways of finding nothing, and only one of them is good news**: screened and shown to do real work with nothing above expectations (*nothing found*); screened with nothing establishing that it does real work (*not covered by this result*); and screened having touched no data at all (*the harness measured an empty workload*). `02-architecture.md` §1.5 names the third as a failure mode with a required response — report honestly and stop, never "no issues found" — and the second is F6's whole subject, so each has its own sentence and a test asserts the healthy sentence is **absent** from both. Collapsing any of them into the first is how a tool tells somebody their code is fine when it has not looked at their code. AC 2 is structural: there is deliberately **no error type meaning nothing found**, because an exception is something a caller handles and moves past, and this is the answer. **"Touched no data" is decided at zero rather than at *almost* zero**: a workload returning three rows might be a stub and might be a correct aggregate, and no measurement here separates those — inventing a threshold would put a number where a judgement belongs, and F6's verification is what speaks to the ambiguous case. It is also decided over **amounts only** — `materialized` plus the catalogue's `TOTAL` counters — because an empty workload still takes time, so consulting the duration would let a workload that returned nothing in four milliseconds read as having done something; an adapter's own metric could be a rate, a share or a flag and is not consulted at all. **The thresholds and conditions travel with the answer** (`CLAUDE.md`: exclusions carry their preconditions, and a null result is the largest exclusion this system produces), recorded **per workload** since a screen may have swept two workloads differently and one set of conditions at the top would be true of neither. **The guard runs in the other direction too**: constructing a null result from a ranking with flags in it raises, because reporting *nothing found* from a screen that found something is the one sentence this artifact exists to make impossible. `extra="forbid"` again, for S-4.1's reason. **Sabotage-verified on thirteen properties**, all caught; one needed a new test — consulting every metric rather than only the known amounts was invisible until a test asserted that a non-zero `seconds` does not count as data touched.

**EPIC 4 COMPOSITION CHECK (2026-08-08)** — `src/coldfix/screening/assess.py`, ADR 053, 14 tests in `tests/screening/test_screening_composed.py`. The epic finished with five stories, 93 passing tests and **no way to use it**: performing its own sentence — *find what is worth investigating using zero model calls* — took four calls in the right order plus a branch that lived in no module. **Five defects, none reachable from a test of one stage.** (1) *The branch was silent when wrong.* Asking for a plan when nothing was flagged returned `investigate=(), deferred=(), within_budget=True` — indistinguishable from a healthy plan, and S-4.5's null result was never produced at all. `assess` returns `Plan | NullResult` and the two are now exclusive by construction, since `plan` refuses an empty ranking and `null_result` already refused a flagged one. (2) *Guard counters belonged to the screen rather than the workload.* `screen` applied one `extra_counters` callable to every workload, so a project of six read one subject's counters six times and attributed them to the other five — never exercised, because no test had run more than one workload with guard counters at once, which is a thing only a composition does. (3) *A duration needs both ends above the noise floor.* `cpu_seconds` comes from `process_time`, which moves in ~15.6ms steps on Windows — the granularity that bit S-3.7 and S-3.13 — so a sub-millisecond workload records zero ticks small and two large, and a **quantisation artefact of 31ms cleared the 20ms floor and flagged the batched control**. (4) *A caveat attached to everything is one nobody reads.* `blocked_seconds` is unfittable on any workload fast enough for the two clocks to agree, so recording it as *could not tell* made **every** null result on a healthy workload say it did not cover everything; only metrics that could have been flagged are recorded now. (5) The test file collided with Epic 3's composition under pytest and mypy — the second time that trap has been hit, both times while writing a composition check. **Epic 4 catches one of the three planted defects, and that is now an assertion**: the over-fetch cannot be screened, because every metric on it and on its control grows linearly and the defect is payload *width* (S-3.18's field floor, in diagnosis); the downstream-cost workload cannot either, because its cost is a duration below the floor (S-3.19's whole reason). **Seven sabotages, all caught**, three after adding the tests that reach the property. One was dropped as unbreakable: the screen's determinism survived both shuffling and `id()` ordering, because every input to the sort is already deterministic — over-determined, like S-3.19's teardown.

**EPIC 4 CORRECTED `08-audit.md` TWICE, THE SAME WAY BOTH TIMES.** F6's work-verification formula (ADR 051) and S-4.3's *flag superlinear growth* (ADR 052) were each written from the defect they were about — a stub route, a quadratic — and each rejects the **correct** shape: F6's `queries rose` is false for every batched endpoint, and *superlinear* misses the N+1, which is linear. The audit is right about what to check and was not checked against a control. Read its fixes that way.

---

# EPIC 5 — Replay cache and cost control

**Goal:** make development fast and production affordable. **Build this before the agents.**

### S-5.1 — Experiment replay cache
Depends: S-3.2
Why: changes iteration speed from ~5 cycles a day to ~50.
AC:
- Cache keyed on `(repo_sha, workload_id, experiment_spec, fixture_hash)`
- Stores the full measurement result
- Cache hit returns in under 100ms
- A previously-run investigation replays end to end with zero model calls and zero container starts

**DONE (2026-08-09)** — `src/coldfix/replay/cache.py`, ADR 054, 40 tests in `tests/replay/test_cache.py`. All four AC met. **`git rev-parse HEAD` is the wrong `repo_sha`, and it is wrong in exactly the case this story exists for.** §6's stated purpose is debugging — which means an uncommitted tree — and the commit sha does not move for a whole afternoon of editing, so a cache keyed on it hands back a recording made *before* the change under test on every lookup, and the faster the cache is the more convincingly it lies. `repo_identity` returns the sha on a clean tree and `<sha>+<digest>` otherwise, over `git diff HEAD` rather than `git status --porcelain`: porcelain reports the same thing for every edit to a file, so an identity built from it would move once and then hold still — a test makes two different edits to one file and requires two identities. Untracked files count (a new module is code the experiment ran against), ignored ones do not (a virtualenv is not, and hashing one would cost seconds per key). Not S-1.1's `execute`, which bounds captured output: a truncated diff hashes the same for two trees that differ past the limit, which is a stale hit rather than a visible failure. **The key is hashed as a structure, never as a joined string** — `("api.books", "list")` and `("api", "books.list")` join identically under any separator that can occur inside a field, and a dot occurs in every workload id the slug rule permits; the hit that results is another workload's measurement. `ExperimentKey.of` derives `workload_id` and `fixture_hash` from one `Workload` together, so the two fields describing the subject cannot describe different subjects. **The machine partitions the store rather than joining the key** (ADR 054): a result is mostly durations, and as a fifth key field the environment would be a value somebody could later compare loosely — *close enough, same OS* — with no defensible threshold for how similar two machines must be before one's timings may stand in for the other's; as a directory the question never arises and a foreign recording simply misses. **`run` returns a `Recall`, not the result**, because a bare `T` makes a recording indistinguishable from a fresh run at every call site downstream, and *no finding without a measurement* is only meaningful if *when* survives with it. Recordings are JSON and not pickles, since S-5.2's whole method is opening one that produced a surprising answer. An unreadable entry is a **miss** — recomputing is always correct — but counted apart from one, or a cache that has silently stopped working forever would look exactly like a cold one; asking for the wrong result type **raises**, since as a miss two callers sharing a key would each recompute and overwrite the other's recording and both would appear to work. AC 4 is asserted by removing the mechanisms rather than counting calls: the recorded screen of all six planted workloads replays with `socket.socket`, `socket.create_connection`, `subprocess.run` and `subprocess.Popen` all poisoned, and a second test checks it from the subject's side — every bound workload comes back uninvoked, because a replay that quietly re-seeded would still be the ninety minutes this story removes. AC 3 is measured against a sixty-metric, six-point recording rather than a toy, since the cost of a hit is parsing and validating the value. **Sabotage-verified on eighteen properties**, all caught; two were rewritten first. One tested nothing (it removed an untracked file's *name* from the digest while its contents were still hashed) and one was dropped as **unbreakable**: naming the staged write like an entry changes nothing, because lookup is by exact digest filename, so no file other than `<digest>.json` can ever be read whatever the stage is called — the atomic-rename property is already carried by the sabotage that writes in place. A nineteenth survives and is over-determined in the same way: reading `self._environment` instead of the recording's cannot differ, since the partition guarantees every entry a cache can read was written by a cache with its environment. **The date test needed a bracket on both sides** — a lower bound alone passes for a `recall` that stamps the moment of the replay, which is the wrong date and the flattering one.

### S-5.2 — Replay mode
Depends: S-5.1
AC:
- A recorded investigation can be replayed to debug downstream agents
- Replay is byte-identical to the original for deterministic experiments
- Non-deterministic experiments are marked and re-run rather than replayed

**DONE (2026-08-09)** — `src/coldfix/replay/cache.py`, ADR 055, 25 tests in `tests/replay/test_mode.py` (65 across the package). All three AC met. **AC 3's obvious reading deletes S-5.1.** Every measurement carries `seconds`, `cpu_seconds` and `blocked_seconds`, and no duration repeats — so if *deterministic* meant the artifact reproduces byte for byte, nothing in this system would be deterministic and the cache built in the previous story could never legitimately hit. `Determinism` is therefore a claim about the **answer**: a screening sweep concludes from counts, which reproduce to the integer and which ADR 052 already requires to be what raises a flag; a timing comparison, a load curve or a fuzzing campaign concludes from a distribution and answers differently on a second run. **It is declared and nothing here can check it**, so it sits at the call site nearest the primitive, exactly where `experiment_spec` sits. **The default is `SAMPLED`** — an experiment nobody classified is one nobody has thought about, and the other default spends the first unconsidered call site on a silently stale answer — and the default has teeth: adopting it broke four of S-5.1's own tests. **A replay while recording needs the claim from both sides and the stricter wins**: a caller who has decided their experiment is deterministic must not thereby promote somebody else's sample, and one who has said they need a fresh sample must not be overruled by a recording taken as deterministic. **`RECORD` and `REPLAY` treat a sample oppositely, and that is not an inconsistency**: a fresh investigation must never take one afternoon's sample as a standing fact, while a debugging session must never let the session being debugged stop being the session that was recorded — so recording re-runs it and replay plays it back, and the `Recall` says which, with the *a fresh run would answer differently* sentence present on a replayed sample and deliberately **absent** on a deterministic one (Epic 4: a caveat attached to everything is one nobody reads). **Three modes on one object because the alternative is a branch in every caller** — `run` takes the same shape in all three, which is what makes replay a way of debugging the agent that calls it rather than a second route through it. `OFF` earns its place from S-15.1, whose AC is *N runs with the cache disabled*: agreement between ten replays of one recording is 100%, and without a mode that study writes `if use_cache:` around every experiment, which is Epic 4's branch-in-no-module again. Two refusals are structural rather than conventional: **`replay()` has no `compute` parameter** (a caller able to pass one still had to build the checkout, container and database a debugging session does not have), and **only a recording cache writes** — `record` refuses in the other two, because a replay that wrote back what it played back would restamp the session it was reading and destroy the record of when the run happened. **A missing recording is refused and the refusal lists the store**, since the four things that could be wrong — different tree, different fixture, spec spelled differently, experiment never run — look identical from the call site. **No artifact here describes an investigation**: that is S-8.4's append-only log, and inventing a second would guess at a schema it already specifies (S-1.7's recorded argument for leaving `Certification` unlogged); `recordings()` answers the question replay actually raises, which is *what is on this disk*. **Sabotage-verified on sixteen properties, all caught** — but only after the sixteenth was added: *replay mode records over what it played back* survived the first pass, because the property held merely as a consequence of one early return, and it is now a refusal on `record`. **S-5.1's AC-4 test was passing for the wrong reason and this story found it**: the planted fixture needs neither a socket nor a subprocess, so poisoning both left a *live* second screen able to pass it — poisoning the world proves nothing reached for it, and only the hit count proves nothing needed to. That assertion is now there.

### S-5.3 — Token accounting
Depends: S-0.2
AC:
- Every model call records: phase, agent, step type, model, input tokens, output tokens, cached tokens, cost
- Cost is queryable per phase, per finding, and per run
- A run report includes euros per confirmed finding

**DONE (2026-08-09)** — `src/coldfix/cost/accounting.py`, ADR 056, 36 tests in `tests/cost/test_accounting.py`. All three AC met. **AC 1's field list, read literally, cannot produce a cost.** The API reports two cache figures, not one, and they bill in **opposite directions**: a cache *write* costs 1.25× the input rate at the five-minute TTL and 2× at the hour — **more than not caching at all** — while a *read* costs 0.1×. A million write tokens and a million read tokens are both "a million cached tokens" and differ in price by 12.5×, so no arithmetic recovers a bill from the collapsed figure, and **the error is signed the flattering way** because caching is understood as a discount: one `cached_tokens` field priced at the read rate under-bills every cold prefix, which is the first call of every investigation. There is therefore **no `cached_tokens` field anywhere in the module**. The same trap sits one field earlier — **`input_tokens` is the uncached remainder, not the prompt** (the prompt is the sum of all three), so a reader who adds it to a cached-tokens figure double-counts and one who does not under-reports; `prompt_tokens` spells out the sum. The TTL is recorded **per call** rather than assumed, since 1.25× against 2× is a 60% difference on every cached prefix with nothing in the totals looking wrong. **`ModelCall` has no cost field to set** — `CLAUDE.md` forbids an agent reporting a measurement and a cost is one, so this is S-4.1's `work_verified` construction applied to the number with the strongest incentive to be wrong. **An unknown model is refused rather than defaulted**: fast mode on `claude-opus-5` bills at $10/$50 against $5/$25 — the same model id at twice the rate — so the book is keyed on the *billing* identity, and the first unlisted model this system meets will be a newer, dearer one, so a default under-bills. The book carries the date it was read; **ADR-002's indicative table was checked against current published rates and is correct as written** — no correction needed. Money is `Decimal`, because §12's figures are quoted to the cent and a project is ~1,000 runs of ~250 calls. **Euros carry their rate and its date** (a hardcoded rate is correct the day it is written and silently wrong after), and **`eur_per_confirmed_finding` is `None` when nothing was confirmed** — not zero, not the total, not infinity: S-4.5 ships *nothing found* as a successful answer, so the cost is real and the ratio undefined, and the rendered report says so in words because a missing figure must not read as a free run. **Not every call belongs to a finding**: §11 makes grounding once-per-repository, so `finding_id` is optional, the remainder is reported separately, and `reconciles` asserts the per-finding table plus the remainder equals the run total — trivially true today and the only thing that will notice the first time a call is attributed twice. `Phase` has **no screening member**, since Epic 4 is zero model calls and a slot for it could only ever be filled by a bug. **Sabotage-verified on twenty-three properties, all caught**, including pricing a cache write as a discount — the exact defect a single `cached_tokens` field would have shipped.

### S-5.4 — Budget enforcement
Depends: S-5.3
AC:
- Per-phase step caps: ground 60, investigate 40 experiments, repair 3 attempts, audit 2 rounds
- Global euro ceiling
- Exhaustion halts, checkpoints, and reports — it does not warn and continue
- A progress check escalates when N steps produce no new information

Notes: caps must be in code, not configuration. The worst case without them is unbounded.

**DONE (2026-08-09)** — `src/coldfix/cost/budget.py`, ADR 057, 32 tests in `tests/cost/test_enforcement.py`. All four AC met. **AC 3 describes the global ceiling, not every cap, and read as universal it makes three of the four wrong.** `02-architecture.md` §7.2 gives each phase its own disposition and only one is a halt: ground *aborts with a diagnostic*, investigate **emits the partial chain with its exclusions**, repair and audit *escalate*, and only the euro ceiling *halts, checkpoints and reports*. The investigate row is the one that matters — forty experiments that established something and ran out is **an answer**, the same answer S-4.5 ships when a screen finds nothing, and halting would discard the most expensive thing in the run. So exhaustion carries a `Disposition` and `dispositions()` makes the four enumerable, because a handler written against a single outcome is silently wrong for three of six phases in the direction of throwing away a real result. AC 3's other clauses hold everywhere: exhaustion **raises** rather than returning a flag, carrying the counters, the disposition and the spend — S-1.7's `NoiseFloorTooHighError` argument, and also AC 3's checkpoint, deliberately **not** a checkpoint schema (S-6.1's artifact, not guessed at). **Four caps, four units, and calls are none of them**: §12.1 budgets 120 model calls per finding against a cap of 40 *experiments*, so an experiment is ~3 calls and a cap counted in calls would stop investigation at a third of its budget — S-4.4's finding recurring. **Two scopes, wrong in both directions and plausibly**: a run-wide investigate counter gives five findings eight experiments each, and a per-finding ground counter re-grants 60 steps per finding — a cap that *rises* with the number of findings. Each audit gets its **own** two rounds; a shared pool would let a patch audit spend rounds a finding audit had not reached. **Caps are compiled in and may only be lowered** — both the constructor and `tighten()` refuse a raise, so the backlog note holds for a running process and not just the source file; lowering stays legal because spending less is not the failure mode and forbidding it would make the caps unusable on a smoke test. **A ceiling checked after the spend is a report, not a ceiling**: `authorize` refuses on a worst case that prices every prompt token as a one-hour cache write (2×, ADR 056's dearest input) and assumes all of `max_tokens` returns. **Progress is measured from the conclusion, not the agent's opinion** — *did that step teach me something* is F6's self-judged criterion, so steps carry a harness-computed digest of the conclusion, and it is over the conclusion rather than the measurement for S-5.2's reason (no duration repeats, so a digest over raw numbers never detects a stall). A stall and an exhaustion are **different exception types**, since exhausted means stop and stalled means change approach while budget remains. **Sabotage-verified on twenty-four properties, all caught — after one test was strengthened**: re-scoping the patch audit to the run survived, because the test compared two phases on one finding and nothing had been recorded against the phase under test; the property needs two findings competing for one counter. Fourth time a passing sabotage meant a weak test rather than weak code. **The test-file name collision was hit a third time** — `tests/cost/test_budget.py` collides with `tests/screening/test_budget.py` under mypy; renamed `test_enforcement.py`.

### S-5.5 — Model routing
Depends: S-5.3
Why: ~30 calls per run genuinely need the frontier model; ~220 do not.
AC:
- Each call site declares `creative` or `mechanical`
- Routing maps step class to model tier
- Tier assignment is configurable without code changes
- A test asserts mechanical steps never hit the frontier tier by default

**DONE (2026-08-09)** — `src/coldfix/cost/routing.py`, ADR 058, 36 tests in `tests/cost/test_routing.py`. All four AC met. **`creative` is a property of the step type, not a declaration.** `04-cost.md` §3 is a table of eight step types and the mechanical check that would catch a wrong cheap answer against each; six have one, hypothesis generation and attack design record *none exists*. So the class is derived, and `STEP_KINDS` keeps the **name** of each check rather than a boolean — a boolean nobody can audit is a routing decision made on a fiction, and S-5.6 needs to know *what* validates a step before it may retry one. AC 1's declaration is still taken and then **checked**: believed, a mislabelled step is the whole non-negotiable defeated through the front door, since relabelling hypothesis generation as mechanical sends it to the cheap model with every other guard satisfied. F6's finding again. **AC 3 sits directly against S-5.4's *caps in code, not configuration*, and both are right** — the two stories are asymmetric in mirror image because the harm runs opposite ways: caps permit lowering and refuse raising (unbounded spend is the harm); routing permits **dearer** and refuses creative below the frontier (an unvalidatable wrong answer is the harm, and one bad hypothesis wastes an investigation branch worth far more than the upgrade it saved). That is why AC 4 says mechanical avoids the frontier *by default* rather than always — the dearer override is permitted and tested. The creative rule is enforced on the per-phase route as well as the class-level one, because a rule enforced only on the general key gets walked around on the specific one. **A tier is what it costs, not what it is called** — the hole every other rule leaves open, since creative work always routes to the tier *named* frontier and the models behind tiers are configurable, so putting the cheapest model there satisfies every check while defeating all of them; tiers are resolved against S-5.3's price book and must be **ordered by price** (inversion refused, equality allowed — two tiers on one model is a legitimate deployment), and an unpriceable model is refused outright because it would produce a run whose cost is unknown. **The route is keyed on phase as well as class**: §12.3 runs grounding's mechanical calls cheap with a mature playbook and the investigate loop's mid-tier — two mechanical steps, two tiers — and a router that could not express that could not implement the €2,150 figure. `model_for` has **no default step class**, so a call site cannot decline to declare. ADR-002 is not contradicted: it states the *unrouted* default and explicitly releases cheaper tiers to E5's routing wherever a validator exists, and a patch has one. `frontier_share` puts the story's *why* in the run report — ~30 of ~250 calls — because a share drifting upward is the routing quietly ceasing to work. **This is the third Epic 5 story whose AC summarize a table elsewhere and lose what it specifies** (S-5.3's two cache figures, S-5.4's four dispositions, S-5.5's per-phase routing and validator column): in this epic the backlog is the summary and `04-cost.md` is the specification. **Sabotage-verified on twenty-one properties, all caught.**

### S-5.6 — Cascade with escalation logging
Depends: S-5.5
AC:
- Cheap model attempted first wherever a deterministic validator exists
- Escalates after two failures
- Escalation rate logged per step type
- **No cascading on hypothesis generation or attack design** — no validator exists for those

**DONE (2026-08-09)** — `src/coldfix/cost/cascade.py`, ADR 059, 34 tests in `tests/cost/test_cascade.py`. All four AC met. **AC 4 is not a separate rule, it is a consequence of AC 1.** Cascade is gated on `mechanical_check is not None`, so `04-cost.md` §3's two *none exists* rows — hypothesis generation and attack design — are refused by the same condition that admits the other six. No branch names them, and a new step type with no check would be refused without anybody editing this module. Written as its own rule it would have been one somebody could forget to apply, which for `CLAUDE.md`'s standing non-negotiable is the wrong shape. **A caller may not supply the missing validator**: `cascade` refuses a creative step type even when handed a `validate` callable, because §3's table *is* the statement that no deterministic check exists and a caller-supplied one is a judgement wearing a validator's clothes — accepting it would route the one step nothing can verify onto a cheap model with every other guard satisfied. **The cascade starts where S-5.5 routes and escalates one rung**, which reconciles two things that look like they conflict: §12.3 lists repair as *cascade mid→frontier* while S-5.5's AC 4 says mechanical never hits frontier by default. Both hold once **routing** and **escalation** are separated — repair is *routed* mid and only *reaches* frontier by failing its own check twice. The rule is uniform and reproduces §12.3 at both ends: repair mid→frontier, grounding cheap→mid. An invariant follows: since S-5.5 never routes mechanical work to the top tier, every cascadable step always has somewhere to escalate to. **A rejected result is never returned** — `NoDearerTierError` rather than handing back the last attempt, because a cascade that returned an answer its own validator refused would make the validator decorative and the caller cannot tell the two apart by looking. **§3 specifies a fifth thing the AC omit**: *if a step escalates more than ~30% of the time, promote it permanently* — above that, two cheap attempts plus a dear one cost more than starting dear. A rate below ten samples is **`None`, not a number** (one escalation out of one is 100%, and promoting on that is a coin flip — S-4.2's rule again), and the log is **two-sided**: a step type that has never escalated across enough attempts is either the result the technique exists for or a validator that cannot fail, and the log reports the number rather than choosing. **S-5.5's decision to keep the validator's *name* pays off here** — the refusals, `cascadable()` and the promotion report all quote the check, which is what lets a reader verify a routing decision against §3 rather than trust it. **Fourth Epic 5 story whose AC summarize a table and lose part of it** (S-5.3's second cache figure, S-5.4's four dispositions, S-5.5's per-phase routing, S-5.6's promotion threshold). **Sabotage-verified on twenty-one properties, all caught.**

### S-5.7 — Cache-friendly context assembly
Depends: S-5.3
Why: the single largest cost variable in the system.
AC:
- Prompt structure is: stable system prefix, stable playbook, stable source, **append-only log**, varying question
- The experiment log is never reordered or re-summarized mid-investigation
- A test asserts the prefix is byte-identical between consecutive calls in one investigation
- Cache hit rate is measured and reported

Notes: 120 calls at 60k uncached costs ~$39; the same calls at 12k with caching cost ~$1.68. Any reordering of the log invalidates the cache and silently multiplies cost.

**DONE (2026-08-09)** — `src/coldfix/cost/context.py`, ADR 060, 39 tests in `tests/cost/test_context.py`. All four AC met. **Every way of losing the cache is silent** — the request succeeds, `cache_read_input_tokens` is zero, the bill goes up — so this story is about failures that read as success. **The four cacheable boundaries fit the four `cache_control` breakpoints exactly**, with the varying question after the last of them and never given one (caching it writes an entry no later call can read and spends one of only four doing it). **AC 2 is an absence, not a discipline**: there is no method that reorders the log, none that re-summarizes it, and none that changes a stable segment — the system prompt, playbook and source are captured at construction, so a `datetime.now()` in them is evaluated once, which is what makes the prefix byte-identical rather than merely intended to be. **The minimum cacheable prefix is not monotonic and the cheap tier is the worst**: opus-5 512, sonnet-5 1024, **haiku-4-5 4096** — and a prefix below the minimum caches *nothing*, with no error. S-5.5 routes grounding to haiku *because it is cheap*, and §12.3's engineered grounding is ten calls with a mature playbook — a short prompt — so **routing a step down a tier can raise its effective cost**, the exact opposite of the routing's purpose. `minimum_prefix` refuses an unrecorded model rather than defaulting, and the below-minimum message names the routing interaction, because a reader just told to use a cheaper model will not otherwise connect the two. **The log is one block because a breakpoint looks back at most 20**: rendered per experiment it crosses that at experiment 21, and S-5.4 caps investigation at 40 — the obvious implementation stops caching exactly halfway to its own budget. One block also makes append-only checkable (call N's log is a byte prefix of call N+1's). **Token counts are measured, never estimated** — `viability()` answers `UNKNOWN` without a `count_tokens` figure, since `tiktoken` is OpenAI's tokenizer and undercounts Claude 15-20% on prose and more on code, and here the estimate would be *of whether a cost control works* (S-4.5's rule). **The first call can never hit** and pays the 1.25× write premium, so `warm_hit_rate()` excludes it — a blended figure alone makes a working cache look worse the shorter the run — and the report names tokens *written* too, since a write bills above the uncached rate (ADR 056). **Sabotage found three real gaps out of twenty-three, the highest proportion in Epic 5**: the breakpoint and lookback guards sat inside `render`, which builds a fixed five-block tuple, so neither could ever fire (S-3.12's finding again — a guard no test reaches is a guard nobody has checked); they are now a `check_blocks` function a test exercises directly. And `stable_prefix()` had only been asserted against *itself*, so dropping the source segment changed no test — a prefix missing a segment would report *byte-identical* while the omitted part moved freely. A fourth survivor was redundant code and was removed rather than tested. All twenty-three caught now.

### S-5.8 — Context pruning with on-demand detail
Depends: S-5.7, S-5.1
AC:
- The log in context holds one-line summaries per experiment
- `read_experiment(n)` returns full output, stacks, and raw counters
- The prompt states explicitly that detail is retrievable
- No information is discarded — only deferred

**DONE (2026-08-09)** — `src/coldfix/cost/pruning.py`, ADR 061, 38 tests in `tests/cost/test_pruning.py`. All four AC met. **The detail is held, not referenced, and pruning is what `render` omits** — there is no `truncate`, `forget`, `evict` or `compact`, so AC 4 is an absence rather than a promise (S-5.7's construction again). Held here rather than pointed at, because *nothing discarded* must not depend on another store still having it. **Retrieved detail never re-enters the log**: S-5.7 made the log append-only so the cached prefix stays byte-identical, and writing experiment 3's stacks back into the middle of it at call fifty would invalidate every breakpoint after — **turning the 23× win into a loss on the same call that was trying to save tokens** — so `read_experiment` returns text and mutates nothing. **The summary is composed, not authored** (F6, fourth time this epic): an agent asked to summarize its own experiment can write *nothing of interest* and the detail is then never retrieved by anyone — information preserved and lost at once — so the header comes from the primitive and target and only the outcome is supplied. Indexes are assigned by the log, since `read_experiment(7)` must mean the seventh experiment and a caller-supplied index can collide, skip or restart; an out-of-range index is **refused by name rather than clamped**, because the caller is a model and a model that guesses must be told, not handed the nearest record. **§5's 60-80% is measured, not repeated** — `reduction()` against an unpruned log and `net_reduction()` after retrieval — and **retrieving every experiment exactly once cancels the saving precisely** (zero tokens saved, plus round trips the metric does not count), with any re-read taking it negative; not clamped, because a clamp would hide the case the metric exists to expose. **AC 1 says one line and §5 shows two**: neither binds — the token budget does, so each *part* is single-line and bounded, giving §5's exact shape at a predictable size across forty experiments. Fifth Epic 5 story where the AC and the cost document differ and the document is the specification. **Two defects were found by tests failing rather than by sabotage**: the emptiness check was written against total characters, but the retrieval notice always renders, so an empty log reported a confident 0% saving instead of *no experiments yet*; and *retrieving everything* cancels the saving exactly rather than going negative — a sharper statement than the one first written down. **Sabotage-verified on twenty-two properties, all caught**, one after rewriting a clamp inserted *after* the range check, which was dead code rather than a sabotage.

### S-5.9 — Vendor cost comparison on effective cost
Depends: S-5.3, S-15.3
Why: ADR-002 chose a vendor on SDK ergonomics, not on price, and the question will otherwise be re-argued from list prices and memory.
AC:
- Runs the same fixed scenario set against at least two vendors
- Reports **cost per confirmed finding**, not cost per token
- Reports effective input cost with caching in effect, alongside list price, and states the measured cache hit rate for each vendor
- Reports experiments-to-conclusion per vendor — a dearer model reaching the answer in fewer experiments may be cheaper overall
- Records each vendor's minimum cacheable prefix and cache TTL, since both change effective cost independently of list price
- Result is published in the cost report and supersedes ADR-002's provider choice if the numbers warrant it

Notes: **list price is the wrong number for this workload.** Cache reads bill at roughly 0.1x of input on the first-party API, and the append-only experiment log exists precisely so the cached prefix stays byte-identical — so effective cost is dominated by hit rate, not sticker rate. Caches are also model-scoped, so a vendor that is 30% cheaper per token but has a larger minimum cacheable prefix, a shorter TTL, or weaker prefix semantics can cost more here. S-0.8's harness is already the right shape for this: a fixed scenario set with programmatic scoring, so swapping the request layer is the only work. ADR-002 is a record of a decision, not a commitment — this story exists to make it falsifiable rather than defended.

**PARTIAL (2026-08-09)** — `src/coldfix/cost/vendors.py`, ADR 062, 28 tests in `tests/cost/test_vendors.py`. **AC 3 and AC 5 are met; AC 1, 2, 4 and 6 are blocked**, and were put to Allen before any code was written (`CLAUDE.md`: ask rather than improvise when a dependency is missing). **AC 1** needs a second vendor's account and real spend — confirmed with Allen that **there is no second vendor**, so this is blocked indefinitely rather than pending; ADR-002's deferred different-vendor requirement for the Adversary is blocked for the same reason. **AC 2** needs E9's finding audit to confirm anything, and S-0.8's scenarios produce no findings at all. **AC 4 is not merely unmeasured — S-0.8 measured it and found it unmeasurable**: in 60 runs the model chose *no finding, stop* **zero times**, reasoning correctly, withholding the verdict, and always proposing one more experiment; until something bounds that the figure has no value, and S-5.4's budget halt is what bounds the damage. **AC 6** needs the numbers. What is built is the half that makes ADR-002 falsifiable rather than defended: `effective_input_usd_per_mtok` shows an input token costs ~a quarter of list at §12.2's engineered shape (120 calls, 12k prompt, 85% cached), with the write premium **amortised across the calls that read it** so a one-call run pays it in full; and the AC 5 facts — minimum cacheable prefix (per model, from S-5.7), cache TTLs and their write premiums, the read multiplier, prefix-match semantics and cache scope — are recorded with a **date and a source**, because a profile with no provenance is memory with a dataclass around it. **One profile is recorded and the second is deliberately absent**: `recorded_profile` refuses an unknown vendor, and a test asserts `VENDORS` holds exactly one entry so adding a second is a deliberate act with a failing test behind it. The Notes' claim — *30% cheaper per token but a larger minimum cacheable prefix can cost more* — is demonstrated against a **hypothetical** profile that lives only in the test file and is named hypothetical, with a control asserting it wins once the prompt clears its minimum (without which the demonstration would pass for a module that simply always preferred Anthropic). **Absent measurements report their absence and name their blocker** — *needs E9's finding audit*, *not measurable: the model never concludes* — because a reader who does not know why a column is empty will fill it with a guess; `cheapest()` returns `None` below two vendors and the report says *this is a cost model rather than a comparison*. The Anthropic profile is **derived** from S-5.3's price book and S-5.7's prefix table rather than copied, since a second copy is a second thing to go stale. **ADR-002 stands, untested** — nothing here supersedes it, and the report says so. **Sabotage-verified on twenty-four properties, all caught**, one after a real finding: `price_book_agrees` took no argument, so it could only ever return `True` and a version that always did was indistinguishable — **fourth time this project has recorded S-3.12's finding** that a guard nothing can make fail is a guard nobody has checked.

**EPIC 5 COMPOSITION CHECK (2026-08-10)** — `src/coldfix/cost/session.py`, ADR 063, 26 tests in `tests/cost/test_cost_composed.py`. The epic finished with nine stories, 294 passing tests and **no way to spend money through it**: performing its own sentence — *make development fast and production affordable* — took seven objects in the right order, and **no test file in the epic touched more than three of the nine modules**, always a module plus its direct dependency. **Four defects, and the first two are invisible because both wrong joins keep the cache working** — caching is a prefix match, so a log that is wrong in *content* but still append-only reports full hits and a rising bill, with no failing request to notice. (1) *Two append-only logs with no correct join.* S-5.7's `Investigation` appends a line; S-5.8's `PrunedLog` renders a block. Appending each summary drops the retrieval notice, so the agent is never told `read_experiment(n)` exists and never asks — **detail preserved and lost at once, the exact failure S-5.8 was written to prevent**; appending the rendered block after each experiment re-appends every earlier experiment with it, so at S-5.4's cap of 40 the log is carried **40 times** on the prompt whose whole purpose is to be small. A log now has one owner (`log_source`) and the other object refuses `append` **and** `entries` — `entries` refuses rather than returning an empty tuple, because an empty tuple lets `is_append_only` compare two empty lists and report the property holding over a log it never saw (S-3.12's finding, fifth time). The one test that joined the two modules appended `render()` **once, after all 40 experiments** — a static snapshot, never the incremental case a real investigation has. (2) *Caches are model-scoped and nothing respected it.* S-5.9 records the fact in `CachePolicy.scope`; `Investigation` binds one model at construction, the router picks one per step and the cascade escalates to a third mid-step, so the obvious composition sends one prompt to three models and calls the result a cache. `Session` holds **one prompt per model** and never blends hit rates — a blended figure averages over caches that share no entry and credits the second model with the first one's warm calls. (3) *A cascade spends three times what was authorized, and worse than three times*: `cascade` makes up to three calls against a ceiling built to price one, and the third runs a **tier dearer**, so even multiplying by the attempt count under-prices it — authorization moved inside the `attempt` callable S-5.6 already took and nothing had used. (4) *`frontier_share` cannot see an escalation*: it maps (phase, class) through the router, and escalation is not a routing decision, so the one path S-5.6 guarantees exists is the one path the metric is blind to — and escalation is exactly how frontier use drifts upward, which is what the figure exists to catch. **S-5.7's routing hazard is now priced rather than described**, giving S-5.9 its first caller: at §12.3's engineered grounding (2k prompt, 85% cached) `claude-haiku-4-5` caches nothing against its 4096 minimum and bills $1.0000/MTok while `claude-sonnet-5` clears its 1024 and bills $0.7316 — **the cheap tier is 37% dearer** — with a control at 5k where haiku caches and wins by 3x. **Sabotage-verified on fourteen properties, all caught** — and **the first sabotage run was itself invalid**: the runner restored with `git checkout -- <paths>`, and a pathspec naming an untracked file makes git error and restore *nothing*, so the new module kept every sabotage and later results were confounded by earlier ones. A sabotage harness is code, and code unchecked against its own failure mode is what these checks exist to find. **Two limits recorded rather than closed**: `ModelCall` carries the step *class* while `EscalationLog` is keyed on step *type*, so §3's promotion rule (*above ~30%, starting dear is cheaper*) is still decided on rate alone though the ledger now holds every number the comparison needs; and **nothing in Epic 5 has met a real API** — the arithmetic is right and the guards fire, which is not the same as working in the system.

---

# EPIC 6 — State and persistence

**Goal:** state that survives crashes, and knowledge that survives rewinds.

### S-6.1 — Checkpointed state schema
Depends: S-0.2
AC:
- TypedDict with reducers: `experiments`, `attempts`, `flags` use append semantics
- A test proves a node returning a single experiment appends rather than replaces
- Schema validated on every node transition

Notes: `Annotated[list, add]` is load-bearing. Without it the agent loses its own history, re-tests rejected hypotheses, and loops while appearing to work. This is the most common bug in building these systems.

**DONE (2026-08-11)** — `src/coldfix/state/checkpoint.py`, ADR 064, 34 tests in `tests/state/` (`test_checkpoint_state.py` and `test_state_in_langgraph.py`). All three AC met. **No ADR had ever chosen the orchestrator**: every document assumes LangGraph (`02` §8, `03` §1.1, S-12.2's *SQLite checkpointer*) but it was never a recorded decision and never a dependency, and the backlog's decisions table lists orchestration nowhere — ADR 064 is that record. **AC 2 is only meaningful against the real runtime**, since *a node returning a single experiment appends rather than replaces* is a claim about the framework's merge step; against a reducer we call ourselves it passes whether or not the schema is annotated, which is the one thing worth knowing. So a spike compiled real graphs before anything was designed, and **one of its findings contradicted the design premise**: a Pydantic state schema does **not** give per-transition validation — LangGraph validates the graph's *input* and nothing after it. Two more decided the module: **a node returning an unknown key is silently ignored** (no error, no write, and `extra="forbid"` does not catch it), and **a custom reducer runs on every write and may raise**. **The schema is a Pydantic model, superseding §1.1's literal `TypedDict`** — `CLAUDE.md` requires a Pydantic model for every artifact crossing a node boundary and this is the artifact that crosses *every* one; a `TypedDict` is also `dict[str, Any]` under strict mypy and cannot be validated at construction. The reducers survive the change untouched, because `Annotated[list[X], AppendOnly(...)]` is `typing` plus a callable. **`langgraph` is a dev dependency and nothing under `src/` imports it**, so the schema stays framework-independent while being read correctly by the framework; it becomes a project dependency at S-12.1. **The reducer is checked rather than `operator.add`**: `add` appends but accepts the mirror-image bug in silence — a node returning the whole channel instead of its delta **doubles** history rather than losing it, which S-5.7's cached prefix and S-5.8's `read_experiment(7)` both depend on not happening; a test asserts `add` accepts exactly what `AppendOnly` refuses. **AC 3 is a wrapper because the framework leaves the hole**, and the unknown-key case is the one that matters: `{"experiements": [...]}` writes nothing and reports nothing, leaving an investigation that ran to its cap having recorded no experiments — the note's *loops while appearing to work*, reached through a typo. **The trust ledger is deliberately absent** (F5): keeping it here *is* the defect, since a rewind would restore the trust level that preceded the failure that caused the rewind. Every field is JSON-representable because ADR 003 stores checkpoints in SQLite or Postgres, and the artifacts these channels carry belong to epics that do not exist yet — the guess S-5.4 declined to make when it left this schema to this story. **Two LangGraph typing traps are recorded for S-12.1**: `add_node` infers `NodeInputT` from `input_schema=`, and without it that variable solves to `Never` so every *correctly annotated* node fails to type-check while an unannotated lambda passes; and `_Node` is a `Protocol` whose `__call__(self, state: ...)` takes a **named** parameter, so a plain `Callable[[State], ...]` can never satisfy it — `node` is therefore signature-preserving. **Sabotage-verified on thirteen properties, all caught**, including dropping the annotation from each channel independently, and the baseline was re-run green *after* the pass — the check the Epic 5 composition run skipped.

### S-6.2 — Persistent store
Depends: S-6.1
Why: rewind must not discard the knowledge that motivated it.
AC:
- Separate database holding: failure memory, playbooks, trust ledger, replay cache
- Append-only; no checkpoint restore touches it
- A test rewinds to an earlier checkpoint and asserts failure memory from the later state is still present

**DONE (2026-08-11)** — `src/coldfix/state/persistent.py`, ADR 065, 19 tests in `tests/state/test_persistent_store.py` (12 of them `postgres`-marked, run against a real container). All three AC met. **Append-only is enforced by a database trigger, not by this module's method list**: a Python class with no `update` method is append-only until somebody opens a connection, and the point of this store is that it survives operations performed *on* the system rather than through it — so the tests attempt `UPDATE`, `DELETE` and `TRUNCATE` through `psycopg` rather than through the module (`CLAUDE.md`'s rule for a safety property). **`TRUNCATE` is guarded separately and deliberately**: it is the exact verb ADR 003 names when it says dropping checkpoints must not reach the playbook, and Postgres will not accept it on a `FOR EACH ROW` trigger — so the obvious single trigger leaves it open **while looking complete**, and it gets its own `FOR EACH STATEMENT` trigger and its own test. **One journal, not four schemas**: the four things F5 lists are *what must survive*, not four shapes this story knows, so entries are `(collection, key, jsonb)` and Epic 13 decides what one means — the third time this project has declined the same guess (S-5.4 left the checkpoint schema to S-6.1, S-6.1 left the log entry to S-8.4, S-6.2 leaves the playbook to S-13.1). **AC 1 lists the replay cache and S-5.1 already built it on a filesystem**; moving it into Postgres would trade away both of ADR 054's properties — a recording openable by hand when it produced a surprising answer (S-5.2's whole debugging method) and a foreign machine's recording missing rather than matching — for tidiness. What F5 requires is that a checkpoint restore cannot reach it, which a directory outside the checkpoint database satisfies at least as completely as a row would; **`MEMBERS` records all four with where each lives**, and a test asserts the replay cache is filesystem-backed and is *not* a journal collection, because a list quietly holding three is the kind of partial truth this project keeps finding in its own summaries. **The store takes a `VerifiedDatabase`** — S-2.5's construction reused, and its default name patterns already include `coldfix_*`, which is evidence the guard was built expecting to cover our own store. **`refuse_shared_store` enforces ADR 003's separation**, which had been recorded and never enforced: a `Path` is the development SQLite checkpointer and cannot collide, and a URL naming the same database on the same server is refused. **AC 3 is proved against a real rewind rather than a simulated one** — S-6.1 made `langgraph` available, so the test compiles a graph with a checkpointer, learns something in a late node, rewinds via `get_state_history`, and asserts the checkpointed state went back while the failure memory did not; a second test takes the stronger form and **resumes** from the rewound checkpoint, asserting the resumed run reads what was learned *after* the checkpoint it resumed from — which is the property that actually stops it repeating the rejected attempt. **Sabotage-verified on twelve properties, all caught**, including removing the `TRUNCATE` trigger independently of the row-level one; baseline re-run green after the pass.

### S-6.3 — Store experiment results by reference
Depends: S-6.1, S-5.1
AC:
- Checkpointed state holds hashes and summaries, not full results
- Full results live in the replay cache
- Checkpoint size stays bounded as the experiment log grows
- A 40-experiment investigation produces checkpoints under a stated size limit

**DONE (2026-08-11)** — `src/coldfix/state/reference.py`, ADR 066, 23 tests in `tests/state/test_results_by_reference.py`. All four AC met. **The stated limit rests on arithmetic rather than a sample**: every `ExperimentRef` is size-checked at construction against `MAX_REFERENCE_BYTES` (1 KiB), so S-5.4's cap of 40 experiments gives a log that **cannot** exceed 40 KiB whatever the measurements were, and the 64 KiB state limit has that behind it with 24 KiB left for artifacts that do not grow with the investigation. Without the per-entry check the bound is a hope — a spec carrying a large parameter block grows the checkpoint with nothing noticing, and the checkpoint is written after *every node*. **The reference carries the key, not only the digest**: F13 says *keyed by hash* and the digest is the filename, but S-5.1's lookup takes a key and derives the digest, so a digest alone finds the file only by scanning the store; carrying the key makes a fetch one read and makes the reference self-describing — and a key is identity, not a result, so the state still holds no measurements (asserted). **The outcome bound is S-5.8's, shared rather than copied** — F13's closing note about `04-cost.md` §5 is a design instruction, and importing `MAX_SUMMARY_CHARS` is what stops the prompt log and the checkpoint log drifting apart; the summary is composed from primitive and target with only the outcome supplied (S-5.8's construction, F6's reason). **Size is measured as JSON, and the reasoning behind that was wrong until a test corrected it**: the assumption was that ADR 003's Postgres checkpointer stores JSON so the JSON encoding *is* what gets written — LangGraph's serializer is **msgpack and smaller**, 13,404 bytes against 15,761 for a full forty-experiment log (~85%). That is the safe direction, since a state fitting this limit fits what is actually written; a proxy that under-estimated would pass here and be breached on disk. The ratio is pinned by a test rather than reasoned about. **The sharpest property is that checkpoint size does not depend on measurement size** — two investigations identical but for what they measured, 100 bytes of output per experiment against 100 kilobytes, produce checkpoints of *exactly* the same size. **F13's claim is measured rather than repeated**: the same forty stored by value come to **1.21 MB**, about 18× the limit at a conservative 30 KB of output each — the control without which 64 KiB would pass equally for a design that stored everything and was tested with small results. `ResultNotStoredError` names the state and the store disagreeing, which is what a checkpoint carried to another machine produces, since S-5.1 partitions recordings by environment. **One incidental fix**: the base exception was `ReferenceError`, which shadows a Python builtin (`ruff` A001) — now `ExperimentReferenceError`. **Sabotage-verified on thirteen properties, all caught**, including raising the per-entry budget until the arithmetic stops holding and raising the stated limit until the by-value control passes; baseline re-run green after the pass.

### S-6.4 — Post-patch staleness policy
Depends: S-6.1
AC:
- After a patch ships, screening results for workloads touching modified files are invalidated
- Untouched workloads keep their measurements
- Pending findings whose context files were modified are invalidated and re-investigated rather than repaired from a stale chain

**DONE (2026-08-11)** — `src/coldfix/state/staleness.py`, ADR 067, 19 tests in `tests/state/test_post_patch_staleness.py`. All three AC met. **F14 and §6's interacting-findings flaw are the same question about two artifacts**, so there is one mechanism and two consequences. The obstacle: **nothing records which files a workload touches** — S-4.1's `Workload` has an entry point, a fixture and a reset method and no notion of the source it executes, and the honest source is a measurement (S-3.9 captures stacks, and a frame names a file), so this module takes coverage from its caller rather than inventing it. **Unrecorded is a third state and it invalidates**: a workload whose files nobody recorded cannot be *shown* unaffected, and flattening that to *fresh* keeps a measurement the patch may have invalidated — the exact route to repairing from a stale chain that §6 exists to close. `UNCOVERED` stays **distinct from `STALE` in the report while identical in the action**, because the fixes differ — a stale workload needs re-screening, an uncovered one means nobody is recording what the workloads run, and one number hides a missing instrument behind a routine invalidation (S-3.1's four-way split, same reason). The distinction is in the type too: `files` is `frozenset[str] | None` with a named `Coverage.unrecorded` constructor, since an empty set is a *claim* that the subject runs no modified file and `None` is the absence of one. **There is no `FindingAction` meaning repair-from-a-stale-chain** — two members, only `FRESH` yields `REPAIR`, and the test enumerates `Freshness` so a state added later must decide what it means rather than defaulting to repairable. **Paths are normalized to one form and an unnormalizable one is refused**: git reports repo-relative forward-slash paths and a stack frame reports an absolute one, and intersecting the two forms yields the empty set — every workload reads unaffected, every stale measurement is kept, and **nothing raises**. **A test found that same bug in the guard itself**: `PurePath.is_absolute()` is platform-dependent, so on Windows `/home/allen/subject/src/api.py` has no drive, reports `False`, and would have normalized to a repo-relative-looking path matching nothing git reports — correctly refused on Linux, silently accepted here. `_absolute_anywhere` now checks both flavours. **The policy is only as good as the coverage it is given, and nothing produces it yet**: every workload reports `UNCOVERED` until something records what they run, so everything is re-screened after every ship — correct for the current system and **visible in the report** rather than a policy that looks like it is saving work while keeping stale measurements. **Sabotage-verified on fifteen properties**: fourteen caught on the first pass, and the fifteenth survived because the *test* was weak — it asserted the stale file's name appeared in `report.describe()`, which also prints the patch's own modified files, so it passed via the header line whatever the per-workload line said. Now asserted on the assessment's own line. **Fifth time a passing sabotage has meant a weak test rather than weak code.**

**EPIC 6 COMPOSITION CHECK (2026-08-11)** — `src/coldfix/state/investigation.py`, ADR 068, 17 tests in `tests/state/test_state_composed.py`. The epic finished with four stories, 95 passing tests and **no way to run one investigation through them**: performing its own sentence — *state that survives crashes, and knowledge that survives rewinds* — took four modules in the right order plus three joins that lived in none of them, and **no test file in the epic touched more than two of the four**. **Three defects.** (1) *The staleness policy's answer had nowhere to go.* S-6.1 held `screening` as a flat `Sequence[JsonValue]` and S-6.4 invalidates **per workload** — nothing in a flat sequence of opaque entries says which workload an entry belongs to, so a correct answer **could not be applied** and the caller had to rebuild the channel from a shape that does not carry the identity the rebuild needs. Neither story could see it: one owns the shape and knows nothing of the rule, the other owns the rule and takes coverage as an argument. `screening` is now keyed by workload id and `apply_ship` is the rebuild, once. (2) *`experiments` accepted a full measurement.* S-6.3 bounds a checkpoint by storing references and S-6.1's channel is `list[JsonValue]`, so a node appending the measurement itself satisfied the schema, the reducer **and** `check_update` — every check either story makes — and F13's guarantee held only as long as every caller remembered; reproduced against the shipped modules before the fix. `record_experiment` is the one way in and `check_state` refuses a log holding anything else. It also enforces the size limit, which had the same shape of gap: S-6.3 proved the bound for 40 references and S-5.4 caps investigation at 40, but the two live in different modules and nothing joined them. (3) *Nothing produced coverage*, so S-6.4's correct *unrecorded is not untouched* left every workload invalidated forever — but a reference carries the experiment key and the key names the workload, so `coverage_from_state` reads back what the log already holds. **It is workload-granular, not file-granular, and says so** (file coverage is S-3.9's stacks). **Sabotage-verified on twelve properties, all caught — after four survived the first pass.** Three were **weak tests rather than weak code**: the size-limit test built its oversized state from raw measurements so it failed the *reference* check first and removing the size check broke nothing; the shipped-finding test left the patched finding out of the pending list so removing the filter changed nothing; and there was no test for an out-of-range index at all. **Sixth time a passing sabotage has meant a weak test.** The fourth survivor was **an artefact of the harness**: the runner passed `-m "not slow"`, deselecting the Postgres tests, and the assertion that `learn` writes to failure memory lives in one — re-run without the filter it was caught. A sabotage harness that excludes tests silently reports properties as unverified; the Epic 5 run was invalidated by a different fault in the same script, and both were visible only because the baseline is re-checked *after* the pass.

---

# EPIC 7 — Explorer agent

**Goal:** turn an unknown repository into a runnable, scalable, resettable workload.

**This is the riskiest component. Build it alone and prove it before anything downstream.**

### S-7.1 — Framework fingerprinting
Depends: S-0.1
AC:
- Detects framework, version, ORM, database, test runner from manifest files and imports
- Returns a structured fingerprint used to key playbooks
- Unknown frameworks produce an honest "unsupported" result

**DONE (2026-08-11)** — `src/coldfix/explorer/fingerprint.py`, ADR 070, 23 tests in `tests/explorer/test_fingerprint.py`. All three AC met, and **nothing here calls a model** — reading `pyproject.toml` and looking for `manage.py` is a function. **The version is reported as *declared*, because that is all a manifest holds**: `django>=5.0` is a **constraint**, not a version — what is importable could be 5.0 or 5.2, and on a project with a lockfile it is whatever the lock pinned rather than either — so recording it as *the version* would put a number in the fingerprint that nothing measured, which is the shape of claim the first non-negotiable exists to prevent. The field is `declared_version`, `describe()` says so in words, and the installed version is left to S-7.2, the story that stands the environment up and can ask it. **Identified-and-unsupported is a different answer from unknown**: *this is Flask, which is not supported yet* sends somebody to the roadmap and *nothing here looks like a web application* sends them to check the directory — so `Unsupported` carries what it **did** identify, and `Framework` names frameworks this system cannot ground (naming is not supporting). The two outcomes are **exclusive by construction** — `Fingerprint | Unsupported`, and a `Fingerprint` is only built for a supported framework (S-4.5's rule: a healthy-looking result with an empty field reads as success at every call site). **Every facet carries the file that established it**, which also lets the fingerprint keep strengths of evidence apart rather than flattening them: `psycopg` in the dependencies says Postgres is *possible*, `ENGINE: django.db.backends.postgresql` says it is what runs — the settings file wins, and where only the driver is found the evidence reads **"a driver, not a configured engine"**; `manage.py` beats a dependency list for the same reason, since a project can depend on Django for one management command but only an application has `manage.py`. **A facet nothing establishes is `None`**, enumerated by `undetermined` so the gaps are reportable and S-7.2 knows what to find out. **The playbook key is framework and *major* version** — a playbook learned against Django 5.0 applies to 5.0.3, and keying on the full version would make every patch release a cold start, so S-13.5's learning curve would never bend; a constraint with no determinable floor keys as `unversioned` rather than under a version nobody established. A test asserts **fingerprinting ColdFix's own root refuses it honestly**, since `realtime.py` had the mirror of that trap. **Sabotage-verified on fourteen properties, all caught** — two needed the *sabotage* rewritten rather than the code or the test: defaulting `Detected.evidence` changed no call site because every construction passes one, and the poetry pattern never matched the file. **A sabotage that changes nothing is not evidence of anything**, which is the same failure the harness itself had in the last two composition checks.

### S-7.2 — Environment standup
Depends: S-2.1, S-7.1
AC:
- Starts the database, installs dependencies, runs migrations
- Distinguishes "database not started" from "database started but rejecting connections"
- `logs(service)` and `ps()` tools available to the agent for diagnosing failures

Notes: the two failure states above look identical without log access. The agent needs those tools or it guesses.

**DONE (2026-08-11)** — `src/coldfix/explorer/standup.py`, ADR 071, 18 tests in `tests/explorer/test_standup.py` (2 `postgres`-marked, run against a real container). All three AC met. **The two states are told apart by two probes, not by parsing one error message**: `psycopg` raises `OperationalError` for both, and the text differs by driver version, locale and server release, so matching on it is a guess dressed as a check. The socket is probed on its own *before* the protocol — refused → `NOT_LISTENING` (wait, or check the port); accepts but the protocol fails → `REFUSING` (read the server's message; **waiting will not fix credentials**); both succeed → `READY`. That is a measurement rather than an interpretation, and it is the fork the note says an agent cannot see unaided. **Four states, because there are four repairs**: `NO_CONTAINER` is not a flavour of `NOT_LISTENING` — an exited container and a server still initialising look identical at the socket — so Docker is asked first; and `UNKNOWN` is not a failure, because reporting a verdict when Docker itself is unreachable would send the agent to restart a service it cannot see (S-3.1's rule). A test asserts every state prescribes a **different** action, so the split cannot decay into decoration. **Migrations are not attempted against a database that is not accepting** — the common way standup goes wrong, and it produces exactly the error the note is about — so the database is diagnosed *between* starting it and migrating. The commands are supplied rather than derived: S-7.1 says what the project is, and what stands *this* project up is a fact about its tooling that E14's adapter owns. **Two measurements worth keeping.** The probe timeout is **per resolved address, not per probe**: a host resolving to both `::1` and `127.0.0.1` with a server on only one pays it once before reaching the other — four seconds a diagnosis against `localhost` on Windows, which is most of why this test file dropped from 55s to 19s. And **the production guard reached across an epic boundary and was right to**: pointing a test container's database at `postgres` was refused by S-2.5 because that name matches no test-name pattern, so the container is created with a `coldfix_*` database — a guard that only fired inside its own epic would not be a guard. **Sabotage-verified on twelve properties, all caught — after two survived, both for want of a test**: nothing forced the `UNKNOWN` branch (Docker runs on this machine, and the branch is about what `diagnose` does with *cannot say*), and the stop-at-first-failure test asserted the *verdict* — continuing past a failure leaves both `stopped_at` and `succeeded` unchanged, so it passed either way; it now asserts the later steps were never attempted. **Eighth time a passing sabotage has meant a weak test.**

### S-7.3 — Entry point enumeration
Depends: S-7.2
AC:
- Enumerates HTTP routes, CLI entry points, management commands, background job handlers, integration tests
- Returns candidates ranked by likely usefulness
- Handles frameworks where routes are dynamically registered

**DONE (2026-08-11)** — `src/coldfix/explorer/entrypoints.py`, ADR 072, 43 tests in `tests/explorer/test_entry_points.py`. All three AC met, and **nothing here calls a model** — reading `urls.py` is parsing and asking Django for its route table is a subprocess. **AC 3 decides the design, and it is S-7.1's finding one level down**: a manifest's `django>=5.0` is a constraint and the installed version is a different fact; a `path()` call is a *declaration* and the route table is a different fact. A URLconf is a module, not configuration — a DRF router registers six routes per viewset when it imports, `include()` splices another application's table in under a prefix, a comprehension over a list of models produces a route per model — so a parser reading `path(...)` calls does not miss those by being weak, it misses them by being a parser. There are therefore **two enumerators kept apart by how each found the thing**: `PARSED` establishes that a call appears in a file and nothing about whether it registers or what prefix it lands under; `RESOLVED` runs a program in the **subject's** interpreter that calls `django.setup()` and walks `get_resolver().url_patterns`. **`django` is now a dev dependency for that reason and only that reason** — nothing under `src/` imports it, the program is source text, and the alternative was testing AC 3 against a fake resolver that would assert only what the fake was written to believe (the `langgraph` precedent, and S-0.7b's rule). **The route table is claimed complete only when the framework answered**, since a parse cannot prove completeness of something built by running code and ADR 009's *endpoint* predicate would otherwise be satisfied by a repository whose routes are all registered by a router. **The second half of that was found by a test and corrected the design**: `include("shop.api")` imports eagerly, so one broken URLconf takes the whole table with it and the resolver *answers* — zero routes, one problem — which counted as available-therefore-complete reports a repository as having no endpoints when what happened is that nobody could read them. **What a parse cannot expand is recorded rather than dropped** (`Unexpanded`, with the construct and the reason), which is the honest form of AC 3 before an environment exists; an `include(...)` mount is recorded the same way and **is not emitted as a candidate**, because requesting a prefix returns 404 and scored as a parameterless route it would sit at the very top of the list the Explorer works down. **AC 2's ranking is anchored on S-7.8 and says in words that it is a prior**: there is exactly one downstream gate, so a candidate is useful to the degree it looks able to pass it, and every term is a reason to expect it will or will not — kind 1–4, no path parameter +4 (*it addresses a set, and a set is what grows*), each parameter −2, each segment of depth −1, framework or plumbing −10. Infrastructure routes are **ranked last, never dropped**: AC 1 is enumeration and AC 2 is order. **Three unfamiliar repositories were enumerated and two ranking rules came out of that rather than out of review** — netbox ranked **39** routes level at the top with no depth term (18 with it), and it produced **430 unexpanded registration sites against 86 parsed routes**, so AC 3 is most of that repository rather than a DRF hypothetical. **One incident recorded rather than quietly fixed**: the enumerator was run against S-0.6's holdout during development and `tests/test_holdout_discipline.py` caught it; the depth rule was re-derived from netbox alone, the any-segment infrastructure rule stands on its a priori reason, nothing was added to `ALLOWED`, and the committed tests name only the development targets. **Sabotage-verified on twenty-five properties, all caught — after four survived**: three were **weak tests that passed for a reason other than the one they claimed** (the parameter test compared routes of different depth, so the depth term separated them; the management-command test compared `rebuild_reports` with `migrate`, so the collection-name bonus separated them; and no test reached the branch where the interpreter cannot be *started*, since a settings module that will not import is a command that runs and exits non-zero), and the fourth was a harness pattern that never matched. **Ninth time a passing sabotage has meant a weak test.** Baseline re-run green after the pass.

### S-7.4 — Auth resolution
Depends: S-7.3
AC:
- Detects the auth scheme from settings and failed-request responses
- Creates credentials and attaches them to subsequent requests
- Handles custom user models with non-standard username fields
- Playbook consulted before exploration

**DONE (2026-08-13)** — `src/coldfix/explorer/auth.py`, ADR 073, 44 tests in `tests/explorer/test_auth.py` (2 `postgres`-marked, all `slow`). All four AC met, and **nothing here calls a model** — reading `settings.REST_FRAMEWORK` is a subprocess, base64 is a function, and deciding what a `401` means is a table. **AC 1 names two sources in one line and they are not two ways of learning one fact**, which is S-7.1's finding and S-7.3's a third time: `DEFAULT_AUTHENTICATION_CLASSES` says what DRF will *accept* and `AUTHENTICATION_BACKENDS` says what can verify a password, but a view can be `AllowAny` inside a project whose defaults demand a token, and a project with no auth settings at all can wrap one view in `login_required`. So `Established` records which source settled a requirement, **the observation wins**, and a disagreement is *reported* rather than corrected — the same shape (settings naming `TOKEN`, the route answering `403`) is also exactly what a session-authenticated DRF endpoint looks like from outside. **A followed redirect is never a route that answered.** `login_required` does not challenge, it redirects, and a client with redirects enabled turns that `302` into a `200` holding the login page — nothing in the status, headers or body separates it from the endpoint answering, so the Explorer grounds on a login form and S-7.8 then *correctly* reports that its bytes do not grow. `Reply` carries `answered_path`, which every HTTP client already has (`response.url`, `redirect_chain`), and a `200` from another path is never read as *no authentication required*. **Credentials are minted, not negotiated**: driving the login form needs a CSRF token out of a page, the name of a field this stage has just established is not always `username`, and it stops dead at a second factor — while Django's own test client writes a session row and hands back a cookie, which is what this does in the subject's interpreter. **A login flow is a flow; a session is a row.** **A credential is a fixture, not a setup step** — S-2.6 resets between S-7.8's two scales and takes the user row with it, so a captured credential works for the first half of every sweep and returns `401` for the second, reading as *the endpoint failed at scale*; `Credential` carries the `Recipe` that made it and minting is idempotent by construction. **AC 3 is answered by `get_user_model()`, never by a parser**: `create_user(username=…)` against a model whose `USERNAME_FIELD` is `email` writes an account nothing can log in as, and a model whose manager cannot make users is **refused** rather than worked around, since writing the row directly stores a hash that fails later as a `401` rather than here as an error. **`JWT` is detected and deliberately not minted** — every package signs its own way and none is installed to verify against, and a branch that ships unverified is worth less than a refusal naming what would make it work. `djangorestframework` is now a **dev** dependency on ADR 072's exact terms. **AC 4 is a seam, not a schema**: `playbook_from_store` reads `Collection.PLAYBOOKS` out of S-6.2's journal under S-7.1's key, `resolve_auth` consults it **before the first request**, and entries are carried **unread** — S-13.1 owns what one means and S-13.2 owns the gate that makes trusting one safe. **Every status code in the tests was produced by the framework**, through `django.test.Client` in the subject's own interpreter: real `login_required` redirects, DRF's `401`-with-`WWW-Authenticate` for tokens and its `403`-without for sessions. **That found a defect nothing else could**: the token branch guarded the *import* of `rest_framework.authtoken.models`, but a model whose app is absent from `INSTALLED_APPS` still imports — Django leaves it without a manager and the `AttributeError` lands on the next line — so an ordinary project (DRF present, its optional token app not) produced a traceback instead of the sentence naming the app to add. **A guard on the wrong verb is not a guard.** **Sabotage-verified on thirty-six properties across two passes, all caught — after one survived.** The survivor was neither weak code nor a weak test but **a redundant condition**: `needs_credential and can_be_minted` reads as two guards, and `NONE` is not a mintable scheme precisely because there is nothing to mint for it, so the first term could never decide anything. It is now one condition saying so, with the intent it expressed verified from the other side — `can_be_minted` gaining `NONE` is a sabotage the tests catch. **Tenth time a passing sabotage has meant something other than working code**, and the first time it has meant *the code says a thing twice*. One gap the pass exposed by omission: a `404` fell through to `UNKNOWN`, whose sentence is *something is enforcing authentication* — false about a route that is not there, and not hypothetical, since S-7.3 emits routes with path parameters and requesting one literally returns `404`. `speaks_to_auth` separates them, with a control asserting the four real answers are not reported as inconclusive.

### S-7.5 — Fixture discovery
Depends: S-7.2
AC:
- Locates existing factories or fixtures (factory_boy, pytest fixtures, management commands)
- Uses them in preference to synthesis
- Records the fixture recipe in the workload artifact

**DONE (2026-08-13)** — `src/coldfix/explorer/fixtures.py`, ADR 074, 32 tests in `tests/explorer/test_fixtures.py` (all `slow`). All three AC met, and **nothing here calls a model** — finding a class whose `Meta.model` is `Book` is parsing and counting rows either side of a run is arithmetic. **AC 3 decides the design, and by arithmetic rather than by taste**: S-4.1 already defines the artifact, and `FixtureRecipe` requires an `entity`, a `per_parent` and a `distribution` — *how many rows of what, spread how* — and **a file establishes none of the three**. `BookFactory` says it makes books; it does not say that `create_batch(10)` also wrote ten authors through a `SubFactory`, and `per_parent` is exactly that ratio. So **a located factory is a declared capability and a recipe is what a run wrote** — the epic's running distinction a fourth time, here *forced* by an existing schema rather than chosen, since minting from the parse would put three unmeasured numbers into the artifact that keys S-5.1's replay cache and carries S-3.3's blindness qualification. **AC 2's preference is anchored on S-7.8, and the anchor is whether the size can vary**: kind 1–4, takes a count +4, fixed set of rows −6, names its model +1, contents countable +1. **A `loaddata` fixture file is worth least, and that is the finding rather than a slight** — fixed rows with fixed primary keys, so loading it twice is a collision rather than twice the data, and **a repository whose only fixture is a `.json` file needs S-7.6 despite having a fixture**; `NeedsSynthesis` carries what *was* located, because *has a fixture that cannot be scaled* and *has nothing* send a reader to two different places (S-7.1's rule). A pytest fixture is located because AC 1 names it and **never chosen**: driving one means running the subject's own suite, and S-2.4 refuses to edit a test to change what it seeds. **The distribution is measured by a real `GROUP BY` and refused when it cannot be named** — forty children over ten parents divides evenly and says nothing about whether one parent holds thirty-one, which is the assumption S-3.3's whole argument is *about*; **parents holding nothing are counted too**, since they are absent from a grouping over the child table and leaving them out makes *nine parents with one child and one with none* indistinguishable from *nine with one*. A non-uniform spread **does not become `POWER_LAW` by elimination**: `Distribution` has three values and none means *not uniform*, so `recipe_from` refuses with the measured spread and names S-7.7, the story that makes distribution a parameter of `scale()` rather than a label on found data. A `per_parent` measured as zero is refused rather than clamped to one. **The entity is the model that grew by what was asked for**, not the largest grower — seeding five authors with three books each grows the book model by fifteen, and `scale(n)` means n *of the entity*. `factory-boy` is now a **dev** dependency on ADR 072's terms. **Writing a real factory found a real defect**: the rule for skipping abstract bases was *no `Meta.model`*, and a factory that subclasses another to add a `RelatedFactoryList` declares no `Meta` at all and inherits its parent's — that is the ordinary shape of the factory building the most interesting data, **the only shape where `per_parent` is not 1**, and the rule dropped exactly it. Abstractness is what `Meta.abstract` says. **Sabotage-verified on twenty-nine properties across two passes, all caught — after two survived, and both survivors were the same shape of weak test: a comparison whose subjects differ in more than the one thing under test.** The ranking test put a factory against a fixture file — three terms apart — so zeroing the scalability penalty left the order unchanged; it is now two mechanisms differing only in scalability, with the end-to-end ordering kept as a separate assertion. And **every exercising test started from an empty database, where a total and a difference are the same number**, so nothing established that `grew` subtracts — a repository shipping seeded data is ordinary, and there the total would name the wrong entity and inflate every count; there is now a test that seeds before it measures. **Eleventh and twelfth times a passing sabotage has meant a weak test.** One test was also renamed for claiming more than it exercised: it triggers an unimportable module, not a factory raising.

### S-7.6 — Fixture synthesis
Depends: S-7.5
AC:
- Reads the schema and walks foreign key chains to construct valid rows
- Handles required fields, enums, and unique constraints
- Handles multi-level FK chains discovered on IntegrityError
- Falls back gracefully and reports when synthesis fails

**DONE (2026-08-13)** — `src/coldfix/explorer/synthesis.py`, ADR 075, 33 tests in `tests/explorer/test_synthesis.py` (12 `slow`, 21 in the fast subset). All four AC met, and **nothing here calls a model** — walking foreign keys is a topological sort and picking the first of a field's `choices` is indexing. **AC 3 names an instrument, and it does so because a schema lies about itself in three ordinary ways**: `blank=True` is a *form* concept, so a `blank=True, null=False` column is required by every database and optional in every admin form (the commonest way a plan built from the models alone fails on its first row); a `UniqueConstraint` is **invisible on the fields it covers**; and an unapplied migration leaves the database stricter than the models describing it. So **the ORM declares and the database decides** — the epic's distinction a fifth time, with the roles unusually sharp — and the loop is plan, attempt, read the refusal, revise. **The planner is a pure function over a schema mapping and only the executor runs in the subject's interpreter**, which puts ordering, counts, value choice and revision where they are typed and unit-tested and leaves a thin program that creates rows and reports what was refused; 21 of the 33 tests need no subprocess. **An error message is not a diagnosis** (S-7.2's rule, second occurrence): integrity error text varies by driver, server version and locale, so `psycopg`'s structured `column_name`/`constraint_name` are read *first* and patterns over the message are the SQLite fallback — and **`Learned` records which of the two settled it**, because a revision built on a regex over someone's translated error is worth less than one built on the server's own field. A refusal naming nothing — a check constraint, a trigger — is **reported, never revised**, since the same plan submitted again is not made satisfiable by being submitted again. **The whole plan is one transaction, and that was found by a real run rather than by review**: with one transaction per step, the first attempt created its publishers and authors, failed on a unique book code, and the revision then failed on **the author e-mail it had itself inserted a second earlier** — a retry loop that writes either rolls back what it wrote or poisons its own next attempt, and rows left behind would inflate S-7.5's `grew` and S-7.8's scaling either way. The exception is caught *outside* the atomic block, because querying inside a broken one raises `TransactionManagementError` and buries the integrity error underneath it. **Refusals come before writes where a refusal is possible**: an unfillable column type is reported with the model, the field and the type (writing `None` fails at the database with a worse message; writing a zero produces rows that are valid and meaningless), and a required FK cycle is reported the same way. A field with `choices` takes one of them, because Django does not enforce choices at the database and a row holding `coldfix-0` in a status column inserts cleanly and then breaks the application that reads it. **Synthesized data is uniform by construction and says so** — `07-use-cases.md` §5 is blunt that *if every generated customer has three orders, an N+1 that only hurts customers with three thousand stays invisible* — so the recipe says `UNIFORM` because that is what was **built**, and `Synthesis.blindness` states the consequence in words so it travels with the fixture. **That is the mirror of S-7.5 and the difference is the point**: there the distribution is *measured* and a non-uniform spread is refused; here it is *chosen*, and until S-7.7 the only choice is the blindest one. **Sabotage-verified on twenty-nine properties across two passes, all caught — after one survived**, and the survivor was **an unreachable branch** (the third known cause, not a weak test): nothing made the subject's program fail to answer, so replacing that refusal with an empty payload changed no outcome. It now has a test that breaks the settings module, because *this project has no models* and *this project would not load* are two answers and flattening them turns a broken subject into what reads as a bad argument.

### S-7.7 — Skewed fixture generation
Depends: S-7.6, S-3.3
AC:
- Can generate power-law and long-tail distributions across relationships
- Distribution is a parameter of `scale()`
- Recorded in every measurement taken with those fixtures

**DONE (2026-08-14)** — `src/coldfix/explorer/synthesis.py` (wiring) and `src/coldfix/screening/workload.py` (the widened artifact), ADR 076, 19 tests in `tests/explorer/test_skewed_fixtures.py` plus one added to S-7.5's file. **S-3.3 already generates the three shapes and nothing here re-implements it** — this story is the wiring and the bookkeeping, and **the bookkeeping is where the decision was**. `FixtureRecipe.per_parent` is one `int`, a skewed fixture has no single children-per-parent, and that field feeds `digest()`, which keys S-5.1's replay cache. **`per_parent` is now the heaviest parent and `parents` is a new optional field** — agreed with the user before implementation. The two alternatives were worse: **the mean is the one reading that is never the interesting one** (the whole reason to build a long tail is the request that takes minutes while every other request stays fast, and that request is made by the heaviest parent, so recording the mean would name the shape and then describe it with the number the shape exists to avoid); and the nominal divisor keeps the field constant while making it mean two different things by distribution, which stops S-4.1's own *the number `Σk²` is about* from being true for two of the three shapes. With `entity`, `per_parent` and `distribution`, `parents` makes the fixture **reproducible**, since `allocate` is deterministic — and `None` means *not recorded*, **never one parent**. **The shape applies to the target's own parents, not to the whole chain**: applied at every level of `Publisher → Author → Book` it compounds into one nobody asked for, and the cost S-3.3 is about is paid where the request walks rather than three joins up. **The per-row parent assignment is computed in the planner**, so the subject indexes a list and decides nothing — the arithmetic stays S-3.3's, and the shape lands in the plan and therefore in the replay key rather than in a decision made at write time. **A shape that will not fit is refused, never flattened**: `allocate` guarantees every parent at least one child, so twenty parents and twenty children leave nothing to skew with, and building it anyway would write `LONG_TAIL` into the one field that exists to stop a fixture being described as a shape it does not have; the refusal names which knob to turn. **The blindness note inverts under a skew** — a flat result under a long tail is a *much stronger* exclusion than a flat result under uniform data, because the cost was given somewhere to hide and did not hide there — and keeping one sentence for both would make it boilerplate. **`07-use-cases.md` §5's admission stops being true of this tool.** **Sabotage-verified on fourteen properties, all caught, no survivors** — but **two patterns did not apply on the first run**, which is ADR 072's *a sabotage that changes nothing is not evidence of anything*, and rewriting them surfaced a genuine gap: the parent count recorded by S-7.5's discovered recipe had **no test at all**, because the pass ran only the two files this story touched most. **A sabotage pass scoped to the files a story edits will miss what the story changed elsewhere.** One unrelated flake was seen and confirmed as one: `test_perturbation.py::test_in_serial_code_the_sensitivity_is_just_the_share_of_runtime` failed once in a full fast-subset run, passed alone, passed with this story's changes stashed, and passed on a second full run — S-3.14 measures timing sensitivity and CPU contention distorts it, the same class as the recorded `test_isolation.py` flake.

### S-7.8 — Objective work verification — **SAFETY**
Depends: S-7.6
Why: the agent is incentivized to claim success because success completes its task.
AC:
- `work_verified` is computed by the harness, not the agent
- Requires all three: query count, response bytes, and wall time each rise between N=10 and N=100 by stated thresholds
- The agent cannot override or supply this value
- A workload failing verification is rejected regardless of what the agent claims

**DONE (2026-08-14)** — `src/coldfix/explorer/work.py`, ADR 077, 31 tests in `tests/explorer/test_work_verification.py`. All four AC met. **Half of this already existed and is not re-implemented**: S-4.1 put `work_verified` on the artifact as a property with no field behind it, and what was missing was the half that produces the numbers it reads. **The signature is the enforcement.** `verify_work` takes a path, an interpreter, a seeding plan and a credential — **there is no argument through which a query count, a byte count or a duration could arrive** — and `accept` takes exactly one parameter, the harness's own measurements. A gate that accepted a claim and ignored it would be one refactor away from honouring it, so there is no `claimed`, no `override` and no `force`; **a test asserts the signature by inspection and fails the moment somebody adds one to make a demo pass.** Fifth instance of the recurring construction: make the unsafe state have no object to exist in. **The three metrics are measured in the subject's interpreter** — wall time and bytes could be read from outside, but **the query count cannot**, since nothing outside the process knows how many statements a request issued — so one program drives the request under Django's own `CaptureQueriesContext` and reports all three for the same invocations. **A warm-up precedes the measured requests** (the first request through a Django stack pays imports, template compilation and connection setup, and charging those to the small scale point is how a flat workload comes to look like a growing one) and **the median of five is taken, not one reading**, because the wall-time condition is a ratio that a single scheduling slice flips. **The sweep resets between scale points**: measuring N=100 on top of N=10 makes it a measurement of a hundred and ten, and the growth is then arithmetic rather than a property of the workload. **An error response is refused rather than measured** — an error page is cheap, constant and identical at every scale, exactly the profile the check exists to reject. **F6's first condition is ADR 051's corrected one** (*queries did not fall*); this story's AC still carries the audit's original *rise*, which rejects every correctly batched endpoint, and the correction predates the criterion. **The guarantee is the schema, not the type checker**: `Workload(..., work_verified=True)` produces **no mypy error at all**, because pydantic's plugin does not model `extra="forbid"` as a signature — it type-checks and fails only at runtime, which is exactly why `CLAUDE.md` puts enforcement in code rather than convention. **The aggregate false negative is kept and named**, with a `/count/` endpoint in the fixture so the refusal is exercised rather than assumed. **Sabotage-verified on twenty-eight properties across three passes, all caught — after two survived and three patterns never applied.** **All six attacks on the safety properties were caught on the first pass** (removing the gate's check, adding `force`, reading the verdict off the wrapper, un-forbidding unknown fields, un-freezing the artifact, making `work_verified` unconditional). The two genuine survivors were **unreachable or unobserved rather than wrong**: the empty-samples guard could not be reached because `repeats` was **silently clamped to one** (clamping answers a question nobody asked — it is now a refusal, and the malformed-answer branch is forced with `monkeypatch` at the subprocess boundary), and **removing the warm-up changed only noise**, so the warm-up is now *timed and reported* — a field that disappears when it does. One weak assertion of S-7.3's recorded shape: `min <= seconds <= max` is satisfied by **every** sample, so a sabotage returning `samples[0]` walked through the test meant to establish the median; three disagreeing samples are now fed in at the subprocess boundary, where what is under test is the arithmetic rather than the answer. **A residual risk is stated rather than solved**: the agent chooses *when* to call this, and wall time is noisy near its threshold, so repeated attempts on a borderline workload will eventually pass one — the median narrows the window and S-7.10's cap bounds the attempts, but nothing here makes a borderline case deterministic.

### S-7.9 — Workload artifact emission
Depends: S-7.8, S-4.1
AC:
- Emits a validated workload object
- `evidence_of_work` is mandatory and harness-computed
- Reset method verified by S-2.7 before emission

**DONE (2026-08-14)** — `src/coldfix/explorer/emission.py`, ADR 078, 19 tests in `tests/explorer/test_emission.py` (18 in the fast subset; the envelope is an artifact and most rules about it need no subprocess). All three AC met. **`03-agents.md` §2.4 gives the reason** — *`evidence_of_work` is mandatory and exists to make "it ran but did nothing" structurally unreportable as success* — and this is where *structurally* has to become true of a **document** rather than of an object in memory, which is a different problem. **S-4.1's solution does not survive serialization**: `work_verified` is a property with no field behind it, which is exactly what stops an agent writing it into a `Workload` — and a property is **not serialized**, so a document that must carry the evidence has to carry a **copy**, and a copy is the one thing an agent could edit. **So the copy is checked against the recomputation.** `EmittedWorkload` holds the verdict and the evidence as ordinary required fields, and validating one **recomputes both from the observations it carries** and refuses any disagreement — tampering does not produce a more convincing workload, it produces a document that will not load. **Mandatory and powerless is the pair AC 2 asks for**, and neither half alone gives it: mandatory without the check is a field an agent fills in, and the check without the field leaves *unverified* indistinguishable from *not yet asked*. Three things are compared, and the third is not obvious — the verdict, the evidence string, and the envelope's `reset_strategy` against the workload's `reset_method`; **the evidence string names the scales and the ratio**, which is what makes the subtler edit (leave the verdict alone, improve an observation) fail too. **The reset method is proved by a type rather than asserted by a name**: `emit` requires S-2.7's `VerifiedReset`, whose constructor refuses a failing report, so there is no way to emit holding only the name and **no unverified proof exists to offer**. A proof of a *different* strategy is refused — verification is a property of a strategy **on a project**, not of a strategy (S-0.5 had rollback alone pass its own check and fail 10/10 on sequences) — and the cycle count travels in the document so a reader knows whether the guarantee behind it was established over three cycles or ten. Emission goes through S-7.8's `accept`, so an unverified workload is refused at the boundary rather than emitted with `work_verified: false` for somebody downstream to notice. **One consequence worth stating: the evidence string is now part of a validation contract**, so changing the wording of `work_evidence` invalidates every previously emitted document — the correct direction, but it makes the string an interface rather than a message, and S-17.2 will have to version it to publish old emissions. **Sabotage-verified on sixteen properties across two passes, all caught — after one survived**, and the survivor was **S-7.4's shape again**: `emit` records `reset.strategy` where the guard immediately above has just established it equals `workload.reset_method`, so swapping them changes no outcome and no test can tell them apart. The pair is separable only by attacking **both at once**, and that combined sabotage *is* caught — removing the guard and recording the claimed strategy accepts a wrong proof. **Second time a passing sabotage has meant the code states a thing twice**, and the generalisation is that **a guard which forces two expressions equal makes every later choice between them untestable**: find the pair, and attack the guard and the choice together.

### S-7.10 — Caps and honest failure
Depends: S-5.4, S-7.11
AC:
- 60-step cap enforced
- Progress check escalates after 15 steps with no new information
- Per-stage attempt budget enforced on top of the global cap (S-7.11 supplies the stages)
- On failure, reports **which stage never completed** and what was attempted there
- **Never reports success when no workload does real work**

Notes: "reports what was attempted" is a transcript; "stage 4 never completed, here is its predicate and the last error" is something a user can act on and S-17.2 can publish. The per-stage budget is the tighter instrument — S-0.3's grounding runs took 5–19 minutes, and detecting at stage 2 that a repo will not ground saves the other seven stages.

**DONE (2026-08-14)** — `src/coldfix/explorer/run.py`, ADR 080, 22 tests in `tests/explorer/test_run.py`. All five AC met, and **most of the machinery is S-5.4's, deliberately**: it already compiles `GROUND` at sixty steps with a disposition of `ABORT`, already authorizes *before* the work rather than after, and already has a stall check. Reimplementing any of it would be a second set of caps to keep in step with the first. **What S-5.4 could not supply is the two things this story is about.** **S-7.11 gives "no new information" something to mean**: S-5.4 compares a digest of what a step *concluded*, and left to grounding that digest would have to be the agent's own account of its progress — the self-judged criterion F6 exists to remove — so the digest is **the nine-verdict stage report**, and fifteen steps that move nothing have taught the run nothing whoever believes otherwise. **The digest is the verdicts and not the details**, because a detail string carries row counts and error text that drift between otherwise identical steps (S-5.2's argument about durations, one layer up). **Grounding stalls at fifteen, not S-5.4's three**, and a budget built with the wrong value is **refused rather than corrected** — silently substituting the right one would hide that the caller asked for something else, and escalating after three unchanged reports would abandon a repository mid-install. **The per-stage budget is eight, chosen against S-0.3** (the worst single stage took six attempts) — and **with the defaults it dominates the stall check**: a run hammering one stage is stopped at eight, five steps before fifteen, which is exactly the note's point and is now asserted by a test rather than left as an inference. What the global stall catches is the *other* shape: an agent moving **between** stages, spending steps, changing nothing anywhere. **A failure leads with the incomplete stage**, states what done would have meant in ADR 009's words, gives the last measurement, and lists **only the attempts made at that stage** — the other fifty are noise to somebody fixing this one. `give_up` **returns** rather than raises, because an agent choosing to stop is not an error and the null-result rule makes an honest *this will not ground* a legitimate output. **AC 5 is structural: `finish` is the only method that returns an `EmittedWorkload`**, it calls S-7.9's `emit` → S-7.8's `accept`, and a test asserts by inspection that no other method returns one. **Sabotage-verified on sixteen properties across two passes, all caught — after one survived**, and the survivor was **the recurring shape a fourth time**: every test attempted the stage that was *also* the incomplete one, so *last attempted* and *first incomplete* coincided and a report built on either read identically. **The generalisation, now stated in four stories: before asserting a value comes from rule A rather than rule B, check the fixture makes A and B disagree.**

### S-7.11 — Stage predicates — **SAFETY**
Depends: S-7.1
Why: without a definition of done per stage, an agent stuck at stage 4 and an agent progressing normally are indistinguishable until the global cap fires.
AC:
- Grounding is modelled as the nine stages in ADR 009, each with a predicate
- **Every predicate is computed by the harness; the agent cannot supply or override one** — the S-7.8 rule, extended to all stages
- Predicates are framework-scoped and resolved through the S-7.1 fingerprint
- A stage whose predicate is already true is complete without action (repos shipping a seeded database skip seeding)
- A test proves an agent claiming a stage complete cannot advance while that stage's predicate is false

**DONE (2026-08-14)** — `src/coldfix/explorer/stages.py`, ADR 079, 41 tests in `tests/explorer/test_stages.py`. All five AC met. **ADR 009 had already made the decision**; this story is the code plus three choices the ADR did not have to make. **An agent may claim, and a claim settles nothing.** `claim(stage, …)` exists and is *meant* to be called — an agent reporting *I think I have configured it* is the agent doing its job, and refusing to hear it only pushes the claim into prose nobody checks — but what comes back is **that stage's predicate measured now**, and a claim about a stage that does not hold is refused with what was measured and what done would have meant. S-7.8's construction generalised: there the gate had no parameter a claim could occupy; here the claim is welcome and powerless. **Three verdicts, because three next moves**: a predicate that could not be evaluated is not one that failed, and *migrations are unapplied* vs *nothing could be asked because the database is not answering* send a reader to two different places (S-7.2's flattened-ignorance mistake in a new costume). **`UNKNOWN` does not complete a stage** — treating ignorance as progress is how a run reaches the final gate having skipped everything — so `first_incomplete` reports the first stage that does not *hold* rather than the first that *fails*. **The seeding predicate excludes the framework's own tables, and that is the story's real finding**: ADR 009's *row counts exceed a threshold in at least two tables*, implemented literally, **is satisfied by a freshly migrated and entirely empty repository**, because `migrate` populates `django_content_type` and `auth_permission` by itself — and the Explorer would go on to measure nothing. Only the application's own tables count; the framework's rows are **reported separately** (they are real rows a reader should see, and what they are not is data). The threshold is ten, S-7.8's small scale point — a database is seeded when it holds enough to be *measured* — and two tables rather than one because the costs this system looks for live in relationships. **The predicates delegate rather than re-deriving**: `configure` runs the framework's own `check` (Django knows what a misconfigured Django looks like and this module does not), and `auth`/`work` read S-7.4's and S-7.8's results rather than recomputing them. The three questions only the framework can answer are asked in **one** subprocess, so a nine-stage report costs one `django.setup()`. **Sabotage-verified on twenty-six properties across two passes, all caught — after three survived.** **All five attacks on the safety properties fell on the first pass.** **All three survivors were the same gap: no test reached the negative case.** `connect`, `endpoint` and `auth` were each only exercised where they hold — SQLite opens anything it can create, the fixture always had a route, and the auth resolution was only ever `None` — so each now has a subject that fails it, plus the control that an *open* route satisfies the stage rather than failing it. **The generalisation: a predicate tested only where it holds is a predicate tested as a constant**, and the fixture that makes the positive case easy is usually why the negative case is missing. One test also failed on first run for a reason worth recording: `assert "0 row(s) exist…" not in detail` **passes on `"30 row(s)"`**, because a negative assertion over formatted text is a substring check and numbers make substrings of each other; it now matches the number with a regex.

Notes: S-0.3 produced sixteen distinct obstacles across three repositories, no two identical, and **every one fell into one of nine stages with no obstacle needing a tenth**. Specifics never repeated; the taxonomy stayed closed. That asymmetry is what makes this tractable — an unfamiliar obstacle inside a known stage is a bounded search against a stated success condition, which is the shape an agent handles. This is the strongest lever found in E0 for the unknown-unknowns problem, and it is what lets S-13.1 key playbook entries to a stage rather than to a whole run.

### S-7.12 — Date-anchored environments
Depends: S-7.2
Why: a repository last touched in 2019 does not break because it is complex; it breaks because we hand it a 2026 toolchain.
AC:
- An anchor date is derived from the repository's most recent commit
- Dependency resolution is constrained to packages published on or before the anchor (`uv --exclude-newer`)
- The interpreter version is read from the repo's own declarations — `python_requires`, classifiers, `tox.ini`, CI matrices — and fetched to match
- The anchor, the resolved dependency set, and any override are all recorded in the workload artifact
- A test grounds a repository whose unpinned dependencies resolve incorrectly at HEAD and correctly at its anchor

**DONE (2026-08-14)** — `src/coldfix/explorer/anchor.py` plus `EnvironmentAnchor` on S-4.1's `Workload`, ADR 081, 27 tests in `tests/explorer/test_anchor.py` (4 marked `index`). All five AC met, and **the premise is measured rather than asserted**: `django>=2.0` resolves to **6.1** today and to **2.1.4** as of a 2019 anchor. **ADR 010 had decided the design**; three questions it did not have to answer came up in the code. **An enumeration is a claim and a floor is a minimum**, and ADR 010 lists the four sources as though they were one kind of evidence. A classifier, a `tox` envlist and a CI matrix each *enumerate* versions the project says it supports; `requires-python = ">=3.8"` says 3.8 works and **nothing whatever about 3.11**. Taking the floor as *the* version hands 3.8 to a project whose CI tests 3.11, so `Basis` records which was found and an enumeration beats a floor. **Versions are compared numerically** — `"3.9" > "3.10"` is true for strings, so a lexical maximum hands a project testing 3.9 through 3.12 the oldest of them. **The committer date, not the author date**: a patch written in 2015 and applied in 2019 was *resolved against* 2019's index by whoever applied it. **An override carries no commit and no separate flag** (two fields that could disagree about whether an override happened eventually would), and **an override with no reason is refused** — ADR 010 requires the override because a contemporary dependency may carry a since-fixed incompatibility or a known vulnerability, and *why this run differs* is the whole value of recording it. **A checkout with no history is refused, not dated today.** **AC 4's "workload artifact" is `Workload`, not Epic 7's envelope**, and reproducibility decides it: ADR 010's *anchoring makes the dependency set a recorded input rather than a function of when the tool happened to run* means the inputs must travel with the thing a later process reasons about; S-0.4's byte-identical guard counters are void if a rerun silently resolves a different Django. `EnvironmentAnchor` is optional and **`None` means not recorded, never resolved-against-today**. **AC 5 needs a real package index**, so those tests carry a new `index` marker — distinct from `slow` and `docker` for the same reason those are distinct from each other. **The bound is carried in the code**: `Anchor.residue` says in words that this covers the Python layer only, and a test asserts the sentence travels, because *the environment is era-matched* is exactly the claim somebody would quote without it. **Sabotage-verified on nineteen properties across two passes, all caught — after two survived and one pattern never applied**, and **both survivors were fixtures that could not discriminate**: the committer-vs-author test set **both** git dates to the same value, and the CI decoys were `v4.2` and `timeout-minutes: 4.5` — **which the version filter discards for an entirely different reason**, so removing the key check changed nothing and the test passed against exactly the code it was written to reject. **The sharper generalisation: a negative test needs a decoy the code would otherwise accept; a decoy rejected by some other rule proves only that the other rule works.**

Notes: see ADR 010. S-0.3's sample was drawn entirely from repositories committed to within three days of selection, which is why the *Python version mismatch* and *dependency resolution failure* rows of its recurrence matrix came back empty — the spike could not have detected this class. **Anchoring covers the Python layer only**; an old `psycopg2` needing a `libpq` current Debian no longer ships is an OS-level problem with no `--exclude-newer` equivalent, and that residue belongs in S-17.2 rather than being described as solved. The anchor must be overridable, since a contemporary dependency version may carry a since-fixed incompatibility or a known vulnerability.

---

**EPIC 7 COMPOSITION CHECK (2026-08-14)** — `tests/explorer/test_explorer_composed.py`, ADR 082, 9 tests. The epic finished with twelve stories, **359 passing tests**, a sabotage pass on every one — and **no way to run one repository through them**: performing its own sentence takes nine modules in the right order and **no test file in the epic touched more than three**. **Six defects, and every one was a join** — not one was a module wrong about its own subject. (1) *A ranked route could not be requested as it came out*: the resolver reports `books/`, every downstream story takes a *path*, so the composed run requested `books/` and got a 404 — (2) *which `resolve_auth` then read as an authentication requirement*, stopping the run with **this route needs a credential nobody made**. The diagnosis was perfect and the input was wrong: **two correct modules producing a confident, wrong answer between them**, which is the shape composition checks exist for. `Candidate.request_path` now exists and is `None` for a *parsed* route (a parse cannot establish what prefix an `include()` mounted a fragment under), and `Enumeration.drivable` filters the ranked list, because it mixes both and leaving every caller to filter is how one of them does not. (3) *The environment could not reach the artifact*: S-7.12 computes an anchor, S-4.1 gained a field to hold one, and **nothing could get from the first to the second** — `verify_work` is the only code that builds a `Workload` and took no environment, so S-7.12's AC 4 was satisfied by a schema and unreachable in practice. (4) *Seeding always synthesized*, so S-7.5's *use existing fixtures in preference to synthesis* was honoured inside its own module and **nowhere else**, and a repository shipping a perfectly good `BookFactory` was measured against invented rows; `verify_work` now takes a `Seeder` defaulting to synthesis. (5) *`target` was required even when a seeder made it meaningless.* (6) **The sharpest: `prefer()` chose the wrong factory.** The subject ships `AuthorFactory` and `BookFactory`; S-7.5 ranks by how well a mechanism seeds *two scales* — a property of the mechanism, not of the workload — so they tied and **the alphabetical tie-break chose `Author`**. The run seeded a hundred authors, drove `/books/` and measured an empty list: **one query, thirteen bytes.** Every S-7.5 test passed throughout, because **none of them had a second factory to choose between**. `prefer(discovery, entity=…)` now prefers a mechanism that builds the entity the workload needs, and nothing infers which entity a route serves — that is the Explorer's to know. **Three of the six are one shape: a value one story produces and another consumes, where nothing in either story's tests holds both ends** — a module's own tests always supply their own inputs, so they cannot see this class at all. **Two of the six were AC satisfied in isolation and unreachable in practice**, which is worth remembering when reading a `DONE` note: *the criterion is met* and *the criterion is reachable from the rest of the system* are different claims. **Sabotage-verified on ten properties, all caught — after two survived**, both fixtures that could not discriminate (the fifth consecutive story to record that shape): removing the resolved-only condition changed nothing because every route in the subject was root-mounted, so a parsed fragment was a valid address **by luck** — the subject now mounts one under `include()`, where the fragment is `orders/` and the address is `/api/orders/`. **Finding a defect and being able to keep it fixed are two different pieces of work.**

---

# EPIC 8 — Diagnostician agent

**Goal:** determine the cause by experiment, and switch instruments when a hypothesis fails.

### S-8.1 — Hypothesis generation
Depends: S-3.1, S-5.5
AC:
- Separate call at temperature 0.8
- Receives the experiment log, exclusions, source under suspicion, and applicable instruments
- Returns a structured hypothesis plus the primitive that would test it
- Routed to the frontier tier; no cascading

**DONE (2026-08-15)** — `src/coldfix/diagnosis/hypothesis.py`, ADR 084, 17 tests in `tests/diagnosis/test_hypothesis.py`, plus two added to S-0.7b's suite. **The first call to a model in this system** — everything before it is deterministic, which is the shape `00-BRIEF.md` §1 argues for. All four AC met, and **two of them were already enforced elsewhere**: `CLAUDE.md`'s *never cascade on hypothesis generation* is S-5.5 (which derives *creative* from §3's table rather than from a caller's declaration) plus S-5.6 (which cascades only a step whose caller supplies a validator), so AC 4 needed **no new mechanism** — it needed this module to use `StepType.HYPOTHESIS_GENERATION` and to have **nowhere to pass a validator**. `generate` has no `validate` parameter and a test asserts it by inspection; third instance of the construction after S-7.8's missing `force` and S-7.10's single exit. **AC 1 required a change to S-0.7b: temperature joins the request digest.** The client had no temperature at all, and adding the parameter is obvious while **adding it to the digest is the decision** — S-0.7b's own argument for including the model applies one axis over, because `03-agents.md` §2.4 sends the Diagnostician's two calls at 0.8 and 0.0 and **those are frequently the same question about the same log**, so without it the recording made for the call that *must not vary* would answer the call that is supposed to, and nothing would fail. Required rather than defaulted, on S-5.4's `max_output_tokens` argument. **The instrument the model names is checked against the instruments it was offered** — a hypothesis proposing a primitive S-3.1 withheld is a step that fails in S-8.2 *with the reason already lost* — and the control is asserted too, since a validator refusing everything would pass the negative test and leave the agent unable to propose anything. **Nothing repairs a malformed answer**: *the model answered something else* and *the model was wrong* need different fixes, and a parser that guesses turns the first into the second. A refusal is checked before the text is read (S-0.7b: a decline is a successful response with an **empty content list**), and a truncated reply is refused because half a JSON object parses as nothing. **Sabotage-verified on fourteen properties, all caught, no survivors** — including all three non-negotiables: the call made at the interpretation temperature, the step declared mechanical so it may cascade, and the temperature removed from the request's identity.

### S-8.2 — Experiment design
Depends: S-8.1
AC:
- Translates a hypothesis into a concrete experiment specification
- Specification is validated against the chosen primitive's schema
- Mechanical step — routed to the mid tier with cascade

**DONE (2026-08-15)** — `src/coldfix/diagnosis/design.py` and `schema.py`, ADR 085, 49 tests in `tests/diagnosis/test_design.py`. All three AC met, and **the mirror of S-8.1 in every respect that matters**: that story is creative, frontier-only and structurally unable to request a cascade because nothing can check it; this one is mechanical, mid-tier and cascaded because AC 2 hands it the check. **AC 3 required a ninth row in `04-cost.md` §3, and there was no way to avoid it**: `Step.step_class` is *derived* from that table (S-5.5, so a call site cannot misdeclare) and `cascade()` refuses any step type whose check is `None` (S-5.6), so a step with no row has no derivable class and cannot be routed or cascaded at all. Borrowing the closest existing row would let a row about *one* primitive decide the tier for a step serving all thirteen. The check is what AC 2 states, and it is the same **kind** of check as the evidence chain's already-present row — a schema over an artifact, no judgement in it — which is the argument for the cascade-safe side rather than a convenience. §3 and `STEP_KINDS` were updated together, and **the non-negotiable is re-asserted from the story that touched the table**, since editing §3's rows is precisely how the two *none exists* entries would stop being *none exists* without anybody deciding to. **The story's substance is what a primitive's schema turns out to be.** `scale_volume` takes nine parameters and a model can answer three — `scales`, `distribution`, `counters`; the other six are the grounded workload — so **a specification is not a call, it is the answerable half of one**, and the partition falls out of annotations that were already written: a parameter whose type a JSON document can express is the model's, everything else is the harness's. **Nothing had to be added to any primitive**, which is the test of whether a partition is real, and it holds across all thirteen instruments by sweep rather than by assertion on one. Read from the callable, which is S-3.1's `Primitive.signature` argument one layer on and worse here because the reader is not a human. **A mapping of numbers is never the design's to set**: the registry's one `Mapping[str, float]` is `bounds.headroom(metrics=…)`, the shape of a *measurement*, and admitting it would defeat *agents do not report measurements* through the front door. **The schema checks shape, not sense, and the bound is stated in code** — `scales=[-4]` is a well-typed `Sequence[int]` and a meaningless sweep, refused by `scale_volume` on its own rules — so *validated against the schema* means the primitive will accept the call and **never** that the experiment will produce a measurement. **The second finding: a retry has to be able to differ from the attempt it retries.** §3's *2 cheap attempts, then strong* silently assumes the second attempt is a second answer, and at temperature 0 it is the same call — same model, same prompt, same sampling — which against S-0.7b's client is literally the same digest, replaying the recording already rejected. So the rejection is fed back into the question and the temperature stays 0.0: **a retry told what was wrong is a correction, a retry at a higher temperature is a dice roll.** The rejections go **last**, because §4 puts the varying part last and this is the one step designed to make three calls. That line also decides what is retryable: a malformed reply or a schema failure is a *wrong answer* and cascades; a refusal or a truncation is an *absent* one and raises. `UndesignableError` carries every attempt's rejection, because `NoDearerTierError` reports the step type and the model and **not what was wrong**, which here is the whole diagnosis. **There is no `validate` parameter, which is S-8.1's absence inverted** — that story's danger was a caller requesting a forbidden cascade, this one's is a caller supplying a check that returns `True` and makes the cascade decorative. Fourth instance of the construction. **Sabotage-verified on twenty-seven properties, all caught — after two survived, and one was a defect in this story's own claim.** The mapping exclusion survived because the decoy could not discriminate: adding `Mapping` to the specifiable origins changed nothing, since a mapping has two type arguments and the *element* check rejects it for that instead — sixth consecutive story to record a fixture that could not discriminate, and its sharpest form yet, **a rule that can only be broken by two simultaneous edits cannot be tested by one**. And **`strict=True` was guarding nothing while the docstring said it was**: turning it off changed no behaviour, because `JSONValue` covers every type JSON has and there is nothing for a coercion to reach for — what keeps a boolean a boolean is `bool` being *in* the union, and that is the sabotage that fails. The claim was corrected rather than the setting removed, because a guarantee resting on something that is not providing it is worse than no claim: it is the one nobody re-checks.

### S-8.3 — Result interpretation
Depends: S-8.2
AC:
- Separate call at temperature 0.0
- Returns verdict: confirmed / narrowed / rejected, with the measurement attached
- A test asserts identical inputs produce identical verdicts across repeated calls

Notes: the split temperature is a real design decision. Hypothesis generation benefits from diversity; interpretation must not vary.

**DONE (2026-08-16)** — `src/coldfix/diagnosis/interpretation.py` plus `replies.py`, ADR 086, 39 tests in `tests/diagnosis/test_interpretation.py` and 9 in `test_replies.py`. All three AC met. **This needed a tenth row in `04-cost.md` §3, and unlike S-8.2's it is a documented omission rather than an argument from the loop's shape**: §2 — the section whose whole thesis is *route by step, not by agent* — lists **"Interpret a growth table | Diagnostician | ~40"** in its *mechanical* table, which makes it the most frequent step the Diagnostician takes and the largest single mechanical workload in the system, and §3 gives it no row. Since the class is derived from §3, the one step §2's argument is most about was the one step §3 could not express, and it would have run creative on the frontier — the exact drift §2 exists to prevent. **Two rows added by one epic is worth stating plainly: §3 is not an enumeration of the loop's steps**; it was written against the agents that existed then, and Epic 8 is the first code to walk the whole investigate loop. A test asserts from *both* stories that the two *none exists* rows are untouched. **The check that makes it cascade-safe is `CLAUDE.md`'s first two non-negotiables made mechanical**: the verdict carries the figures it rests on, every one is checked against the mapping the harness took, and a name that was not measured, a value that does not match, or a verdict citing nothing are all refused — *confirmed, queries grew 40x* against a flat table fails. **Exact, with no tolerance**, because a tolerance would be this module deciding how far a quoted figure may sit from the measured one, which is a judgement and the point is that the check contains none. **The attached measurement is the harness's structurally** — filled from the mapping the function was handed, with a `measurement` key in the reply never read — and **the citation check runs twice deliberately**: in `parse` to give the cascade a correctable sentence, and in a model validator so no other code path can build one without, since *enforced by schema, not by prompt* would be false if it lived only in the one function that calls it today. **The bound is stated: it catches fabrication, not misjudgement**, and S-5.6's `never_escalated()` is what tells those apart in practice. **AC 3's obvious test is worthless and the story's real work is upstream of it.** Against S-0.7b's replaying client, *identical inputs give identical verdicts* is trivially true — one digest, one recording, one verdict — and would pass against code that rendered the measurement in a different order every call. It tests the cache. **Temperature 0 makes an answer stable for a *fixed prompt* and says nothing about a prompt that moves**, so the property is that identical inputs produce an identical **request**: the measurement is rendered in sorted key order (a `Mapping` iterates in insertion order, so the same measurement built two ways would be two prompts) with values through `json.dumps`, and the tests compare a byte-identical question and a **request digest agreeing across a process boundary** (S-8.4's construction — hash-order randomization only has room to move between processes). The end-to-end test asserts the two calls reached the *same recording*, and says in its docstring that the digest is the assertion and the verdict is not. **`replies.py` was extracted as the second case** — the reason to share is the *rejection text*, since those sentences are fed back to the model as corrections and two hand-written versions would drift; **S-8.1 is deliberately not a third caller**, because it raises where the mechanical steps return and ADR 085's line between a wrong answer and an absent one is what that difference is. **Sabotage-verified on twenty-two properties, all caught — after three survived, of three different kinds.** (1) *A weak assertion*: the all-problems-at-once test checked for `"db.query"` and passed against a first-problem-only implementation, because the *unmeasured metric* message lists everything that **was** measured and `db.query` is in that list — an assertion matching a different part of the same output is not an assertion about the part it names. (2) *Two guards no test reached*, both in the newly extracted module, both previously sabotaged only through their callers, and neither reachable by any reply either caller's suite sends — a truncated reply has no closing brace, so the malformed-JSON path had never executed. (3) **One of those guards could not fire at all**: the *that was not an object* rejection shipped in S-8.2 is unreachable, since the pattern takes text from the first `{` to the last `}` and JSON beginning with `{` that parses is an object. That makes it S-7.4's redundant condition — unverifiable by construction, reading as protection while protecting nothing — and S-7.4's recorded remedy is the one applied: collapse it, and verify the intent from the other side with a parametrised sweep, keeping an object nested in an array as the control.

### S-8.4 — Append-only experiment log
Depends: S-6.1, S-5.7
AC:
- Every experiment appended with hypothesis, primitive, design, measurement, verdict
- Never reordered or re-summarized
- Serialization is stable and cache-friendly

**DONE (2026-08-14)** — `src/coldfix/diagnosis/log.py`, ADR 083, 30 tests in `tests/diagnosis/test_experiment_log.py`. **Taken before S-8.1 with Allen's agreement**, because S-8.1's AC says it *receives the experiment log* and this story defines what one is — the project's own precedent of not guessing another story's schema (S-5.4 left the checkpoint to S-6.1, S-6.1 left this log to S-8.4, S-6.2 left playbook columns to S-13.1). All three AC met, and **most of the work was not duplicating what already exists**: S-5.7's `Investigation` has an `append` and no `reorder`, and **S-5.8's `PrunedLog` already holds records, renders summaries, defers detail and guarantees the byte-prefix property**. **Epic 5's composition found *two append-only logs* as a defect**, and recorded why it was invisible — caching is a prefix match, so a log wrong in *content* but still append-only reports full cache hits and a rising bill with nothing failing — so `ExperimentLog` **wraps** `PrunedLog`: one log, one rendering, one way in. What S-8.4 adds is the **record**, since S-5.8 holds the cost view `(primitive, target, outcome, detail)` and AC 1 needs hypothesis, design and measurement too — S-8.6 assembles an evidence chain from these, and a chain built from records missing their measurements is exactly the *conclusion drawn from reading code* the first non-negotiable prevents. **A defect was found by running it: the artifact must be validated BEFORE the summary is taken.** The first version appended to the pruned log, took its index, then built the `Experiment` — and **an append-only log cannot retract an entry**, so a record passing S-5.8's rules and failing AC 1's left a summary in the *rendered prompt* with no experiment behind it (`artifacts: 0, pruned records: 1`), with nothing raising at the point the two collections stopped agreeing. Order is now build-then-append, and **every way of being refused is parametrised**, because the two collections are validated by different rules and only some fired first. **Append-only is an absence** — no `reorder`, `summarize`, `replace`, `forget` or `truncate`, asserted by inspection so it fails the moment somebody adds one for a demo — and `experiments` returns a tuple so a caller sorting what it was given is not sorting the log. **"Cache-friendly" is tested as the property the cache needs**: the render at N is a **byte prefix** of the render at N+1. The **digest test runs in a fresh interpreter**, because the guarantee a digest has is that a second process agrees — S-4.1 recorded a sabotage walking through a digest test that built the model twice in one process. `Verdict` is defined here because the log stores it; **`NARROWED` is kept separate** since collapsing it into `REJECTED` throws away the half of the search space the experiment bought. **Sabotage-verified on thirteen properties, all caught, no survivors** — including the ordering defect, which is now a sabotage in its own right.

### S-8.5 — Conditional exclusions
Depends: S-8.4
Why: an exclusion recorded as fact permanently blocks the correct hypothesis.
AC:
- Every exclusion records its preconditions: fixture shape, platform, concurrency, scales tested
- When a later experiment changes a condition, affected exclusions are surfaced as stale
- The agent may re-test a stale exclusion
- A test proves a uniform-fixture exclusion is reopened when skewed fixtures are introduced

**DONE (2026-08-16)** — `src/coldfix/diagnosis/exclusions.py`, ADR 087, 36 tests in `tests/diagnosis/test_exclusions.py`. All four AC met. **`invalidated_if` is derived, not stored, and that is the one departure from F3's sketch**: the sketch carries `conditions` and `invalidated_if` as two fields and they are two statements of one fact — an exclusion is invalid exactly when a condition it was established under no longer holds — so staleness is **computed on every read** (S-7.12's argument for refusing an override flag beside an override value; a stored flag is also wrong for exactly as long as nobody recomputes it). **The four conditions are two kinds, and that is the story's substance.** *Categorical* (fixture shape, platform) covers by membership, and multi-valued is ordinary rather than hypothetical since S-3.3's `compare_shapes` sweeps all three distributions in one experiment. *Numeric* (concurrency, scale) covers by **envelope**, min to max — an exclusion that saw 10, 100 and 1000 covers 500 and does not cover 10 000 — and **the asymmetry is the model rather than a convenience**: a defect invisible at 10/100/1000 but present at 500 would have to be non-monotonic, while one appearing past 1000 is an ordinary threshold (a cache that stops fitting, a page that splits, an index the planner abandons). Treating scales as a set would reopen everything on every intermediate point, which is Epic 4's *a caveat attached to everything is one nobody reads* with the opposite sign. The kind is a property of the **dimension**, declared once, so one exclusion cannot treat scale as categorical while another treats it as a range. **All four are required, which is AC 1 read literally** — and it also removes a tri-state, because with an optional dimension a later experiment reporting a condition the exclusion never recorded is neither a change nor a non-change (S-3.1's lesson). **An exclusion can only be made from a rejected `Experiment`**, so *no finding without a measurement* comes free and there is no constructor taking a sentence; a confirmed experiment is a finding and a narrowed one is a hypothesis that survived, and an exclusion built from either would tell the agent a live branch was closed. **`reopen` refuses a LIVE exclusion, which AC 3 does not ask for**: F3 names the danger of an exclusion treated as permanent fact, and fixing it creates the opposite one — an agent setting aside an inconvenient result by calling it stale — so a reopening must be something the recorded conditions justify, and the refusal says what would have to change. **Two bounds are stated in code rather than implied**: nothing here decides whether a *new* hypothesis is the same as an excluded one (a semantic judgement with no deterministic check, so a live exclusion blocks by being rendered as settled and not by any refusal), and `RESIDUE` says in words that four dimensions are modelled — an experiment varying a fifth produces an exclusion that **looks fully conditioned and is not**. **`render()` returns sentences, closing S-8.1's open note** that it took `exclusions: Sequence[str]` because *S-8.5 owns what an exclusion is*; the structure now exists and still hands over sentences, asserted by feeding S-8.1's `render_question`. **Sabotage-verified on twenty-two properties, all caught — after two survived and one sabotage was a no-op I wrote badly.** The pair that matters was caught from the start and is why every staleness test here has a control beside it: *nothing is ever stale* restores F3 exactly, and *everything is always stale* passes AC 2 and AC 4 while making every exclusion worthless. **The survivor worth recording is the second instance in two stories of one shape**: the AC 4 test asserted `"fixture shape" in describe(skewed)` and the **settled half of that same string already says *under fixture shape uniform***, so a rendering naming no drifted condition passed — re-anchored on *established at … went to …*. S-8.3's survivor was identical one module earlier, which makes it a habit rather than an incident: **when asserting that a rendering mentions X, check X does not already appear elsewhere in it.** The no-op sabotage (*fixture shape stops being a condition*, written as a comment change) exposed a real gap when rewritten properly — no test had covered all four dimensions individually, and one is now parametrised so none can quietly stop counting.

### S-8.6 — Evidence chain assembly
Depends: S-8.5
AC:
- Pydantic model requiring: symptom, exclusions, localization, mechanism, complexity, site, context, confidence
- **Every localization link requires an attached measurement** — schema rejects otherwise
- Context files each carry the reason they were implicated
- Golden-file test for serialization

**DONE (2026-08-16)** — `src/coldfix/diagnosis/chain.py`, ADR 088, 44 tests in `tests/diagnosis/test_chain.py` plus the golden file at `tests/diagnosis/golden/evidence_chain.json`. All four AC met. **Two of §2.4's lines turned out to be the story.** (1) **`exclusions` holds S-8.5's `Exclusion`, not a bare experiment.** §2.4 sketches an exclusion as `{hypothesis, primitive, measurement, verdict}` — with **no conditions** — and F3 is the finding that an exclusion recorded as fact permanently blocks the correct hypothesis. S-8.5 fixed that *inside the investigation*; flattening it back here would reintroduce F3 at the boundary where it does most damage, because §2.4 calls this artifact *the body of the eventual pull request*. **"Not the database, queries flat at 7, 7, 7" printed in a PR with no mention of the uniform fixtures is F3's false fact with a reviewer's signature under it**, so `render()` prints the conditions beside the claim and a test asserts it. (2) **`confidence` is derived, required and recomputed — S-7.9's construction.** §2.4 says *derived from number of independent confirmations* and `03-agents.md` §4.4 writes a bare `float`; the authority map gives artifact schemas to §2.4. A confidence an agent writes is a number nobody measured, but a bare property would not survive serialization — and this artifact is serialized on purpose, travels to two agents and becomes a PR — so the copy is **mandatory and powerless**: validating recomputes it and refuses disagreement, and `assemble` has no `confidence` parameter at all. **Independence is counted in distinct primitives** (two runs of one instrument are one kind of evidence; scaling and ablation agreeing is S-8.7's thesis behaviour showing up in the number), and **exclusions deliberately do not raise it**, since counting them would let an agent lift its own figure by excluding things nobody suspected. **It is not a probability and two properties keep that honest**: `1 - 2**-k` is a *model* (each independent instrument halves the remaining doubt), it can never reach 1 so no chain claims certainty, and the report says in words that it is a count on a 0–1 scale — `00-BRIEF.md` §6's diagnostic agreement across ten runs stays the project's actual reliability number. **AC 2 is structural rather than a validator**: a link holds an `Experiment` and S-8.4 already refuses one with an empty measurement, so there is **no `measurement` field to leave blank** (asserted by inspection, with the active attempt made one layer down). `share_of_cost` is bounded to a fraction and requires a stated `basis` — `Bound.basis`'s construction — and the schema says it cannot check the arithmetic. **`RESIDUE` travels into the rendered report**: this proves every link carries a measurement and that the confidence matches, and **does not** check that the mechanism follows, which `08-audit.md` names as the separate flaw the finding audit exists for. **A defect in S-8.5 was found by serializing one**: numeric conditions were stored as Python `int` under a field annotated `str | float`, and pydantic's union serializer said so — a stored type disagreeing with its annotation is a golden file waiting to move; `_canonical` now converts. **Sabotage-verified on twenty-six properties, all caught — after five survived in two distinct groups.** (a) **Two redundant conditions, S-7.4's shape for the third and fourth time this epic**: `min_length=1` on `localization` cannot change an outcome because *at least one confirming link* strictly implies *at least one link*, and `min_length=1` on `Implicated.reason` cannot because *not whitespace* implies *not empty*. Both collapsed, leaving the stronger check — and the surviving refusal is the better sentence. **Habit worth stating: when two checks sit beside each other, ask whether one implies the other before writing a test for both.** (b) **Three genuinely untested guards, found only because the redundant pair forced a second pass** — `mechanism`, `scope` and `metric` could all be emptied with every test passing while their neighbours `basis` and `reason` were covered. **That asymmetry is the tell: a field is not tested because it is important, it is tested because somebody happened to think of it**; they are now swept in one parametrised test over every string a reader depends on.

### S-8.7 — Instrument switching — **the thesis behaviour**
Depends: S-8.1, S-3.1
AC:
- On a rejected hypothesis, the next hypothesis must select a different primitive where the evidence supports it
- Demonstrated end to end: a repo where query count is flat, the agent concludes "not the database," switches to ablation, and localizes the real cause
- The switch and its rationale appear in the experiment log

Notes: this is the demo that justifies the entire architecture. Record it as a video when it first works.

**DONE (2026-08-16)** — `src/coldfix/diagnosis/loop.py`, `rationale` added to S-8.4's `Experiment`, `switches()`/`describe_switches()` on `ExperimentLog`, the ablatable subject at `tests/fixtures/planted/rendering.py`, ADR 089, 15 tests in `tests/diagnosis/test_switching.py`. All three AC met. **AC 1 is a code-enforced property, not an expectation of the model**: *the next hypothesis must select a different primitive* is a rule about the system, and `CLAUDE.md`'s hard-enforcement table says such a rule lives in code — so a hypothesis re-proposing an instrument already rejected under unchanged conditions is refused and re-asked, and after three repeats the loop raises `NoNewInstrumentError`, **written as a result rather than a fault** (the honest reading is *this subject's applicable experiments are exhausted*, which §9 ships as an answer). **Re-asking is not cascading and the distinction is the non-negotiable**: `generate` is called again at the same temperature on the same tier with a longer exclusion list, no validator is supplied, and what makes it legitimate is that the thing corrected is not the hypothesis's *quality* — which nothing can judge — but a fact the agent can be told, that this instrument has already answered. A test asserts `validate` appears nowhere in `propose`. **"Where the evidence supports it" is S-8.5's rule, not a new one**: the loop asks the register which instruments are settled *under the conditions in force*, so a reseed reopens one without this module knowing reseeding exists. **AC 3 required a field**: `Experiment` stored the primitive and not why it was chosen, and the primitive alone shows *that* the instrument changed while **the thesis claim is about the choosing** — so `rationale` (which S-8.1 already produced and was discarding) is now required, not defaulted. `switches()` is a **view, not a record**, the shape S-8.5 refused for `invalidated_if` and S-8.6 for `confidence`; `describe_switches()` names the provoking verdict, because switching after a rejection and after a confirmation are different behaviours and only the first is claimed. **What the demo proves is bounded in writing, because this is the story where over-claiming would be easiest.** The measurements are **real** — `scaling.volume` and `ablation.stub` execute against a planted defect, two queries at every scale and stubbing the renderer removes essentially all the wall time — and the subject is not a new defect but `render_with_expensive_downstream` expressed as a collaborator so an instrument can interpose (the shape S-3.4's docstring says it needs and no real subject had provided). **The model calls are replayed**, so the test proves the rejection propagates, the harness refuses a repeat and the log records the switch — and it does **not** prove a model would choose to switch unprompted. `run_investigation` takes the client as a parameter for exactly that reason: **the video runs this same function against `AnthropicClient`**, since a demo of a second implementation would be a demo of the second implementation. **The measurement reaching the model is counts and a rounded share**, because a prompt carrying `8.2447281639…` differs on every run and an end-to-end replay over raw timings is impossible in principle; counts reproduce to the integer (ADR 052) and the timings travel in `detail`. **The loop holds no budget** (`max_steps` is a loop guard against a `while True` around a paid API; S-8.9 owns caps) **and assembles no chain** (S-8.6 needs a symptom, mechanism, site and implicated files that the loop never measured). **Sabotage: the FIRST PASS WAS WORTHLESS AND REPORTED A CLEAN SWEEP OF NINETEEN** — the runner passed `--timeout=120` to a pytest without `pytest-timeout`, so every run exited non-zero on an argument error and every sabotage read as caught. The tell was present throughout: every "caught" line had an **empty `by:`**. Third instance of this class after Epic 5's `git checkout` and Epic 6's marker filter; the runner now **fails loudly on a non-zero exit with no parsed failure** and checks the baseline at both ends. **The real pass: nineteen properties, all caught after four survived, and all four were untested properties rather than weak code** — the rationale dropped on its way into the log (AC 3 was asserted everywhere except through the loop that has to satisfy it), the loop treating a narrowed verdict as confirmed and the loop unbounded (neither path reachable with a defect that confirms on step two, so both needed a synthetic run), and **`switches()` reporting every consecutive pair** — the fixture had two experiments with two different primitives, so *every pair* and *every changed pair* were the same list, the sixth instance of a fixture that could not discriminate.

### S-8.8 — Reseed tool
Depends: S-7.7
AC:
- The Diagnostician can request new fixtures with a specified shape
- Reseeding invalidates affected exclusions per S-8.5
- Cost of reseeding counted against the experiment budget

**DONE (2026-08-16)** — `src/coldfix/diagnosis/reseed.py` plus `Investigation.reseed`, ADR 090, 17 tests in `tests/diagnosis/test_reseed.py`. All three AC met, and **almost everything needed already existed**: S-7.7 builds a fixture at a chosen shape, S-8.5 decides what a changed condition reopens, S-5.4 has both halves of a step cap. What was missing was the doorway — and the guards that stop it being a way around the other two. **This is the other half of S-8.5**: that story made an exclusion conditional so it *could* be reopened, and until now nothing moved a condition on purpose, so **`Conditions` changed only when somebody rebuilt the world by hand and *may be reopened* was a property nothing exercised**. The composition test asserts it through `Investigation` rather than through the function alone, because AC 1 is *the Diagnostician can request new fixtures* and a capability reachable only from a helper would satisfy the criterion without closing the gap (Epic 7's finding restated: *the criterion is met* and *the criterion is reachable* are different claims). **Two guards the AC do not ask for.** (1) **A reseed that would move no condition is refused** — it reopens nothing, establishes nothing, and costs one of the forty experiments; this is S-8.5's `reopen` guard pointing the other way, and the control matters as much, since reseeding to the same shape at a **wider scale** does move a condition and refusing it would make the guard a prohibition on reseeding at all. (2) **The conditions change only after the seeding succeeded**, which is the whole correctness argument: adopting them first and failing second would reopen every exclusion mentioning the old shape **against a fixture that was never built**, and the agent would re-run an experiment believing the world had changed, get the same answer and record it as new evidence — **worse than the gap this story closes, because it manufactures a reason to disbelieve a correct exclusion** (S-8.4's build-then-append finding one module over). **AC 3 needed no new machinery**: `authorize(phase, finding_id)` with a zero worst-case is already a pure step-cap check and S-5.4's argument for calling it first applies unchanged; a failed reseed is **not** charged, following `Session.run`'s precedent, which is safe only because the refusal propagates and nothing retries a broken seeder in a loop. **The recorded conclusion carries the recipe digest**, which a sabotage proved load-bearing — S-5.4 escalates a phase whose last three steps concluded the same thing, so a reseed always reporting `"reseed"` would stall the investigation on its third genuinely different fixture. **Who decides to reseed is not settled here** (E12's, the same line S-8.7 drew), and this module **seeds nothing itself** — S-7.6 and S-7.7 own that. **Sabotage-verified on sixteen properties, all caught, after two survived and one run hit a harness fault.** **The harness-fault detector added in S-8.7 paid for itself one story later**: a sabotage produced an unparseable file, so pytest exited non-zero with no failure line — which under the old runner would have read as a clean catch — and the `!!! HARNESS FAULT` branch reported it instead. The survivors: **dropping the carried-forward concurrency to the literal `1` changed nothing because every fixture in the file ran at concurrency 1** (seventh instance of a fixture that could not discriminate; a reseed during a load experiment must not quietly reset the recorded load to serial), and **the stall test performed two reseeds where `DEFAULT_STALL_AFTER` is three**, so it could not distinguish a per-fixture conclusion from a constant one — it now runs three different fixtures and asserts no stall, **with a control running the same recipe three times and asserting one**, because a conclusion that never repeats makes the stall check unreachable, which is the opposite defect and just as silent.

### S-8.9 — Budget and progress
Depends: S-5.4
AC:
- 40-experiment cap
- Escalates after 8 experiments with no narrowing
- On exhaustion, emits a partial chain containing the exclusions — a proven negative is a result

**DONE (2026-08-16)** — `src/coldfix/diagnosis/progress.py`, `Investigation.partial_chain`, ADR 091, 21 tests in `tests/diagnosis/test_progress.py` plus 3 in `test_switching.py`. All three AC met. **AC 1's cap existed since S-5.4 — and running a loop against it found it was a THIRD of its size.** `Session.run` called `record_step` once per **model call**, and the loop makes three calls per experiment, so the forty-experiment cap was a **thirteen-experiment cap**. S-5.4's own docstring predicts it word for word: *§12.1 budgets 120 model calls per finding against a cap of 40 experiments — so an experiment is about three calls, and a cap counted in calls would halt investigation at a third of its intended budget.* It survived because nothing ran a whole loop against it: **Epic 5's composition test drove one `session.run` per iteration, which made a call and an experiment look like the same thing — the test that should have caught it encoded it instead.** `Session.run` now records a step only where the phase's cap is counted in steps; a phase counted in experiments, attempts or rounds has its unit counted by whoever owns it. Two Epic 5 composition tests were updated with the reason recorded in them. **AC 2 needed a number the default gets wrong**: §4.5 puts the investigate check at 8 and `DEFAULT_STALL_AFTER` is 3, so S-7.10's rule applies — **a budget with the wrong value is refused, not corrected**. S-7.10 also wrote in a comment that three *is* right for an investigation; **that comment is now corrected**, since every phase that has looked has needed its own value. `Session` gained a `stall_after` pass-through, because it is the only thing that constructs a `Budget` and a phase had no way to ask. **"No narrowing" is decided by the harness** (S-5.4's F6 rule) and **a rejection is not narrowing** — §2.2: *reject → new hypothesis informed by the exclusion; narrow → one level deeper*. **AC 3 is the story: a partial chain is a separate artifact with the OPPOSITE requirement.** `EvidenceChain` requires ≥1 confirming link; relaxing that so it could represent an investigation confirming nothing would destroy the guarantee S-8.6 exists for — a chain would stop meaning *a cause was established*. So `PartialChain` **refuses to hold a confirmation**, the two partition, and neither can impersonate the other; one carrying a confirmation would report an established finding as an absent one, which **loses a result** rather than mis-typing one. **It has no `mechanism`, `site` or `confidence`, and their absence is the artifact's meaning** — those are claims a stopped investigation cannot make, and a field for one is somewhere a reader could put a guess. Empty exclusions are legitimate (forty narrowings exclude nothing while learning something), so the rendering says which case it is. **Stopping is not failing**: `Stopped` has three members because a reader's next action differs for each, each carries §7.2's disposition, `run_investigation` returns rather than raises, and **`max_steps` is gone** — there is now exactly one number that stops the loop, the forty `04-cost.md` costed. **A euro-ceiling breach still propagates**, since S-5.4 says *the halt is the global ceiling's alone* and reporting one as *the experiment cap was reached* would be false about why the run ended. **Sabotage-verified on seventeen properties, all caught — after five survived, EVERY ONE in the loop wiring rather than in the module.** The unit tests exercised `progress.py` thoroughly and the loop's use of it not at all — the same asymmetry S-8.7 hit, *a criterion asserted where it is defined and not where it has to hold*. **Two of the five were not test gaps but code that could not matter**: the loop's `authorize` before each step (removing it changed nothing, because `Session.run` authorizes inside its first attempt against the same cap — S-7.4's redundant condition, **the fifth in Epic 8**), and `Investigation.confirmed`, a property with **no caller at all** duplicating what `PartialChain`'s validator refuses. Both deleted.

---

**EPIC 8 COMPOSITION CHECK (2026-08-16)** — `tests/diagnosis/test_diagnosis_composed.py`, `src/coldfix/diagnosis/emit.py`, ADR 092, 13 tests. The epic finished with nine stories, **295 passing tests**, a sabotage pass on every one — and **no way to take a screened workload, investigate it, and emit an evidence chain**. Epic 7's finding, repeated almost exactly: **three defects, and every one was a join**; not one was a module wrong about its own subject. (1) **Two append-only logs, again — this time across an epic boundary.** `Session` constructs a `PrunedLog` and renders it into the block §4 caches; `ExperimentLog` wraps its own; nothing joined them, so **the session's log block rendered empty forever** while the real log rode in the uncached question. Measured before the fix: two experiments in the loop's log, **zero records in the session's**, a 208-character block containing only S-5.8's retrieval notice. Epic 5 found this *inside its own epic* and recorded why it is silent — caching is a prefix match, so a log wrong in content is still append-only and still reports hits — and what it costs is the entire growing part of the prompt at full price against a cost model assuming 85% cached, plus S-5.8's pruning report (where §5's 60–80% claim is measured) measuring an empty log. **S-8.4 exposed `.pruned` for exactly this join and nothing used it.** (2) **The conditions and the symptom had no producer.** Every caller built them by hand **including every test, which is precisely why nothing noticed**; `Workload` already carries `fixture.distribution` (the shape actually seeded) and `observations` (the scales actually driven). A hand-built `Conditions` can say `uniform` while the recipe says `long_tail`, and **an exclusion recorded under a shape that was never used is permanently and wrongly live** — F3 reintroduced at the join S-8.5 exists to close. (3) **S-8.6 was unreachable**: a confirmed investigation had no path to the artifact the epic exists to produce, so `EvidenceChain` could be built by hand in a test and by nothing in the system — Epic 7's *AC satisfied in isolation and unreachable in practice*, and **the more dangerous half, because the criterion reads as met**. `chain_from` is that path and deliberately invents no mechanism, site or implicated files, and refuses a confirming experiment with no measured share. **ONE DEFECT FOUND AND NOT FIXED, recorded so nobody rediscovers it or quotes §12.3's cost as achieved**: `Session.run` assembles S-5.7's blocks and **the request never carries them** — each diagnosis module builds its own `messages=[{"role": "user", "content": question}]`, so the `cache_control` breakpoints are never sent and **Epic 5's entire prompt-caching design is inert**. Not fixed here because it touches S-0.7b's client, `Session.run` and all three call sites, and changes how every request in the system is shaped; it belongs in its own story with its own sabotage pass. **The harness moved to `tests/fixtures/thesis.py`**, because two suites need the planted subject and a test module importing another test module is a source file mypy sees under two names. **The `query_counter` fixture registers under a name different from its function's**, so importing it does not read as a redefinition of the parameter using it — the obvious route, a `tests/diagnosis/conftest.py`, collides with `tests/sandbox/conftest.py` under mypy. **This project had already recorded that hazard and this session walked into it anyway**, which is the lesson worth keeping: a recorded hazard only helps if it is read *before* the obvious thing is tried. **Sabotage-verified on ten properties, all caught after one survived** — hardcoding `"uniform"` in `conditions_for` changed nothing because the subject's fixture **is** uniform, **the eighth instance of a fixture where the right answer and the wrong answer coincide**; the test is now parametrised over every `Distribution`. Worth carrying into Epic 9: **restoring defect 1 is caught by two tests and neither is about logging** — they are about what the prompt contains and what the run reports, and *a defect whose only symptom is a cost figure needs a test that reads the cost figure*.

---

# EPIC 9 — Finding audit

**Goal:** audit the diagnosis before any repair spend. **Build before the Surgeon.**

**Why this epic exists:** the patch audit checks equivalence, cheats, trades and scope. If the diagnosis is wrong, all of those pass — a correct fix to a non-problem is equivalent, is not a cheat, trades nothing, and breaks no callers.

### S-9.1 — Finding-audit invocation
Depends: S-8.6
AC:
- Adversary role invoked with the **raw experiment log**, not the assembled evidence chain
- Separate message list constructed fresh — no Diagnostician reasoning included
- Different model vendor where configured

Notes: giving it the raw log rather than the chain reduces framing inheritance. Isolation is partial, not clean — document it as such.

**DONE (2026-08-17)** — `src/coldfix/audit/invocation.py`, ADR 093, 21 tests in `tests/audit/test_invocation.py`. All three AC met. **AC 1 and AC 2 contradict each other, and S-8.7 is why**: AC 1 says hand over the raw log, AC 2 says include no Diagnostician reasoning, and **S-8.7 added `rationale` to every log record** — *why this instrument was worth its cost*, written by the Diagnostician, sitting in the raw log. Handing it over verbatim satisfies AC 1 by breaking AC 2. Neither criterion is wrong; they were written before the field existed, and this is the most direct instance yet of composing two stories exposing a conflict neither could see alone. **`08-audit.md` decides it and supplies the number**: isolation *removes the explicit rationalization, which is the documented risk — **72% of reward-hacking episodes carry explicit justifying reasoning***. So `render_evidence` withholds **`rationale`** (prose whose purpose is to justify a choice) and **`outcome`** (the agent's gloss on its own measurement — *"queries flat at 7, 7, 7 — this is clearly not the database"*), and keeps what was tested, what ran, how it was configured and what was measured. **`verdict` stays and that is a decision**: it is a three-valued classification S-8.3 ties to cited measurements rather than prose, and an auditor asked whether an exclusion was adequate must know something *was* excluded — the opposite failure, *isolation by sending nothing*, is a sabotage in its own right because it would satisfy AC 2 perfectly while making the audit useless. **The evidence is rendered from the `Experiment` artifacts, not from S-5.8's summaries**, because those are composed from `outcome` — and removing a field from rendered prose would be editing text to enforce a boundary, which is exactly what `CLAUDE.md` says must not be how this holds. **The audit gets its own `Session`, because a shared one leaks silently**: `Session` caches one assembled prompt per model carrying its owner's system text, playbook and source as a cached prefix, so an audit billed through the Diagnostician's session inherits all of it — **isolation undone by the object it was billed through while every message list stayed clean**. `refuse_shared_session` refuses anything else, checked **before** any spend. Its playbook is deliberately empty, since an auditor reasoning from the investigator's habits inherits exactly the frame this cannot remove and should not add to. **The enforcement is an absence three times over**: `invoke` has no `chain` parameter (AC 1), no `messages`/`history` parameter (AC 2), and no `validate`; `audit_messages` returns a **new list every call**, tested by mutating it. **Routing reuses §3's existing `ATTACK_DESIGN` row** — an audit is an attack, on the diagnosis rather than a patch — so no new row after Epic 8 added two. AC 3 needs no code: the vendor is `Router` configuration and ADR 062 records the second-vendor blocker as indefinite. **`RESIDUE` carries `08-audit.md`'s honest position** — the isolation removes explicit rationalization and **does not remove framing bias**, since the log still records which experiments the Diagnostician thought worth running — asserted word for word including *not describe this as clean separation*. **Sabotage-verified on fifteen properties, all caught, no survivors.** **Deliberately scoping-independent**: Epic 9's shape is under review against S-0.8 (the audit was designed to catch fabrication, which happened **0 times in 60 real-model runs**, while the measured failure was non-termination), and S-9.2–S-9.8 may change — but an isolated auditor working from measurements is needed under any scoping, and its input is already the log rather than the chain, so pointing it at an unfinished investigation is a change to S-9.8's routing rather than to this.

### S-9.2 — Exclusion validity attack
Depends: S-9.1
AC:
- Checks whether ruled-out hypotheses were ruled out under adequate conditions
- Flags exclusions whose preconditions were too narrow

**DONE (2026-08-17)** — `src/coldfix/audit/exclusions.py`, ADR 096, 20 tests in `tests/audit/test_exclusion_validity.py`. Both AC met. **S-8.5's machinery turned on itself**: that story made every exclusion carry the conditions it holds under so it *could* be reopened; this reads them and asks whether they were ever wide enough to establish anything. **The second attack that turns out to be arithmetic** — which axes were varied is computable from `Conditions`, so two of Epic 9's seven attacks need no model, worth noting because the epic's framing (*attacks*, *the Adversary*) reads as adversary calls throughout. **What it cannot do is named rather than glossed over**: it sees which axes were unvaried and cannot judge whether an unvaried axis was *relevant to this hypothesis* — that is S-9.5's — so every objection is phrased as **what was not varied** and the report says in words that the relevance judgement is not being made. **Uniform-only is a sharper objection than single-shape, and the asymmetry is PROVED**: S-3.3 exists because `Σ k²` is minimized exactly when every parent is equal, so for **any** per-parent cost the uniform fixture is the *provably blindest* one — F3's worked example, attacked. Long-tail-only is also single-shape but is the deliberate worst case, so it gets the weaker objection; collapsing the two would tell a reader who used the hardest fixture that they used the blindest. **Actionable and inherent narrowness are reported separately**: uniform-only is reopened by S-8.8's reseed *and the remedy text says so*, closing the loop S-8.5 opened; a **single platform** is none of those — demanding a second architecture is not a remedy anybody can apply — so it is recorded as a bound and does **not** make an exclusion inadequate, which is `00-BRIEF.md`'s *exclusions carry their preconditions* read as a reporting rule. **The scale axis delegates to S-9.4** rather than asking the same question twice, and **a missing fit is not an objection** — not every rejection came from a sweep, and inventing a fit to judge would be auditing a curve nobody drew (`scales is None` means *not audited*, which S-3.1 distinguishes from *passed*). **The control is the load-bearing half**: an auditor objecting to every exclusion satisfies both AC while making §9's *null results are valid output* unreachable, so *a multi-shape sweep is flagged anyway* and *every run is called serial* are sabotages in their own right. **Sabotage-verified on sixteen properties, all caught after one survived** — the report's remedy could be deleted without failing anything, because the tests asserted each objection **has** a remedy and never that the rendering **prints** it. **Testing the data and not the rendering is the same gap the Epic 8 composition found in a different place: the reader gets the report, not the enum.**

### S-9.3 — Fixture adequacy attack
Depends: S-9.1
AC:
- Assesses whether fixture shape could have hidden the real cause
- Can request a re-run under different fixture shape

**DONE (2026-08-17)** — `src/coldfix/audit/fixtures.py`, ADR 097, 13 tests in `tests/audit/test_fixture_adequacy.py`. Both AC met. **AC 2 is the one with teeth**: every audit story before this can only object, and this is the first that can *cause work* — which makes it where ADR 094's warning applies most directly, since an audit whose lever is *run more experiments* worsens the failure S-0.8 measured. **So the capability is split: this module can ASK and cannot DO.** It produces a `ReseedRequest` and has no seeder, no budget and no call into S-8.8; executing goes through `reseed`, which authorizes against the experiment cap **before** it seeds. An auditor that could seed directly would be doing the harness's job and spending budget nobody authorized — from the one component whose whole purpose is scepticism about the run it examines. Asserted by inspection. **A request is only made when it would change something**: S-8.8 refuses a reseed that moves no condition, and this refuses to *ask* for one — two guards on the same waste at different layers, and this is the cheaper one, since S-8.8's refusal arrives after a caller has already spent a round producing an instruction destined for rejection. **Which shape to ask for is derived**: `LONG_TAIL` first because S-3.3 calls it *the deliberate worst case for any per-parent cost*, `POWER_LAW` second as the smooth spectrum a long tail does not cover, `UNIFORM` last because `Σ k²` makes it blindest. **The request changes only the distribution** — same entity, size and source — because `allocate` spends the same total over the same parents, so shape is the only difference; a request that also changed the size would produce a number differing for two reasons. **How it differs from S-9.2**: that audits an *exclusion* (was this ruled out adequately), this audits the *investigation's fixture* against the cause claimed or missed, and produces something executable. They agree uniform is blind because both read the same `Σ k²` proof — two modules agreeing by consulting one argument is not duplication; each holding its own copy would be. **Sabotage-verified on thirteen properties, all caught, no survivors** — the pair that matters being *a request is made even when every shape was swept* against *no request is ever made*. **Two test defects worth recording, both mine**: a control asserted `"could have hidden" not in describe()` and the adequate rendering says *"Nothing ... **could have hidden** a per-parent cost"*, so it failed on the negation — **a negative assertion over formatted text is a substring check, recorded at S-7.11 and walked into again**; and the isolation test asserted `"budget" not in source.lower()` while the docstring discusses the budget at length — **a substring check over source text cannot tell an explanation from an action**, so it now asserts what the module imports and takes.

### S-9.4 — Scale adequacy attack
Depends: S-9.1
AC:
- Checks whether tested scales were large enough to separate linear from superlinear
- Flags fits with poor r² or too few points

**DONE (2026-08-17)** — `src/coldfix/audit/scales.py`, ADR 095, 20 tests in `tests/audit/test_scales.py`. Both AC met. **Nothing here calls a model, and that is the story's first finding**: S-9.4 sits in an epic of *attacks*, which reads as adversary calls, and `CLAUDE.md` says *do not add a model call where a function would do — counting and curve fitting are code*. A test asserts the module imports no client and no session. **The threshold is DERIVED, not chosen.** The obvious implementation picks *ten times* and cannot say why; the number falls out of two figures already measured here — `SUPERLINEAR_ABOVE` is 1.15 so the gap to resolve is **0.15 wide**, and S-0.4 measured **12% drift**. A power fit is linear in log space, so relative error `e` becomes `e / ln(span)` in the exponent, and requiring that `sigma` times smaller than the gap gives `span >= exp(sigma*e/gap)` = **11.0x** — which is why this project's fixtures sweep 10x and 100x rather than 2x. **The consequence worth having runs the other way**: a harness with S-1.7's *certified* 2% floor needs only **1.5x**, so `required_span` takes the noise as a parameter rather than holding every caller to a constant derived from somebody else's machine. The number that makes it non-arbitrary: **at 12% noise a 2x sweep determines the exponent to +/-0.17 against a 0.15 gap** — it cannot tell the classes apart at all. **Two failures that look alike and are not**: a tight fit over a narrow span is *confidently wrong* (r-squared near 1.0 because few points define a line), a loose fit over a wide span is *honestly uncertain*, and the remedies are **opposite** — widen the sweep vs reduce the noise or add points — so they are separate objections and `describe` states the right remedy for each. **The audit's bar is one point above the instrument's**: S-3.2 needs three to *fit*, this needs four to *check*, because at three there is no point that can be dropped and re-fitted — **an audit whose bar equals the instrument's bar is not auditing anything**; distinct scales are counted, not measurements. **A narrow sweep can still support the weaker claim** — the constant/linear gap is 1.0, not 0.15 — so `resolves_growth` answers per claim rather than per sweep, since refusing outright would throw away the exclusions section 9 ships as answers. **Order matters reading the fit**: S-1.5 nulls `exponent`, `power_r_squared` and `growth` *together*, so checking r-squared first would report *the power law does not describe these measurements* about a power law nobody fitted. **Sabotage-verified on seventeen properties, all caught, no survivors** — the pairing that matters being *a narrow sweep is never flagged* against *every sweep is flagged as narrow*, because an auditor that objects to everything passes every negative test while making the epic a machine for rejecting sound findings.

### S-9.5 — Alternative explanation attack
Depends: S-9.1
AC:
- Proposes a different mechanism consistent with the same measurements
- If one exists and was not excluded, verdict is `unsound`

**DONE (2026-08-17)** — `src/coldfix/audit/alternatives.py`, ADR 098, 20 tests in `tests/audit/test_alternatives.py`. Both AC met. **The first attack in this epic that genuinely needs a model** — S-9.2, S-9.3 and S-9.4 turned out to be arithmetic, and *is there another story these numbers would also tell* is not computable; `08-audit.md` names it as the flaw schema validation cannot reach (*"no finding without a measurement" prevents fabrication; it does not prevent a correct measurement supporting a wrong conclusion*). **"NO ALTERNATIVE" IS THE DESIGN, not a tolerated edge case.** AC 2 turns **any** alternative into `unsound` and the amended S-9.8 routes `unsound` back to investigate, so **an auditor that cannot say *I have nothing* guarantees an investigation never ends** — S-0.8's measured failure reached through the audit instead of through the agent. So the prompt offers the empty answer explicitly (*that is a result, not a failure*; *the right answer whenever you have to strain to find one*), because a model that must invent the escape hatch will not use it; the parser treats it as ordinary and case-insensitively; and the report says *this attack passing, not this attack failing to run*, because a reader who cannot tell those apart treats a clean finding as an incomplete audit. **The judgement is the model's; the citations are checked** — an alternative quoting a figure nobody measured is a story that happens to mention numbers. **Checked against EVERY experiment, not one, and this is why it does not reuse `check_citations`**: that compares one mapping to one measurement, while a log holds many and the same metric legitimately differs between them (`db.query` is 7 in the sweep that ruled the database out and 1004 in the ablation that found the cause), so reusing it would call one of two real numbers a fabrication. **A case where writing a second checker was correct rather than duplication**, recorded so nobody tidies it away. **"Not excluded" is ARGUED by the auditor, not decided by code**: deciding whether exclusion X covers alternative Y is the same semantic judgement this story exists because code cannot make, so the auditor is shown the rejections (S-9.1's evidence carries verdicts) and must say which fails to cover its proposal — *there might be another explanation* with no account of why the experiments missed it is not an objection anybody can act on. **A malformed answer raises rather than being re-asked**, because `ATTACK_DESIGN` cannot cascade. **Sabotage-verified on sixteen properties, all caught, no survivors** — the pair that matters being *the empty answer stops being recognised* against *every answer reads as no alternative*.

### S-9.6 — Reproducibility check
Depends: S-9.1, S-5.1
AC:
- Re-runs one key experiment, bypassing the replay cache
- Compares against the recorded result
- Material divergence produces `unsound`

### S-9.7 — Representativeness assessment
Depends: S-9.1
AC:
- Assesses whether the workload resembles something users exercise
- Verdict `unrepresentative` skips to the next finding without repair spend
- Limitation documented: the agent cannot know real traffic patterns

### S-9.8 — Verdict and routing
Depends: S-9.2–S-9.7
AC:
- Verdict schema: `sound` / `unsound` + objection / `unrepresentative` + reason / `negative_sound` / `inconclusive` + what is missing
- `unsound` returns to investigate with the objection in context **only while the investigation has budget left**; with none it escalates
- Cost of the audit is under 15 calls

Notes (**amended 2026-08-17, ADR 094**): the last two verdicts and the budget condition were added after S-0.8. An audit whose only lever is *run more experiments* makes the one failure that spike actually measured — non-termination — worse; and `00-BRIEF.md` §9 makes a null result shippable output, which the original vocabulary had no way to express.

### S-9.9 — Sufficiency and the null result
Depends: S-9.1, S-8.9
Why: **added 2026-08-17 (ADR 094).** S-0.8 measured the agent declining to stop 60 times out of 60, and concluded the stopping decision "probably cannot be the agent's own". S-8.9's cap bounds the damage without deciding sufficiency — and an investigation stopped by it produces a `PartialChain` that **nothing else in Epic 9 can audit**, because every other story assumes a finding exists.
AC:
- Audits a `PartialChain`: were the exclusions established under adequate conditions?
- Distinguishes *nothing was found and that is a result* from *this run stopped too early*
- A run judged sufficient is not returned to investigate, whatever budget remains
- A test proves an investigation the agent would have continued can be stopped by the audit

---

# EPIC 10 — Surgeon agent

**Goal:** fix the confirmed finding, test first.

### S-10.1 — Falsification test generation
Depends: S-9.8
AC:
- First output is a test, not a patch
- Test asserts both cost improvement and correctness preservation
- Test enumerates the cheat classes it is designed to catch

### S-10.2 — Must-fail gate
Depends: S-10.1
AC:
- Test runs against unpatched code before any patch is written
- If it passes, the story stops with a report — no patch is written
- A test proves this gate fires on a vacuous falsification test

### S-10.3 — Test audit before patch
Depends: S-10.1, S-9.1
Why: the test is written by the agent that then writes the patch.
AC:
- Adversary reviews the falsification test **before** patch generation
- Question asked: could a cheat pass this test?
- If yes, the Adversary supplies a strengthened test and the Surgeon must satisfy it
- Costs under 5 calls

### S-10.4 — Patch generation
Depends: S-10.3, S-2.4
AC:
- Scope determined by the evidence chain's context list, not agent guessing
- Multi-file patches supported
- Runs in candidate mode only
- Patch applier rejects protected paths

### S-10.5 — Retry discipline
Depends: S-10.4
AC:
- Three attempts maximum
- **Structural check**: attempt 2 is rejected before running gates if its diff touches the same lines with a similar edit shape as attempt 1
- Failure reasons carried in context
- Temperature raised on retries to force different approaches
- Escalates with full attempt history after three

Notes: "must differ in approach" cannot be self-judged — the agent writes its own approach label and can rename the same idea.

### S-10.6 — Slack-reducing classifier — **SAFETY**
Depends: S-10.4
Why: our own output is the class of change that pushes systems from stable into vulnerable.
AC:
- Pattern-matches the diff for: added cache or memoization, retry logic, connection reuse, pool size reduction, timeout reduction, added buffering
- Matched patches labelled `slack-reducing`
- **Auto-approval permanently blocked** for labelled patches, at any trust level
- Emits a specific staging warning naming what headroom was removed
- Where fault injection is available (S-3.16), runs the retry-amplification check and attaches the result
- **Does not claim the patch was tested for metastability**

---

# EPIC 11 — Patch audit

**Goal:** defeat the patch, not review it.

### S-11.1 — Isolated context
Depends: S-10.4
AC:
- Receives original code, patched code, evidence chain, falsification test
- Never receives Surgeon reasoning, rationale, or prior attempts
- Isolation enforced by fresh message-list construction, not by prompt instruction
- Different model vendor where configured

### S-11.2 — Equivalence attacks
Depends: S-11.1
AC:
- Constructs adversarial inputs: empty collections, nulls, duplicates, ties, unicode, boundary sizes, unordered results
- Runs both revisions and diffs outputs
- On difference, returns a reproducing input

### S-11.3 — Cheat detection
Depends: S-11.1
AC:
- Checks for cached state across runs, deferred work, over-fetching, stubbed responses, shape-specific special-casing
- Verifies the improvement survives a fresh process

### S-11.4 — Trade attacks
Depends: S-3.8
AC:
- Checks the global resource envelope, not just declared guard pairs
- Reports what increased alongside what decreased

### S-11.5 — Scope attacks
Depends: S-11.1
AC:
- `find_callers` locates other call sites of every modified symbol
- Runs the full test suite
- Reports callers outside the tested workload

### S-11.6 — Test-quality attack
Depends: S-11.1
AC:
- Asks whether a cheat could pass the Surgeon's falsification test
- If yes, writes the test that would catch it
- Strengthened test becomes the permanent regression test

Notes: this is the deepest move in the design. The Adversary audits the verifier, not only the artifact.

### S-11.7 — Verdict
Depends: S-11.2–S-11.6
AC:
- Schema: `clean` / `broken` + reproducing input / `suspicious` + concern
- `broken` requires a reproducing input — schema-enforced
- Two rounds maximum, then escalate

### S-11.8 — Adversary ablation study
Depends: S-11.7
Why: if the Adversary adds little, it is theatre and should be cut.
AC:
- Runs a set of findings with and without the Adversary
- Counts bad patches reaching a human in each condition
- Reports the delta
- Repeats at two model tiers to test whether the mid tier misses attack classes

---

# EPIC 12 — LangGraph orchestration

**Goal:** durable execution across hours, crashes, and multi-day human gates.

### S-12.1 — Graph assembly
Depends: E7–E11
AC:
- Seven nodes wired: ground, screen, investigate, audit_finding, repair, audit_patch, ship
- Four routing functions implemented per `08-audit.md`
- Graph compiles and runs end to end on the target repo

### S-12.2 — Checkpointing
Depends: S-12.1, S-6.3
AC:
- SQLite checkpointer in development, Postgres supported for concurrent campaigns
- State persisted after every node
- Checkpoint size bounded per S-6.3

### S-12.3 — Crash resume
Depends: S-12.2
AC:
- A run killed mid-investigation resumes from the last checkpoint with full state
- Tested by killing the process at three different nodes
- Resumed run produces the same final result as an uninterrupted run

### S-12.4 — Human interrupt at ship
Depends: S-12.2
AC:
- `interrupt_before=["ship"]` at trust level 0
- The human sees the evidence chain, the patch, before/after measurements, and the Adversary verdict
- Resume after an arbitrary delay works

### S-12.5 — Early human checkpoint
Depends: S-12.4, S-9.8
Why: otherwise the human arrives after all cost is spent.
AC:
- Optional interrupt after the finding audit, before repair
- The human sees what was found and why, before any fix is attempted
- Enabled at trust level 0, skipped at higher levels

### S-12.6 — Time travel
Depends: S-12.2, S-6.2
AC:
- Rewind to an earlier checkpoint restores checkpointed state
- **Persistent store is unaffected** — failure memory from the later state survives
- A test rewinds after a failed repair and asserts the agent does not repeat the same approach

---

# EPIC 13 — Memory and trust

### S-13.1 — Playbook store
Depends: S-6.2, S-7.1
AC:
- Entries keyed by framework fingerprint
- Structured: situation, action, outcome
- Retrieved into Explorer context at grounding

### S-13.2 — Provisional writes and promotion — **SAFETY**
Depends: S-13.1
Why: a wrong entry propagates silently to all future runs and compounds.
AC:
- New entries are provisional and carry success/failure counters
- Promotion to trusted requires N successes **across different projects**
- Two failures demote and quarantine
- Entries are fingerprint-scoped, never global
- A test proves a wrong provisional entry does not affect a different project

### S-13.3 — Failure memory
Depends: S-6.2
AC:
- Rejected approaches recorded per finding
- Carried into retry context
- Survives rewind

### S-13.4 — Trust ledger
Depends: S-6.2
AC:
- Levels 0–2 per fix category
- **Keyed by project shape characteristics, not category alone**
- New projects start at level 0 regardless of cross-project history
- Cross-project history shown as advisory context
- Any revert or rejection demotes one level

Notes: a `select_related` fix approved 50 times on narrow-table projects may be wrong on a wide-table project. Trust learned elsewhere is context, not authority.

### S-13.5 — Learning curve measurement
Depends: S-13.1
AC:
- Explorer steps to first runnable workload recorded per project
- Plotted against number of projects with that fingerprint
- Should decline; if it does not, memory is not working
- **A controlled ablation of the playbook itself**: ground the same unseen repository with the playbook retrieved and with it withheld, and report the difference in stage completion and steps-to-ground

Notes: the curve is longitudinal and confounded — it declines if the playbook works, and also if later projects happen to be easier. The ablation is the causal measurement, and it is the project's own core primitive applied to the project. It also serves as a regression test: if a playbook edit stops helping, the delta shrinks and that is visible. S-0.4 supplies the method — interleave conditions and require a guard counter (here, stage completion) to move alongside the timing. **Do not copy S-0.4's fixed warm-up discard**: that spike hard-coded one, which S-1.2 forbids for good reason (Barrett et al. found at most 43.5% of VM/benchmark pairs reach steady state at all, so 'discard the first N' is an assumption that is wrong more often than not). Record whether each sample ran in a fresh or reused process and let the analysis decide.

---

# EPIC 14 — Adapters and MCP

### S-14.1 — Adapter interface
Depends: E1–E4
AC:
- Formal interface: `discover_workloads`, `seed`, `run_workload`, `run_tests`, `read_source`, `apply_patch`, `reset_state`, `capabilities`
- Declares hook points, framework-internal frames, protected paths, ORM dialect
- Defined as a Protocol with full typing

### S-14.2 — Django adapter
Depends: S-14.1
AC:
- Implements the full interface for Django + DRF + Postgres
- Query hook via `execute_wrapper`
- Works on the target repo and the holdout repo

### S-14.3 — Second adapter
Depends: S-14.2
AC:
- SQLAlchemy or Flask adapter implementing the same interface
- **Core code unchanged** — asserted by a test that runs both adapters through the same pipeline

### S-14.4 — Adapter conformance suite
Depends: S-14.1
AC:
- A test suite any adapter must pass
- Covers all interface methods plus reset reliability and hook overhead
- Documented so a third party can implement an adapter

### S-14.5 — MCP server extraction
Depends: S-14.3, S-14.4
Why: MCP earns its place only when an adapter exists in a different language.
AC:
- Adapter interface exposed as an MCP server
- Orchestrator connects to it over the protocol
- A reference adapter in a non-Python language demonstrates cross-language operation
- Conformance suite runs against the MCP-hosted adapter

Notes: this is genuinely last. Adding a protocol between two components you wrote yourself is overhead.

---

# EPIC 15 — Evaluation

### S-15.1 — Diagnostic agreement harness
Depends: S-12.1
Why: the headline reliability number, and nobody publishes it for this domain.
AC:
- Runs diagnosis N times independently on one repo with cache disabled
- Reports agreement on the primary finding as a percentage
- Reports the distribution of alternative findings
- Runs at N=10 minimum

### S-15.2 — Benchmark runner
Depends: S-12.1
AC:
- Runs a defined subset of SWE-Perf instances
- Compares against expert patches
- Reports per-category, not aggregate
- Subset size and selection criteria stated openly

### S-15.3 — Cost report
Depends: S-5.3
AC:
- Euros per confirmed finding, per run, per project
- Broken down by phase and model tier
- Cache hit rate and escalation rate included

### S-15.4 — Failure catalogue
Depends: S-15.1
Why: more credible than a success rate.
AC:
- Records repos where nothing was found
- Records caught cheats with the diff and the attack that caught them
- Records diagnoses that flipped between runs
- Records groundings that failed and why
- Published alongside results

---

# EPIC 16 — Reporting

### S-16.1 — Evidence chain rendering
Depends: S-8.6
AC:
- Human-readable report from an evidence chain
- Includes the growth table, exclusions with reasons, and the causal site
- Readable in under two minutes without re-deriving the reasoning

### S-16.2 — Pull request generation
Depends: S-16.1, S-11.7
AC:
- PR body contains: before/after on every varied axis, the evidence chain, guard metrics showing what did not regress, test results, Adversary verdict, and round-one reproducing cases if any
- The falsification test is attached as a permanent regression test
- `slack-reducing` patches carry the staging warning prominently

### S-16.3 — Null result report
Depends: S-4.5
AC:
- Structured report naming workloads screened, thresholds applied, and why nothing was flagged
- Distinguishes "healthy" from "insufficient data to tell"

---

# EPIC 17 — Hardening and release

### S-17.1 — End-to-end run on the holdout repo
Depends: E12
AC:
- Full pipeline runs on the repo reserved in S-0.6, never used in development
- Produces either a finding with a merged-quality PR, or an honest null result
- No manual intervention beyond approvals

### S-17.2 — Documentation
Depends: S-17.1
AC:
- Installation, configuration, and the four-line project config
- Adapter authoring guide
- An honest limitations page derived from `07-use-cases.md` §10

### S-17.3 — Real repository trial
Depends: S-17.1
AC:
- Run against at least three open-source projects
- At least one finding submitted as a real PR with the evidence chain
- Outcomes recorded in the failure catalogue regardless of result

---

## Dependency-ordered build sequence

Condensed to a single path for someone working alone:

```
S-0.1 → S-0.3 → S-0.4 → S-0.5 → S-0.2 → S-0.6 → S-0.7
S-1.1 → S-1.2 → S-1.3 → S-1.4 → S-1.5 → S-1.6 → S-1.7
S-2.1 → S-2.2 → S-2.3 → S-2.4 → S-2.5 → S-2.8 → S-2.6 → S-2.7
S-3.1 → S-3.2 → S-3.4 → S-3.6 → S-3.9 → S-3.8
S-4.1 → S-4.2 → S-4.3 → S-4.4 → S-4.5
S-5.1 → S-5.2 → S-5.3 → S-5.4
      ← M2 reached: a useful tool with zero model calls
S-6.1 → S-6.2 → S-6.3
S-5.5 → S-5.6 → S-5.7 → S-5.8
S-7.1 → ... → S-7.10
S-8.1 → ... → S-8.6 → S-8.7
      ← M3 reached: the thesis demo
S-9.1 → ... → S-9.8
S-10.1 → ... → S-10.6
S-11.1 → ... → S-11.8
S-12.1 → ... → S-12.6
      ← M4 reached: the contribution
E13 → E14 → E15 → E16 → E17
```

**Remaining primitives** (S-3.3, S-3.5, S-3.7, S-3.10 through S-3.18) slot in wherever the Diagnostician needs them. The registry in S-3.1 is what makes that possible — each is an afternoon, not a refactor.

---

## What this backlog decides that the design docs did not

| Decision | Where |
|---|---|
| Implementation language and repo layout | S-0.1 |
| LLM SDK and vendor strategy | S-0.2 |
| Persistence technology, both stores | S-0.2, S-6.1, S-6.2 |
| Adapter interface as code | S-14.1 |
| Development target and holdout | S-0.6 |
| How the tool tests itself | S-0.7 |
| Schemas as validated models | S-4.1, S-8.6, S-11.7 |
